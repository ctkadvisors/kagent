---
phase: 04-review-queue-projection-promotion-path
plan: 05
type: execute
wave: 4
depends_on: [04-01-PLAN.md, 04-02-PLAN.md, 04-04-PLAN.md]
files_modified:
  - packages/workbench-ui/src/command/state.ts
  - packages/workbench-ui/src/command/flows.ts
  - packages/workbench-ui/src/command/flows.test.ts
  - packages/workbench-ui/src/CommandView.tsx
  - packages/workbench-ui/src/command/__snapshots__/cc-reload.test.tsx.snap
  - docs/AGENT-TEMPLATES.md
  - docs/REPLAY-EVALS.md
  - docs/SUBSTRATE-V1.md
autonomous: true
requirements: [REV-01, REV-03]
notes: |
  Wave 4 — Phase 3 attention-flow flip + 3 docs additions. Closes Phase 4.

  Two distinct concerns kept in one plan because each is small and they
  share the "phase wrap-up" scope:

  (a) Phase 3 attention flow gauge `compute()` body flips from the v0.2
      `phase=Failed + suspicious` proxy to the new `reviewQueueRowCount`
      snapshot field. The `FlowGauge` shape is unchanged; only `compute()`,
      the flow's `sourceFields`, and the data-source-field DOM attribute
      change. Surgical ~10-15 line edit per CONTEXT.md `<integration_points>`
      + PATTERNS.md W4.2.

  (b) `cc-reload.test.tsx.snap` regen — Phase 3 pattern (snapshot regen
      lands in its OWN commit so the diff is reviewable in isolation per
      RESEARCH.md Pitfall 1 / LM-8). The snapshot diff is solely the
      data-source-field string change `'phase,suspicious'` ->
      `'reviewQueueRowCount'` plus any DOM additions from Plan 04 that
      affected the rendered tree.

  (c) `state.ts` `CommandSnapshot` adds optional `reviewQueueRowCount?: number`
      field; `CommandView.tsx` wires `useReviewQueue()` (or count-only
      fetch) into the snapshot per CONTEXT.md "Claude's Discretion"
      lifecycle-coupling note.

  (d) Three docs additions (W4.5 in PATTERNS.md):
      - `docs/AGENT-TEMPLATES.md` footer: documents the
        `application/x-kagent-template-candidate+yaml` media type and adds
        a "Promotion via review queue" subsection pointing at Phase 4.
      - `docs/REPLAY-EVALS.md` footer: adds a note pointing at Phase 4's
        REV-03 stub in `routes/review-queue.ts` so when AgentTaskRun ships
        the eval reducer knows to emit `replay-divergence` audit events
        the projection picks up.
      - `docs/SUBSTRATE-V1.md` §4.3: 4 new entries in the audit-event
        catalog table — total 49 -> 53.

must_haves:
  truths:
    - 'The §11 bounds-test (Phase 4 — applies to whole phase per CONTEXT.md `<specifics>` item 6).'
    - 'The §15 one-sentence test (Phase 4 — applies to whole phase per CONTEXT.md `<specifics>` item 7).'
    - '`packages/workbench-ui/src/command/state.ts` `CommandSnapshot` interface gains an OPTIONAL `reviewQueueRowCount?: number` field (additive; mirror the existing `dispositions` field at lines 53-67). Optional so tests not exercising the review-queue path stay back-compat.'
    - '`packages/workbench-ui/src/command/flows.ts` `attention` flow entry has its `sourceFields` flipped from `["phase", "suspicious"]` (Phase 3 lock) to `["reviewQueueRowCount"]` (single source). The `compute()` body changes from the v0.2 proxy `Array.from(snapshot.tasks.values()).filter(...).length` to `const count = s.reviewQueueRowCount ?? 0; if (count === 0) return []; return [{ kind: "attention", sourceFields: ["reviewQueueRowCount"], detailLink: "#/review", label: "review queue", value: count, unit: "items" }];` per PATTERNS.md W4.2.'
    - 'The `attention` flow `detailLink` flips from `"#/tasks"` to `"#/review"` (the new dedicated review page from Plan 04).'
    - 'The `attention` flow `label` flips from `"awaiting review queue projection — Phase 4"` (Phase 3 stub label) to `"review queue"` (post-flip per PATTERNS.md W4.2).'
    - 'The Phase 3 inline-comment block at `flows.ts:285-289` ("Phase 4 owns the real review queue projection") is REPLACED with the post-Phase-4 comment per PATTERNS.md W4.2 lines 1487-1491 verbatim. NO duplicate "old + new" comments left in the file.'
    - '`packages/workbench-ui/src/command/flows.test.ts` `attention` test cases are UPDATED: the existing fire/absent test pair now sets `s.reviewQueueRowCount = N` instead of synthesizing `Failed` tasks. The fire test asserts `count >= 1` -> 1 gauge with `value === count`. The absent test asserts `count === 0 || undefined` -> 0 gauges. NEW additional test asserts the `attention` entry has `sourceFields === ["reviewQueueRowCount"]`. Total `flows.test.ts` test count grows by 1 (the additional sourceFields-shape assertion); the existing 17+ fire/absent pairs stay green.'
    - '`packages/workbench-ui/src/CommandView.tsx` wires the review-queue row count into the `CommandSnapshot` consumed by `<FlowOverlay />`. Planner picks the cleanest integration per CONTEXT.md "Claude''s Discretion" lifecycle-coupling note: default — call `useReviewQueue()` in `CommandView.tsx`, read `.rows.length`, pass as `reviewQueueRowCount: rows.length` into the snapshot prop chain (or use a setter on a useMemo). Any choice that delivers `reviewQueueRowCount` to `flows.ts attention.compute(s)` is acceptable.'
    - '`packages/workbench-ui/src/command/__snapshots__/cc-reload.test.tsx.snap` is regenerated via `pnpm -C packages/workbench-ui test -u` AFTER the `flows.ts` and `state.ts` changes, AND lands in a SINGLE DEDICATED commit (RESEARCH.md Pitfall 1 / Phase 3 pattern). The snapshot diff is reviewer-scrutinizable in isolation.'
    - '`docs/AGENT-TEMPLATES.md` is extended with a footer section "## Promotion via review queue (Phase 4)" containing: the `application/x-kagent-template-candidate+yaml` media type definition; a 1-paragraph description of the producer/reviewer flow; a link back to `.planning/phases/04-review-queue-projection-promotion-path/04-CONTEXT.md`. Existing content is preserved unchanged.'
    - '`docs/REPLAY-EVALS.md` is extended with a footer note "## REV-03 stub — Phase 4 placement" containing: a description of the `replay-divergence` and `eval-failed` ReviewReason slots reserved in `routes/review-queue.ts`; a link to the inline comment block at the top of `review-queue.ts`; a "When AgentTaskRun ships..." paragraph explaining the promotion path. Existing content is preserved unchanged.'
    - '`docs/SUBSTRATE-V1.md` §4.3 audit-event catalog table is extended with the 4 new event types: `review.requested`, `review.accepted`, `review.rejected`, `template.candidate.promoted`. Each row carries a 1-sentence description matching the W0 audit-event JSDoc text.'
    - 'NO test regression. `pnpm -C packages/workbench-ui test --run` exits 0; `pnpm -w typecheck` exits 0; `pnpm -w build` exits 0.'
  artifacts:
    - path: 'packages/workbench-ui/src/command/state.ts'
      provides: 'Existing CommandSnapshot interface + new optional `reviewQueueRowCount?: number` field.'
      contains: 'reviewQueueRowCount'
    - path: 'packages/workbench-ui/src/command/flows.ts'
      provides: 'Existing 8 FLOW_TYPES + the `attention` entry MUTATED: sourceFields, compute body, detailLink, label all flipped per PATTERNS.md W4.2. Inline comment block replaced with post-Phase-4 wording.'
      contains: 'reviewQueueRowCount'
    - path: 'packages/workbench-ui/src/command/flows.test.ts'
      provides: 'Existing tests + UPDATED `attention` fire/absent pair using `reviewQueueRowCount` source instead of synthesized Failed tasks; +1 NEW `sourceFields` shape assertion test.'
      contains: 'reviewQueueRowCount'
    - path: 'packages/workbench-ui/src/CommandView.tsx'
      provides: 'Existing snapshot wiring + review-queue row-count integration.'
      contains: 'reviewQueueRowCount'
    - path: 'packages/workbench-ui/src/command/__snapshots__/cc-reload.test.tsx.snap'
      provides: 'Regenerated snapshot reflecting the data-source-field flip on the `attention` gauge. Lands in a SINGLE DEDICATED commit per RESEARCH.md Pitfall 1.'
      contains: 'reviewQueueRowCount'
    - path: 'docs/AGENT-TEMPLATES.md'
      provides: 'Existing AgentTemplate documentation + new footer section "## Promotion via review queue (Phase 4)" with media-type + producer/reviewer flow + link to Phase 4 CONTEXT.md.'
      contains: 'application/x-kagent-template-candidate+yaml'
    - path: 'docs/REPLAY-EVALS.md'
      provides: 'Existing Phase 5 design + new footer note "## REV-03 stub — Phase 4 placement" with replay-divergence/eval-failed slot description + promotion path note.'
      contains: 'REV-03'
    - path: 'docs/SUBSTRATE-V1.md'
      provides: 'Existing §4.3 audit-event catalog + 4 new rows for the Phase 4 event types.'
      contains: 'review.accepted'
  key_links:
    - from: 'packages/workbench-ui/src/command/flows.ts (attention compute body)'
      to: 'packages/workbench-ui/src/command/state.ts (CommandSnapshot.reviewQueueRowCount)'
      via: 's.reviewQueueRowCount ?? 0'
      pattern: 'reviewQueueRowCount'
    - from: 'packages/workbench-ui/src/CommandView.tsx'
      to: 'packages/workbench-ui/src/api.ts (useReviewQueue hook from Plan 04)'
      via: 'const { rows } = useReviewQueue(); ... reviewQueueRowCount: rows.length'
      pattern: 'useReviewQueue'
    - from: 'docs/AGENT-TEMPLATES.md footer'
      to: '.planning/phases/04-review-queue-projection-promotion-path/04-CONTEXT.md'
      via: 'markdown link to the CONTEXT.md decisions section'
      pattern: '04-CONTEXT.md'
    - from: 'docs/REPLAY-EVALS.md footer'
      to: 'packages/workbench-api/src/routes/review-queue.ts (W1 inline REV-03 comment)'
      via: 'markdown reference to the route module'
      pattern: 'review-queue\.ts'
    - from: 'docs/SUBSTRATE-V1.md §4.3'
      to: 'packages/audit-events/src/event-types.ts (W0 — 4 new const literals)'
      via: 'audit-event catalog rows for review.* + template.candidate.promoted'
      pattern: 'review\.accepted'
---

<objective>
Wave 4 — Phase 3 attention-flow flip + 3 docs additions. Closes Phase 4.

This plan ships:

1. `packages/workbench-ui/src/command/state.ts` (MODIFY) — add optional
   `reviewQueueRowCount?: number` field to `CommandSnapshot` (PATTERNS.md W4.3).
2. `packages/workbench-ui/src/command/flows.ts` (MODIFY) — flip the `attention`
   flow's `sourceField`, `compute()` body, `detailLink`, `label`, and inline
   comment block per PATTERNS.md W4.2.
3. `packages/workbench-ui/src/command/flows.test.ts` (MODIFY) — update the
   `attention` fire/absent pair to use `reviewQueueRowCount`; add +1
   `sourceFields`-shape assertion test.
4. `packages/workbench-ui/src/CommandView.tsx` (MODIFY) — wire `useReviewQueue()`
   (or count-only fetch) into the snapshot prop chain so
   `flows.ts attention.compute(s)` reads a non-undefined `s.reviewQueueRowCount`.
5. `packages/workbench-ui/src/command/__snapshots__/cc-reload.test.tsx.snap`
   (MODIFY) — regenerated via `vitest -u` after the source-field flip.
   **SINGLE DEDICATED COMMIT** (LM-8 / Phase 3 pattern).
6. `docs/AGENT-TEMPLATES.md` (MODIFY) — append "## Promotion via review queue
   (Phase 4)" footer.
7. `docs/REPLAY-EVALS.md` (MODIFY) — append "## REV-03 stub — Phase 4 placement"
   footer.
8. `docs/SUBSTRATE-V1.md` (MODIFY) — extend §4.3 audit-event catalog table
   with 4 new rows.

Output: 5 MODIFIED source/test files + 3 MODIFIED docs files.
Vitest is green: `pnpm -C packages/workbench-ui test --run` exits 0.

§11 bounds-test answer (this plan's slice): zero new substrate state;
~15 lines of `flows.ts` change + 1 line of `state.ts` change + 1 wire in
`CommandView.tsx` + 1 snapshot regen commit + 3 docs footers. Revocation
is `git revert`-clean.

§15 one-sentence test answer (this plan's slice): Flipping the Phase 3
`attention` gauge from a `phase=Failed + suspicious` proxy to the real
`/api/review-queue` row count — paired with the AgentTemplate-promotion
media-type doc, the REV-03 stub forward-promotion-path doc, and the
extended audit-event catalog — closes the Phase 3 / Phase 4 boundary
callback and forward-compatibility-flags the Phase 5+ promotion paths
without expanding substrate primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-review-queue-projection-promotion-path/04-CONTEXT.md
@.planning/phases/04-review-queue-projection-promotion-path/04-RESEARCH.md
@.planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md
@.planning/phases/04-review-queue-projection-promotion-path/04-VALIDATION.md
@.planning/phases/04-review-queue-projection-promotion-path/04-01-PLAN.md
@.planning/phases/04-review-queue-projection-promotion-path/04-02-PLAN.md
@.planning/phases/04-review-queue-projection-promotion-path/04-04-PLAN.md
@.planning/phases/03-resource-flow-overlays/03-01-PLAN.md
@.planning/phases/03-resource-flow-overlays/03-02-PLAN.md
@docs/COMMAND-CENTER-CONTRACT.md
@docs/AGENT-TEMPLATES.md
@docs/REPLAY-EVALS.md
@docs/SUBSTRATE-V1.md
@packages/workbench-ui/src/command/state.ts
@packages/workbench-ui/src/command/flows.ts
@packages/workbench-ui/src/command/flows.test.ts
@packages/workbench-ui/src/CommandView.tsx
@packages/workbench-ui/src/command/cc-reload.test.tsx
@packages/workbench-ui/src/command/__snapshots__/cc-reload.test.tsx.snap

<interfaces>

From packages/workbench-ui/src/command/state.ts (lines 48-71 — CommandSnapshot interface):

```typescript
export interface CommandSnapshot {
  readonly agents: ReadonlyMap<string, AgentSummaryRow>;
  readonly tasks: ReadonlyMap<string, TaskSummary>;
  readonly gatewayCapacity: readonly GatewayCapacityRow[];
  readonly gatewayUsage: readonly GatewayUsageRow[];
  readonly dispositions: ReadonlyMap<string, DispositionOverlayRow>;
  readonly events: readonly CacheEvent[];
  readonly lastEventAt: number;
  readonly error: string | null;
  // Phase 4 will add: readonly reviewQueueRowCount?: number;
}
```

From PATTERNS.md W4.2 (the post-Phase-4 form to drop into flows.ts):

```typescript
// ─────────────────────────── attention ───────────────────────────
// Phase 4 — source flipped to /api/review-queue rows count.
// REV-03: replay-divergence and eval-failed reasons are reserved for
// AgentTaskRun + @kagent/eval (docs/REPLAY-EVALS.md, Phase 5 design,
// pre-implementation as of 2026-05-10). v0.2 producers: zero.
{
  kind: 'attention',
  granularity: 'substrateWide',
  sourceFields: ['reviewQueueRowCount'],
  compute: (s): readonly FlowGauge[] => {
    const count = s.reviewQueueRowCount ?? 0;
    if (count === 0) return [];
    return [{
      kind: 'attention',
      sourceFields: ['reviewQueueRowCount'],
      detailLink: '#/review',
      label: 'review queue',
      value: count,
      unit: 'items',
    }];
  },
  detailLink: (): string => '#/review',
},
```

From packages/workbench-ui/src/api.ts (Plan 04 W3 — useReviewQueue hook):

```typescript
export function useReviewQueue(): {
  rows: readonly ReviewQueueRow[];
  loading: boolean;
  error: string | null;
  refresh: () => void;
};
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (W4-A): Add reviewQueueRowCount to CommandSnapshot in state.ts</name>

<read_first> - packages/workbench-ui/src/command/state.ts (lines 48-71 — `CommandSnapshot` interface; lines 84-end — `useCommandSnapshot()` hook to confirm whether the snapshot is constructed in-hook OR passed in) - packages/workbench-ui/src/command/state.test.ts (full file — confirm test pattern; verify the addition does not break existing tests) - .planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md (W4.3 — verbatim CommandSnapshot extension code)
</read_first>

  <files>
    packages/workbench-ui/src/command/state.ts (MODIFY — add optional reviewQueueRowCount field)
  </files>

  <action>
    1. Open `packages/workbench-ui/src/command/state.ts`. Locate the `CommandSnapshot` interface (lines 48-71).

    2. Add `readonly reviewQueueRowCount?: number;` to the interface immediately after the existing `dispositions` field. Add a leading JSDoc:
       ```typescript
       /**
        * Phase 4 / REV-01 — count of rows in /api/review-queue. Optional so
        * the snapshot stays back-compat for tests that do not poll the route.
        * Wired in CommandView via useReviewQueue().rows.length OR a count-only
        * fetch (CONTEXT.md D-01-A "lifecycle coupling" note — planner picks).
        */
       readonly reviewQueueRowCount?: number;
       ```

    3. If the `useCommandSnapshot()` hook constructs the snapshot in-hook (vs receiving it as a prop), update the construction site to include `reviewQueueRowCount` ONLY IF the hook also calls `useReviewQueue()` here. If the hook is purely about other data sources, leave the wiring for Task 3 (CommandView.tsx).

    4. Run `pnpm -C packages/workbench-ui typecheck`.

    5. Run `pnpm -C packages/workbench-ui test --run state` — existing tests pass (the addition is additive optional).

    6. Atomic commit: `feat(phase-04-w4): add reviewQueueRowCount field to CommandSnapshot (REV-01)`. Co-author.

  </action>

  <verify>
    <automated>grep -q "reviewQueueRowCount" packages/workbench-ui/src/command/state.ts && pnpm -C packages/workbench-ui test --run state --reporter=verbose && pnpm -C packages/workbench-ui typecheck</automated>
  </verify>

  <done>
    - `CommandSnapshot` has the new optional field.
    - Existing state tests pass.
    - Typecheck passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (W4-B): Flip flows.ts attention compute body + update flows.test.ts</name>

<read_first> - packages/workbench-ui/src/command/flows.ts (lines 285-314 — current attention block; surrounding 8-flow array structure) - packages/workbench-ui/src/command/flows.test.ts (full file — locate the existing `attention fires` and `attention empty` tests; understand the makeSnapshot/makeTask helpers) - packages/workbench-ui/src/command/state.ts (Task 1 of this plan — confirm `reviewQueueRowCount?: number` is present) - .planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md (W4.2 — verbatim post-Phase-4 attention block) - .planning/phases/03-resource-flow-overlays/03-01-PLAN.md (Phase 3 attention flow stub — confirm the v0.2 form being replaced)
</read_first>

  <files>
    packages/workbench-ui/src/command/flows.ts (MODIFY — flip attention compute body)
    packages/workbench-ui/src/command/flows.test.ts (MODIFY — update attention test pair + add sourceFields shape test)
  </files>

  <behavior>
    Tests written FIRST (RED — update the existing attention pair to use the new source field; the existing implementation will fail until the implementation flip below).

    flows.test.ts test cases (UPDATE existing 2 + ADD 1):

    - **UPDATED `attention fires`** — given `s.reviewQueueRowCount = 3`, `compute()` returns 1 gauge with `kind === 'attention'`, `value === 3`, `unit === 'items'`, `detailLink === '#/review'`, `label === 'review queue'`, `sourceFields === ['reviewQueueRowCount']`.
    - **UPDATED `attention empty`** — given `s.reviewQueueRowCount = 0` OR `undefined`, `compute()` returns `[]`.
    - **NEW `attention sourceFields shape`** — assert that the `attention` entry in `FLOW_TYPES` has `sourceFields === ['reviewQueueRowCount']` (single-source, NOT the previous `['phase', 'suspicious']` multi-source).

    Implementation requirements (flows.ts):

    - Locate the `attention` entry in `FLOW_TYPES` (lines 285-314 per Phase 3 lock).
    - **REPLACE the inline comment block** (lines 285-289 per PATTERNS.md W4.2 landmine — the existing comment says "Phase 4 owns the real review queue projection") with the post-Phase-4 wording verbatim per PATTERNS.md W4.2 lines 1487-1491. DO NOT leave both old and new comments — REPLACE.
    - Update the `attention` entry's `sourceFields` field from `['phase', 'suspicious']` (or whatever the Phase 3 lock used) to `['reviewQueueRowCount']`.
    - Update `compute()` body to read `s.reviewQueueRowCount ?? 0` and return the single-gauge form per PATTERNS.md W4.2 lines 1496-1507 verbatim.
    - Update `detailLink: '#/tasks'` -> `'#/review'` in the returned gauge.
    - Update `label: 'awaiting review queue projection — Phase 4'` (Phase 3 stub) -> `'review queue'`.
    - Update the entry-level `detailLink: () => '#/tasks'` -> `'#/review'`.

    The other 7 FLOW_TYPES entries are UNCHANGED.

  </behavior>

  <action>
    1. Modify `packages/workbench-ui/src/command/flows.test.ts`:
       - Update the existing `attention fires` test: replace the synthesized Failed task setup with `s.reviewQueueRowCount = 3`. Update assertions to check `value === 3`, `detailLink === '#/review'`, `label === 'review queue'`, `sourceFields === ['reviewQueueRowCount']`.
       - Update the existing `attention empty` test: set `s.reviewQueueRowCount = 0` (or omit -> undefined). Assert `compute()` returns `[]`.
       - Add a new test `attention sourceFields shape`: load `FLOW_TYPES`, find the entry with `kind === 'attention'`, assert its `sourceFields === ['reviewQueueRowCount']`.
       - Run `pnpm -C packages/workbench-ui test --run flows` — the updated attention tests FAIL because `flows.ts` still uses the v0.2 proxy (RED phase).

    2. Modify `packages/workbench-ui/src/command/flows.ts`:
       - REPLACE the inline comment at lines 285-289 with the post-Phase-4 wording verbatim per PATTERNS.md W4.2.
       - Update the `attention` entry's `sourceFields`, `compute()` body, `detailLink`, `label` per the behavior block.
       - Verify the other 7 entries remain UNCHANGED (no accidental edits).

    3. Run `pnpm -C packages/workbench-ui test --run flows` — all flows tests pass (the 17+ existing fire/absent pairs for the other 7 flows + the 3 updated `attention` tests).

    4. Run `pnpm -C packages/workbench-ui typecheck`.

    5. Atomic commit: `feat(phase-04-w4): flip attention flow gauge to /api/review-queue source (REV-01)`. Co-author.

    Per RESEARCH.md Pitfall 1 + LM-8: the snapshot regen lands in the NEXT commit (Task 4). DO NOT include the snapshot diff in this commit.

  </action>

  <verify>
    <automated>pnpm -C packages/workbench-ui test --run flows --reporter=verbose && pnpm -C packages/workbench-ui typecheck && grep -q "reviewQueueRowCount" packages/workbench-ui/src/command/flows.ts && grep -q "'#/review'" packages/workbench-ui/src/command/flows.ts && ! grep -q "awaiting review queue projection" packages/workbench-ui/src/command/flows.ts</automated>
  </verify>

  <done>
    - `flows.ts` `attention` entry uses `reviewQueueRowCount` source.
    - The Phase 3 stub label and detailLink are flipped.
    - `flows.test.ts` attention pair is updated; 3 attention tests pass.
    - The other 7 flow entries are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3 (W4-C): Wire useReviewQueue() into CommandView.tsx so reviewQueueRowCount reaches the snapshot</name>

<read_first> - packages/workbench-ui/src/CommandView.tsx (full file — find the snapshot construction site; look for `useCommandSnapshot()` call OR a manual snapshot literal; identify where to inject `reviewQueueRowCount`) - packages/workbench-ui/src/api.ts (Plan 04 W3 — `useReviewQueue` hook signature) - packages/workbench-ui/src/command/state.ts (Task 1 of this plan — confirm `reviewQueueRowCount?: number` field is present) - .planning/phases/04-review-queue-projection-promotion-path/04-CONTEXT.md (D-01-A "Claude's Discretion" lifecycle-coupling note) - .planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md (W4.4 — wiring options)
</read_first>

  <files>
    packages/workbench-ui/src/CommandView.tsx (MODIFY — wire useReviewQueue rows.length into snapshot)
  </files>

  <action>
    1. Open `packages/workbench-ui/src/CommandView.tsx`. Locate the snapshot construction:
       - If the component calls `useCommandSnapshot()` and receives a snapshot directly: extend the snapshot via spread-merge after the hook call:
         ```typescript
         const baseSnapshot = useCommandSnapshot();
         const { rows: reviewRows } = useReviewQueue();
         const snapshot = useMemo(() => ({ ...baseSnapshot, reviewQueueRowCount: reviewRows.length }), [baseSnapshot, reviewRows.length]);
         ```
       - If the component constructs the snapshot inline: add `reviewQueueRowCount: reviewRows.length` to the snapshot literal, with `reviewRows` from `useReviewQueue()`.

    2. Add the import: `import { useReviewQueue } from './api.js';`.

    3. The existing rendering of `<FlowOverlay snapshot={snapshot} />` (or wherever the snapshot is consumed by flow gauges) now passes the extended snapshot transparently — `flows.ts attention.compute(s)` reads `s.reviewQueueRowCount` from the wired source.

    4. Verify that `useReviewQueue()` polling does NOT cause excessive re-renders of the CommandView. The hook polls every 5s; the snapshot's `reviewQueueRowCount` only changes when rows are added/removed/decided. `useMemo` on the snapshot shape avoids needless re-renders of unrelated subtrees.

    5. Run `pnpm -C packages/workbench-ui test --run CommandView` — existing tests pass. If any test mocks `useCommandSnapshot`, those tests also need to mock `useReviewQueue` to return `{ rows: [], loading: false, error: null, refresh: vi.fn() }` (planner verifies and adds mocks where needed).

    6. Run `pnpm -C packages/workbench-ui typecheck` and `pnpm -C packages/workbench-ui build` — both succeed.

    7. Atomic commit: `feat(phase-04-w4): wire useReviewQueue into CommandView snapshot (REV-01)`. Co-author.

  </action>

  <verify>
    <automated>pnpm -C packages/workbench-ui test --run CommandView --reporter=verbose && pnpm -C packages/workbench-ui typecheck && grep -q "useReviewQueue" packages/workbench-ui/src/CommandView.tsx && grep -q "reviewQueueRowCount" packages/workbench-ui/src/CommandView.tsx</automated>
  </verify>

  <done>
    - `CommandView.tsx` imports and uses `useReviewQueue`.
    - The snapshot consumed by `<FlowOverlay />` carries `reviewQueueRowCount`.
    - Typecheck passes; build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 4 (W4-D): Regenerate cc-reload.test.tsx.snap (single dedicated commit per LM-8)</name>

<read_first> - packages/workbench-ui/src/command/cc-reload.test.tsx (full file — confirm the test still asserts deep-equality across reload; the snapshot regen is the data-source-field text-string update) - packages/workbench-ui/src/command/**snapshots**/cc-reload.test.tsx.snap (full file — current contents; confirm the `'phase,suspicious'` string appears for the `attention` gauge) - .planning/phases/03-resource-flow-overlays/03-02-PLAN.md (Phase 3's snapshot regen pattern — landed in its OWN commit per RESEARCH.md Pitfall 1 / LM-8) - .planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md (LM-8 — verbatim guidance: `pnpm -C packages/workbench-ui test -u`)
</read_first>

  <files>
    packages/workbench-ui/src/command/__snapshots__/cc-reload.test.tsx.snap (MODIFY — vitest -u regenerated)
  </files>

  <action>
    1. Verify Tasks 1-3 are committed; the `cc-reload.test.tsx` test currently FAILS because the rendered DOM contains `data-source-field="reviewQueueRowCount"` (post-flip) but the committed snapshot contains `data-source-field="phase,suspicious"` (pre-flip).

    2. Run `pnpm -C packages/workbench-ui test -u cc-reload` to regenerate the snapshot ONLY for `cc-reload.test.tsx`.

    3. Inspect the diff via `git diff packages/workbench-ui/src/command/__snapshots__/cc-reload.test.tsx.snap`:
       - The diff should be small: the data-source-field string changes from `phase,suspicious` (or whatever Phase 3 lock used) to `reviewQueueRowCount` on the `attention` gauge cell.
       - If Plan 04 W3 added new DOM (e.g., a deep-link from AgentPanel/TaskPanel to `#/review`), additional benign additions in the snapshot are expected — verify each one corresponds to a known Plan 04 change.
       - If the diff is unexpectedly large (e.g., touches unrelated overlay nodes), STOP — investigate before committing. The expected diff is surgical.

    4. Run `pnpm -C packages/workbench-ui test --run cc-reload` — passes (the snapshot now matches the rendered output).

    5. Run `pnpm -C packages/workbench-ui test --run` — full UI suite passes.

    6. Atomic commit: `test(phase-04-w4): regen cc-reload.test.tsx.snap after attention-flow flip`. Co-author. The commit body explains the Phase 3 / Phase 4 boundary callback.

    Per RESEARCH.md Pitfall 1 / LM-8: this commit MUST contain ONLY the snapshot file diff. NO source-code changes ride alongside the snapshot regen.

  </action>

  <verify>
    <automated>pnpm -C packages/workbench-ui test --run cc-reload --reporter=verbose && grep -q "reviewQueueRowCount" packages/workbench-ui/src/command/__snapshots__/cc-reload.test.tsx.snap && ! grep -q "phase,suspicious" packages/workbench-ui/src/command/__snapshots__/cc-reload.test.tsx.snap</automated>
  </verify>

  <done>
    - `cc-reload.test.tsx.snap` regenerated; the diff is surgical (data-source-field string flip + any benign Plan 04 additions).
    - `cc-reload.test.tsx` passes.
    - Full UI suite passes.
    - The snapshot regen is in its own commit (no other source changes ride alongside).
  </done>
</task>

<task type="auto">
  <name>Task 5 (W4-E): Append docs footers to AGENT-TEMPLATES.md, REPLAY-EVALS.md, SUBSTRATE-V1.md</name>

<read_first> - docs/AGENT-TEMPLATES.md (full file — confirm existing structure; identify the natural footer location after current content) - docs/REPLAY-EVALS.md (full file — confirm Phase 5 design status; identify natural footer location) - docs/SUBSTRATE-V1.md (§4.3 — locate the audit-event catalog table; identify the row-format pattern used for existing 49 entries) - .planning/phases/04-review-queue-projection-promotion-path/04-CONTEXT.md (the canonical decision corpus to reference in the AgentTemplates footer) - .planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md (W4.5 — verbatim doc footer guidance) - packages/workbench-api/src/routes/review-queue.ts (W1 — the inline REV-03 comment block to reference in REPLAY-EVALS.md footer) - packages/audit-events/src/event-types.ts (W0 — confirm the 4 new const literals + the JSDoc comment text to copy into the SUBSTRATE-V1.md catalog rows)
</read_first>

  <files>
    docs/AGENT-TEMPLATES.md (MODIFY — append footer)
    docs/REPLAY-EVALS.md (MODIFY — append footer)
    docs/SUBSTRATE-V1.md (MODIFY — extend §4.3 catalog table)
  </files>

  <action>
    1. Append to `docs/AGENT-TEMPLATES.md` — at the end of the file, add a new section:

       ```markdown
       ## Promotion via review queue (Phase 4)

       Candidate AgentTemplates produced by agents (DISP-02 `proposalScope.mayProposeAgainst: ['templates']` allowed) are carried as `ArtifactRef`-shaped blobs at rest. The producing AgentTask carries the annotation `kagent.knuteson.io/template-candidate: "true"`; the artifact's media type is `application/x-kagent-template-candidate+yaml`.

       The candidate surfaces as a row in the workbench-api review-queue projection (`GET /api/review-queue`, `reason: "candidate-template"`). An operator-reviewer's `POST /api/review-queue/:namespace/:name/accept` parses the artifact YAML against `AgentTemplateSpec` and creates the new `AgentTemplate` CR via the existing operator-write path. The promoted CR carries `metadata.ownerReferences` to the producing AgentTask and `metadata.annotations["kagent.knuteson.io/promoted-from-task"] = "<ns>/<name>"`. The audit-event log records `review.accepted` + `template.candidate.promoted` (Phase 4 / REV-02).

       Single-reviewer scope per Phase 4. Multi-reviewer flows (signed quorum, no-self-review, ring-review detection) are future research per `.planning/REQUIREMENTS.md` §4 (`CoalitionProposal`).

       See `.planning/phases/04-review-queue-projection-promotion-path/04-CONTEXT.md` for the full decision corpus (D-01..D-04).
       ```

    2. Append to `docs/REPLAY-EVALS.md` — at the end of the file, add a new section:

       ```markdown
       ## REV-03 stub — Phase 4 placement

       Phase 4 reserves two slots in the `ReviewReason` closed-enum (`@kagent/dto/review-queue.ts`): `replay-divergence` and `eval-failed`. v0.2 producers: zero. The Phase 4 review-queue route (`packages/workbench-api/src/routes/review-queue.ts`) carries an inline comment block at the top of the module documenting the slot and the promotion path.

       When the Phase 5 design ships (`AgentTaskRun` CRD + `@kagent/eval` package + `ReplaySet` controller), the eval reducer should emit `replay-divergence` audit events the projection picks up. Concretely, the projection classifier (`classifyTask`) gains a step 2.5: "if any `AgentTaskRun` for this task has `terminalStatus='replayed-divergence'`, emit reason `replay-divergence` with `replayDivergence` populated." The `ReviewQueueRow` shape is unchanged; the projection module's `compute()` body grows ~15 lines.

       Until then, `verifier-failed` and `suspicious-detector` cover what `.planning/REQUIREMENTS.md` REV-03 calls "replay/eval signals" today.
       ```

    3. Modify `docs/SUBSTRATE-V1.md` §4.3 — extend the audit-event catalog table with 4 new rows (matching the existing row format). Each row carries the event-type literal + a 1-sentence description matching the W0 audit-event JSDoc text:

       ```markdown
       | `review.requested`           | Phase 4 / REV-02 — emitted when an operator POSTs `/api/review-queue/:ns/:name/request` to flag a Completed-clean task for review. |
       | `review.accepted`            | Phase 4 / REV-02 — emitted on accept (always). For `candidate-template` accepts, additionally fires `template.candidate.promoted`. |
       | `review.rejected`            | Phase 4 / REV-02 — emitted on reject; no AgentTemplate CR creation under any reason. |
       | `template.candidate.promoted`| Phase 4 / REV-02 — emitted when accept-on-candidate-template creates an AgentTemplate CR via the existing operator-write path. Distinct from `review.accepted` so consumers can split promotion events from review-decision events. |
       ```

       Verify the existing 49-row count grows to 53. Update any explicit count in the surrounding doc text if present.

    4. NO test runs required (docs files only). Run `pnpm -w build` to verify nothing else regressed.

    5. Atomic commit: `docs(phase-04-w4): add review-queue + REV-03 stub + audit-event catalog footers`. Co-author.

  </action>

  <verify>
    <automated>grep -q "Promotion via review queue" docs/AGENT-TEMPLATES.md && grep -q "application/x-kagent-template-candidate+yaml" docs/AGENT-TEMPLATES.md && grep -q "REV-03 stub" docs/REPLAY-EVALS.md && grep -q "replay-divergence" docs/REPLAY-EVALS.md && grep -q "review.accepted" docs/SUBSTRATE-V1.md && grep -q "template.candidate.promoted" docs/SUBSTRATE-V1.md && pnpm -w build</automated>
  </verify>

  <done>
    - `AGENT-TEMPLATES.md` has the "Promotion via review queue (Phase 4)" footer.
    - `REPLAY-EVALS.md` has the "REV-03 stub — Phase 4 placement" footer.
    - `SUBSTRATE-V1.md` §4.3 catalog has 4 new rows; total grows 49 -> 53.
    - `pnpm -w build` succeeds.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                                                                                                                                                                    |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `useReviewQueue` polling -> CommandView re-renders | The 5s polling cadence drives `reviewQueueRowCount` updates; any consumer that re-renders on count change pays the cost. `useMemo` on the snapshot shape limits propagation.                                                                   |
| Snapshot regen vs accidental DOM addition          | The `cc-reload.test.tsx.snap` regen captures whatever was in the rendered tree at regen time. If Plan 04 introduced unrelated DOM additions, those slip into the snapshot silently. The Task 4 diff-inspection step is the human-side defense. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                     |
| ---------- | ---------------------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-W4-01 | Tampering              | `cc-reload.test.tsx.snap` regen    | mitigate    | Per Task 4 step 3, the executor inspects the diff before committing. Unexpected DOM additions trigger STOP-and-investigate. The snapshot regen lands in a single dedicated commit so reviewer scrutiny is concentrated.             |
| T-04-W4-02 | DoS                    | Polling cascade in CommandView     | accept      | `useReviewQueue` polls at 5s; `useMemo` on snapshot prevents unrelated subtree re-renders. Future work: pause polling when `document.hidden`.                                                                                       |
| T-04-W4-03 | Repudiation            | Phase 3 attention-flow source flip | mitigate    | The Phase 3 inline-comment block at `flows.ts:285-289` is REPLACED with the post-Phase-4 wording. The git history preserves the v0.2 form via the prior commit. The new comment cites `docs/REPLAY-EVALS.md` for forward-promotion. |
| T-04-W4-04 | Information Disclosure | Docs footer cross-references       | accept      | Docs link to `.planning/phases/04-...` paths and to source files at `packages/workbench-api/src/routes/review-queue.ts`. No PII / secrets / cluster-internal info; accepted.                                                        |

</threat_model>

<verification>
- Phase 3 `attention` flow gauge reads from `s.reviewQueueRowCount` post-flip
- `cc-reload.test.tsx.snap` regenerated in a single dedicated commit (LM-8)
- All UI tests pass (`pnpm -C packages/workbench-ui test --run`)
- Whole-workspace typecheck + build pass
- 3 docs footers present and grep-able
- Audit-event catalog count grows 49 -> 53
</verification>

<success_criteria>

- The Phase 3 attention gauge displays the real review-queue row count.
- The `cc-reload.test.tsx.snap` diff is surgical and reviewer-scrutinized.
- Three docs are extended with forward-compatibility footers.
- No test regressions; whole workspace builds clean.
- Phase 4 is COMPLETE — REV-01 (server projection + UI), REV-02 (single-reviewer accept/reject + AgentTemplate promotion), REV-03 (forward-compatible stub) all delivered.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-review-queue-projection-promotion-path/04-05-SUMMARY.md` with:
- Commit list in order (state.ts, flows.ts/test.ts, CommandView.tsx, snap regen, docs).
- Snapshot diff summary (lines added/changed/removed).
- Whether the docs footer text was added verbatim from the plan or rephrased.
- Phase 4 close-out note: "Phase 4 complete. REV-01, REV-02, REV-03 all delivered. Roadmap status: Phase 4 -> COMPLETE; Phase 5 (Workbench usability primitives) is next."

Also update `.planning/STATE.md` and `.planning/ROADMAP.md`:

- STATE.md `## Current Position`: phase 4 -> COMPLETE; "60% (3 of 5)" -> "80% (4 of 5)".
- ROADMAP.md Phase 4 row: `[ ]` -> `[x]`.
- ROADMAP.md `## Progress` table: Phase 4 plans complete count -> "5/5", status -> "Complete".
  </output>
