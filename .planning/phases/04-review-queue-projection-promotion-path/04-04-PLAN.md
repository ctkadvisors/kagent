---
phase: 04-review-queue-projection-promotion-path
plan: 04
type: execute
wave: 3
depends_on: [04-01-PLAN.md, 04-02-PLAN.md]
files_modified:
  - packages/workbench-ui/src/types.ts
  - packages/workbench-ui/src/api.ts
  - packages/workbench-ui/src/api.test.ts
  - packages/workbench-ui/src/App.tsx
  - packages/workbench-ui/src/ReviewPage.tsx
  - packages/workbench-ui/src/ReviewPage.module.css
  - packages/workbench-ui/src/ReviewPage.test.tsx
  - packages/workbench-ui/src/command/ReviewActions.tsx
  - packages/workbench-ui/src/command/ReviewActions.module.css
  - packages/workbench-ui/src/command/ReviewActions.test.tsx
  - packages/workbench-ui/src/TaskDetail.tsx
  - packages/workbench-ui/src/command/source-binding.ts
  - packages/workbench-ui/src/command/source-binding.test.ts
autonomous: true
requirements: [REV-01, REV-02]
notes: |
  Wave 3 — UI surface. Reviewer-facing entry points + DTO re-exports +
  `data-source-field` extension per D7 / CC-01. Depends ONLY on W0 + W1
  (NOT on W2 POST handlers); the UI ships before the POST handlers go
  green is theoretically possible BUT the Accept/Reject buttons require
  the W2 endpoints to be live. Practical execution order: W3 ships AFTER
  W2 lands so the UI Accept/Reject buttons test against real backend
  routes (not just stubs). Wave structure is W0 -> W1 -> W2 (parallelizable
  with W3 read-side) -> W3 -> W4.

  This plan does NOT include the Phase 3 attention-flow flip — that lands
  in Plan 05 W4 to keep the snapshot regen commit (`cc-reload.test.tsx.snap`)
  isolated for reviewer scrutiny per Phase 3 pattern.

  D-03 two-entry-point design:
    1. Dedicated `#/review` page (`ReviewPage.tsx`) mirroring `TaskList.tsx`
       table layout; per-row Accept/Reject/Open Detail buttons.
    2. Inline `<ReviewActions />` component in `TaskDetail.tsx` for tasks
       matching the 4 trigger conditions (phase==='Failed', suspicious
       length>0, review-requested annotation, template-candidate annotation).

  Confirm-dialog modal pattern mirrors `NewTaskModal.tsx` (backdrop,
  Escape-to-close, role="dialog" aria-modal, no formal focus-trap per
  the existing precedent).

  Source-binding extension per CC-01 / D7: `ReviewQueueFieldName` closed
  enum extends `source-binding.ts`; every cell in `ReviewPage`
  carries `data-source-field` per Phase 1/2 pattern. CC-01-style orphan
  assertions fire in dev when a row is rendered without backing source
  data. The orphan assertion in `ReviewPage` uses the same generic
  `assertSourceField` helper Phase 2 shipped — no helper-code change.

  Polling cadence: 5s default per CONTEXT.md D-01-A "Claude's Discretion".
  SSE-driven invalidation deferred per CONTEXT.md (defer to follow-up;
  v0.2 polls).

must_haves:
  truths:
    - 'The §11 bounds-test (Phase 4 — applies to whole phase per CONTEXT.md `<specifics>` item 6).'
    - 'The §15 one-sentence test (Phase 4 — applies to whole phase per CONTEXT.md `<specifics>` item 7).'
    - '`packages/workbench-ui/src/types.ts` re-exports `ReviewQueueRow`, `ReviewReason`, and `ArtifactRefSummary` (if applicable) from `@kagent/dto/review-queue` per Phase 1 / DISP-04 pattern (existing `DispositionOverlayRow` re-export at lines 29-33 is the analog).'
    - '`packages/workbench-ui/src/api.ts` exports `fetchReviewQueue(signal?: AbortSignal): Promise<ReviewQueueRow[]>` mirroring `fetchDispositions` shape; uses `assertIsReviewQueueRow` per item drift defense; throws on non-2xx response.'
    - '`api.ts` exports `acceptReviewQueueRow(namespace, name, body): Promise<AcceptReviewResponse>`, `rejectReviewQueueRow(namespace, name, body): Promise<RejectReviewResponse>`, `requestReview(namespace, name, body): Promise<RequestReviewResponse>` POST helpers mirroring `createTask` shape; throws `ReviewActionApiError` (custom Error subclass with `status: number` field) on non-2xx.'
    - '`api.ts` exports `useReviewQueue(): { rows: readonly ReviewQueueRow[]; loading: boolean; error: string | null; refresh: () => void }` hook with 5s polling default; AbortController per refresh; cleanup on unmount.'
    - '`packages/workbench-ui/src/App.tsx` adds `ReviewRoute = { kind: "review" }` to the `Route` union; `parseHash` returns `{ kind: "review" }` when the hash is `#/review`; the `App` component mounts `<ReviewPage onBack={...} />` for that route.'
    - '`packages/workbench-ui/src/ReviewPage.tsx` renders a table mirroring `TaskList.tsx` layout: column headers `Reason | Task | Agent | Reason Detail | Staleness | Actions`; per-row Accept/Reject buttons opening a confirm dialog; per-row Open Detail link to `#/tasks/<ns>/<name>`. Loading and error states. Uses the `useReviewQueue` hook directly OR `useEffect`+`fetchReviewQueue` inline (planner picks).'
    - 'Every cell in `ReviewPage.tsx` rendering a `ReviewQueueRow` field carries `data-source-field={useSourceField(<ReviewQueueFieldName>)}` per D7 / CC-01. `assertSourceField(row, <field>)` is called on row-level fields per Phase 1/2 pattern (dev-only no-op in prod). The closed-enum is `ReviewQueueFieldName` (extension landed in this plan as `source-binding.ts` modification).'
    - '`packages/workbench-ui/src/ReviewPage.module.css` defines styles for the table, the per-reason pill (color-coded per ReviewReason value), the action-buttons row, and the modal backdrop+dialog. Mirror `TaskList.module.css` shape.'
    - '`packages/workbench-ui/src/command/ReviewActions.tsx` exports `ReviewActions({ task, onDecision }): React.JSX.Element | null` that mounts ONLY when the task matches one of the 4 trigger conditions (D-03): `task.phase === "Failed"` OR `(task.suspicious?.length ?? 0) > 0` OR `annotations["kagent.knuteson.io/review-requested"] === "true"` OR `annotations["kagent.knuteson.io/template-candidate"] === "true"`. Returns `null` otherwise. Renders Accept/Reject buttons + same confirm modal as ReviewPage.'
    - 'Confirm dialog mirrors `NewTaskModal.tsx`: backdrop click closes, Escape-key closes, `role="dialog" aria-modal="true" aria-labelledby={titleId}`, primary input has initial focus via `useEffect` + `ref.current?.focus()`. NO formal focus-trap (matches NewTaskModal precedent).'
    - '`packages/workbench-ui/src/TaskDetail.tsx` mounts `<ReviewActions task={detail} onDecision={refetch} />` ABOVE `<DetailBody>` for tasks matching the 4 trigger conditions (the eligibility check lives inside `ReviewActions` itself; `TaskDetail.tsx` always passes through and the component returns null when ineligible).'
    - '`packages/workbench-ui/src/command/source-binding.ts` exports `ReviewQueueFieldName` closed-enum union with EXACTLY 14 string-literal members matching `ReviewQueueRow` field names: `"taskRef" | "reason" | "reasonDetail" | "enqueuedAt" | "stalenessSeconds" | "phase" | "targetAgent" | "model" | "suspicious" | "verifierError" | "traceLink" | "artifactCount" | "candidateTemplate" | "replayDivergence"` (PATTERNS.md W4.1).'
    - '`packages/workbench-ui/src/command/source-binding.test.ts` adds tests proving `assertSourceField<ReviewQueueFieldName>` and `useSourceFields<ReviewQueueFieldName>` narrow correctly (mirror existing PressureFieldName / FlowFieldName narrowing tests).'
    - 'Test count: ≥6 new tests in `ReviewPage.test.tsx` (render-with-mocked-rows, click Accept -> confirm dialog -> POST -> row removal, click Reject -> confirm dialog -> POST -> row removal, error state, empty state, source-field assertion fires for backing-row data); ≥4 new tests in `ReviewActions.test.tsx` (mounts under each of the 4 trigger conditions, NOT mounted otherwise); ≥4 new tests in `api.test.ts` (fetchReviewQueue happy path; non-2xx throws; acceptReviewQueueRow happy path; non-2xx throws ReviewActionApiError).'
    - 'Vitest gotchas: `globalThis.fetch` (not `global.fetch`) for any direct fetch mocks per Phase 2 precedent. Selective fake timers `{ toFake: ["Date"] }` if any test exercises clock-dependent assertions.'
    - 'Every new `.ts`/`.tsx` source file carries the SPDX MIT license header per CLAUDE.md.'
    - 'NO Phase 3 attention-flow change in this plan (deferred to Plan 05 W4 for snapshot-regen isolation).'
  artifacts:
    - path: 'packages/workbench-ui/src/types.ts'
      provides: 'Existing types + new re-exports `ReviewQueueRow`, `ReviewReason`, `ArtifactRefSummary` from `@kagent/dto/review-queue` (mirror lines 29-33 DispositionOverlayRow pattern).'
      contains: 'ReviewQueueRow'
    - path: 'packages/workbench-ui/src/api.ts'
      provides: 'Existing API helpers + `fetchReviewQueue`, `acceptReviewQueueRow`, `rejectReviewQueueRow`, `requestReview`, `useReviewQueue` hook, `ReviewActionApiError` Error subclass.'
      contains: 'fetchReviewQueue'
    - path: 'packages/workbench-ui/src/api.test.ts'
      provides: 'Existing tests + ≥4 new tests for the review-queue helpers (fetchReviewQueue happy path, fetchReviewQueue 500 throws, acceptReviewQueueRow happy path, acceptReviewQueueRow 422 throws ReviewActionApiError).'
      contains: 'fetchReviewQueue'
    - path: 'packages/workbench-ui/src/App.tsx'
      provides: 'Existing routing + `#/review` hash route -> `<ReviewPage />` mount block.'
      contains: 'review'
    - path: 'packages/workbench-ui/src/ReviewPage.tsx'
      provides: 'New table-shaped page mirroring TaskList.tsx; per-row Accept/Reject buttons + confirm modal; data-source-field DOM attributes.'
      contains: 'ReviewPage'
      min_lines: 200
    - path: 'packages/workbench-ui/src/ReviewPage.module.css'
      provides: 'CSS module mirroring TaskList.module.css; per-reason pill colors; modal backdrop+dialog.'
      contains: 'reasonPill'
    - path: 'packages/workbench-ui/src/ReviewPage.test.tsx'
      provides: '≥6 component tests covering render, accept-flow, reject-flow, error/empty states, source-field assertion firing.'
      contains: 'ReviewPage'
      min_lines: 150
    - path: 'packages/workbench-ui/src/command/ReviewActions.tsx'
      provides: 'Inline component mounted in TaskDetail; renders Accept/Reject buttons + same confirm modal as ReviewPage; null-returns when task is not eligible.'
      contains: 'ReviewActions'
      min_lines: 120
    - path: 'packages/workbench-ui/src/command/ReviewActions.module.css'
      provides: 'CSS module mirroring DispositionOverlay.module.css naming convention.'
      contains: 'review'
    - path: 'packages/workbench-ui/src/command/ReviewActions.test.tsx'
      provides: '≥4 component tests (4 trigger conditions ∋ mount; non-eligible returns null).'
      contains: 'ReviewActions'
      min_lines: 80
    - path: 'packages/workbench-ui/src/TaskDetail.tsx'
      provides: 'Existing detail view + `<ReviewActions task={detail} onDecision={refetch} />` mounted ABOVE `<DetailBody>`.'
      contains: 'ReviewActions'
    - path: 'packages/workbench-ui/src/command/source-binding.ts'
      provides: 'Existing closed-enum types + new `ReviewQueueFieldName` exported (14-member union).'
      contains: 'ReviewQueueFieldName'
    - path: 'packages/workbench-ui/src/command/source-binding.test.ts'
      provides: 'Existing tests + 1-2 narrowing tests for ReviewQueueFieldName.'
      contains: 'ReviewQueueFieldName'
  key_links:
    - from: 'packages/workbench-ui/src/types.ts'
      to: '@kagent/dto/review-queue (W0)'
      via: "export type { ReviewQueueRow, ReviewReason } from '@kagent/dto/review-queue'"
      pattern: '@kagent/dto/review-queue'
    - from: 'packages/workbench-ui/src/api.ts'
      to: '@kagent/dto/review-queue (assertIsReviewQueueRow)'
      via: "import { assertIsReviewQueueRow } from '@kagent/dto/review-queue'"
      pattern: 'assertIsReviewQueueRow'
    - from: 'packages/workbench-ui/src/ReviewPage.tsx'
      to: 'packages/workbench-ui/src/api.ts (fetchReviewQueue, acceptReviewQueueRow, rejectReviewQueueRow)'
      via: "import { fetchReviewQueue, acceptReviewQueueRow, rejectReviewQueueRow } from './api.js'"
      pattern: "from './api\\.js'"
    - from: 'packages/workbench-ui/src/ReviewPage.tsx'
      to: 'packages/workbench-ui/src/command/source-binding.ts (assertSourceField, useSourceField)'
      via: "import { assertSourceField, useSourceField } from './command/source-binding.js'"
      pattern: 'source-binding'
    - from: 'packages/workbench-ui/src/TaskDetail.tsx'
      to: 'packages/workbench-ui/src/command/ReviewActions.tsx'
      via: "import { ReviewActions } from './command/ReviewActions.js'"
      pattern: "from './command/ReviewActions"
    - from: 'packages/workbench-ui/src/App.tsx'
      to: 'packages/workbench-ui/src/ReviewPage.tsx'
      via: "import { ReviewPage } from './ReviewPage.js'"
      pattern: "from './ReviewPage"
    - from: 'packages/workbench-ui/src/command/source-binding.ts'
      to: 'packages/workbench-ui/src/types.ts (ReviewQueueRow re-export)'
      via: 'ReviewQueueFieldName closed-enum derived from ReviewQueueRow keys'
      pattern: 'ReviewQueueFieldName'
---

<objective>
Wave 3 — UI surface. The reviewer's two entry points (dedicated
`#/review` page + inline TaskDetail panel) plus the `useReviewQueue` hook
+ DTO re-exports + the source-binding extension for D7/CC-01 compliance.

This plan ships:

1. `packages/workbench-ui/src/types.ts` (MODIFY) — re-export `ReviewQueueRow`,
   `ReviewReason`, `ArtifactRefSummary` from `@kagent/dto/review-queue`
   (PATTERNS.md W3.2 — exact analog of DispositionOverlayRow re-export).
2. `packages/workbench-ui/src/api.ts` (MODIFY) — `fetchReviewQueue`,
   `acceptReviewQueueRow`, `rejectReviewQueueRow`, `requestReview`,
   `useReviewQueue` hook, `ReviewActionApiError` (PATTERNS.md W3.1).
3. `packages/workbench-ui/src/api.test.ts` (MODIFY) — ≥4 new tests.
4. `packages/workbench-ui/src/App.tsx` (MODIFY) — `#/review` hash route
   (PATTERNS.md W3.3).
5. `packages/workbench-ui/src/ReviewPage.tsx` (NEW) — table-shaped page
   (PATTERNS.md W3.4 — TaskList.tsx analog).
6. `packages/workbench-ui/src/ReviewPage.module.css` (NEW) — table CSS.
7. `packages/workbench-ui/src/ReviewPage.test.tsx` (NEW) — ≥6 component tests.
8. `packages/workbench-ui/src/command/ReviewActions.tsx` (NEW) — inline
   component mounted in TaskDetail (PATTERNS.md W3.5).
9. `packages/workbench-ui/src/command/ReviewActions.module.css` (NEW).
10. `packages/workbench-ui/src/command/ReviewActions.test.tsx` (NEW) —
    ≥4 component tests (4 trigger conditions + non-eligible -> null).
11. `packages/workbench-ui/src/TaskDetail.tsx` (MODIFY) — mount
    `<ReviewActions />` above `<DetailBody>` (PATTERNS.md W3.6).
12. `packages/workbench-ui/src/command/source-binding.ts` (MODIFY) —
    `ReviewQueueFieldName` closed-enum extension (PATTERNS.md W4.1).
13. `packages/workbench-ui/src/command/source-binding.test.ts` (MODIFY) —
    narrowing tests for `ReviewQueueFieldName`.

Purpose: deliver REV-01 + REV-02 UI surfaces. The reviewer can navigate
to `#/review`, see the projection rows, click Accept/Reject, and the UI
calls the W2 POST handlers (Plan 03). For tasks already deep-linked from
TaskList, the inline `<ReviewActions />` in `TaskDetail` shows the same
buttons without leaving the detail page.

Output: 6 NEW files + 7 MODIFIED files. Vitest is green:
`pnpm -C packages/workbench-ui test --run` exits 0.

§11 bounds-test answer (this plan's slice): polling at 5s; AbortController
on every fetch; component lifecycle cleans up on unmount; no new
substrate state. Revocation: a single `git revert` of this plan removes
the entire UI; the W2 backend stays write-capable but unreachable from UI.

§15 one-sentence test answer (this plan's slice): A dedicated
`#/review` page plus an inline `<ReviewActions />` in TaskDetail —
both bound to substrate sources via `data-source-field` per
COMMAND-CENTER-CONTRACT.md D7 — gives the operator two equivalent
entry points to the same single-reviewer accept/reject write surface,
fulfilling REV-02 UI acceptance and REV-01 reload-stable rendering
without expanding substrate primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-review-queue-projection-promotion-path/04-CONTEXT.md
@.planning/phases/04-review-queue-projection-promotion-path/04-RESEARCH.md
@.planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md
@.planning/phases/04-review-queue-projection-promotion-path/04-VALIDATION.md
@.planning/phases/04-review-queue-projection-promotion-path/04-01-PLAN.md
@.planning/phases/04-review-queue-projection-promotion-path/04-02-PLAN.md
@.planning/phases/04-review-queue-projection-promotion-path/04-03-PLAN.md
@docs/COMMAND-CENTER-CONTRACT.md
@packages/workbench-ui/src/api.ts
@packages/workbench-ui/src/types.ts
@packages/workbench-ui/src/App.tsx
@packages/workbench-ui/src/TaskList.tsx
@packages/workbench-ui/src/TaskDetail.tsx
@packages/workbench-ui/src/NewTaskModal.tsx
@packages/workbench-ui/src/command/source-binding.ts
@packages/workbench-ui/src/command/source-binding.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase + W0/W1/W2. -->

From packages/workbench-ui/src/api.ts (lines 116-126 fetchDispositions; lines 134-156 createTask; lines 158-170 CreateTaskApiError):

```typescript
export async function fetchDispositions(signal?: AbortSignal): Promise<DispositionOverlayRow[]> {
  const init: RequestInit = signal !== undefined ? { signal } : {};
  const res = await fetch('/api/dispositions', init);
  if (!res.ok) throw new Error(`fetchDispositions: ${res.status} ${res.statusText}`);
  const body = (await res.json()) as { items?: unknown };
  const items = Array.isArray(body.items) ? body.items : [];
  for (const it of items) assertIsDispositionOverlayRow(it);
  return items as DispositionOverlayRow[];
}

export async function createTask(body: CreateTaskBody): Promise<CreateTaskResponse> {
  const res = await fetch('/api/tasks', {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify(body),
  });
  if (res.status !== 200) {
    let errBody: { error?: string } = {};
    try {
      errBody = await res.json();
    } catch {
      /* ignored */
    }
    throw new CreateTaskApiError(res.status, errBody.error ?? `${res.status} ${res.statusText}`);
  }
  return (await res.json()) as CreateTaskResponse;
}

export class CreateTaskApiError extends Error {
  readonly status: number;
  constructor(status: number, message: string) {
    super(message);
    this.name = 'CreateTaskApiError';
    this.status = status;
  }
}
```

From packages/workbench-ui/src/types.ts (lines 29-33 — DispositionOverlayRow re-export pattern):

```typescript
export type { DispositionOverlayRow, DispositionOverBudgetReason } from '@kagent/dto/disposition';
```

From packages/workbench-ui/src/App.tsx (lines 49-77 — Route union + parseHash + mount-block layout):

```typescript
interface CommandRoute {
  readonly kind: 'command';
}
type Route = DetailRoute | ListRoute | GatewayRoute | ClusterRoute | CommandRoute;

function parseHash(hash: string): Route {
  const clean = hash.replace(/^#\/?/, '').replace(/\/$/, '');
  if (clean === '') return { kind: 'list' };
  if (clean === 'gateway') return { kind: 'gateway' };
  if (clean === 'cluster') return { kind: 'cluster' };
  if (clean === 'command') return { kind: 'command' };
  // Detail route parsing...
  return { kind: 'list' };
}
```

From packages/workbench-ui/src/TaskList.tsx (lines 135-185 — table-shape analog for ReviewPage):

```tsx
<table className={styles.table}>
  <thead>
    <tr>
      <th>Task</th>
      <th>Phase</th>
      <th>Agent</th>
      <th>Started</th>
    </tr>
  </thead>
  <tbody>
    {rows.map((r) => (
      <tr key={r.uid}>
        <td>
          <a href={`#/tasks/${encodeURIComponent(r.namespace)}/${encodeURIComponent(r.name)}`}>
            {r.namespace}/{r.name}
          </a>
        </td>
        ...
      </tr>
    ))}
  </tbody>
</table>
```

From packages/workbench-ui/src/NewTaskModal.tsx (modal pattern — analog for confirm dialog):

```tsx
useEffect(() => {
  const onKey = (e: KeyboardEvent): void => {
    if (e.key === 'Escape') onClose();
  };
  document.addEventListener('keydown', onKey);
  return () => document.removeEventListener('keydown', onKey);
}, [onClose]);

return (
  <div
    className={styles.backdrop}
    onClick={(e) => {
      if (e.target === e.currentTarget) onClose();
    }}
  >
    <div className={styles.dialog} role="dialog" aria-modal="true" aria-labelledby="modal-title">
      ...
    </div>
  </div>
);
```

From packages/workbench-ui/src/command/source-binding.ts (lines 50-62, 106-112, 173-205 — closed-enum pattern + generic helpers):

```typescript
export type DispositionFieldName = /* fields of DispositionOverlayRow */;
export type { PressureFieldName } from './pressure.js';
export type { FlowFieldName } from './flows.js';

export function assertSourceField<T extends object, K extends string>(row: T, field: K): K { /* dev-only check */ return field; }
export function useSourceField<K extends string>(field: K): K { return field; }
export function useSourceFields<K extends string>(fields: readonly K[]): string { return fields.join(','); }
```

From @kagent/dto (W0 — Plan 04-01):

```typescript
export interface ReviewQueueRow {
  /* see Plan 04-01 */
}
export type ReviewReason =
  | 'verifier-failed'
  | 'suspicious-detector'
  | 'human-review-requested'
  | 'candidate-template'
  | 'replay-divergence'
  | 'eval-failed';
export function assertIsReviewQueueRow(value: unknown): asserts value is ReviewQueueRow;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (W3-A): types.ts re-exports + api.ts fetch/POST helpers + useReviewQueue hook + api.test.ts coverage</name>

<read_first> - packages/workbench-ui/src/types.ts (lines 29-33 — DispositionOverlayRow re-export pattern; full file for context on type re-export structure) - packages/workbench-ui/src/api.ts (lines 116-126 fetchDispositions; lines 134-156 createTask; lines 158-170 CreateTaskApiError; full file for hook precedents and existing useDispositions if present) - packages/workbench-ui/src/api.test.ts (full file — confirm existing test patterns; mocking globalThis.fetch; assert response shape) - packages/dto/src/review-queue.ts (W0 — `ReviewQueueRow`, `ReviewReason`, `ArtifactRefSummary`, `assertIsReviewQueueRow`) - .planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md (W3.1, W3.2 — verbatim code excerpts)
</read_first>

  <files>
    packages/workbench-ui/src/types.ts (MODIFY — append review-queue re-exports)
    packages/workbench-ui/src/api.ts (MODIFY — append fetch/POST helpers + hook + error class)
    packages/workbench-ui/src/api.test.ts (MODIFY — append ≥4 new tests)
  </files>

  <behavior>
    Tests written FIRST (RED). Test cases for api.test.ts:

    - **fetchReviewQueue happy path** — `globalThis.fetch` is stubbed to return `{ ok: true, json: async () => ({ items: [validRow] }) }` (where `validRow` is a `ReviewQueueRow` matching `assertIsReviewQueueRow`). The function returns `[validRow]` and `assertIsReviewQueueRow` passes for each item.
    - **fetchReviewQueue 500 throws** — `globalThis.fetch` returns `{ ok: false, status: 500, statusText: 'Internal' }`. The function throws an Error with message containing `'fetchReviewQueue'` and `'500'`.
    - **fetchReviewQueue with empty items** — `globalThis.fetch` returns `{ ok: true, json: async () => ({}) }` (no `items` key). The function returns `[]` (defensive default per fetchDispositions pattern).
    - **acceptReviewQueueRow happy path** — fetch stub returns `{ status: 200, json: async () => ({ taskRef: { namespace, name, uid }, decision: 'accepted', auditedAt: '...', agentTemplateRef: { namespace, name, uid } }) }`. The function returns the body shape; the request is `POST /api/review-queue/<encoded-ns>/<encoded-name>/accept` with JSON body `{ reviewerId?, reasonText? }`.
    - **acceptReviewQueueRow 422 throws ReviewActionApiError** — fetch stub returns `{ status: 422, json: async () => ({ error: 'candidate parse failed' }) }`. The function throws `ReviewActionApiError` with `status === 422` and `message` containing `'candidate parse failed'`.
    - **acceptReviewQueueRow 503 throws ReviewActionApiError with status 503** — fetch returns 503 with the verbatim "write surface disabled..." body; the error.status === 503.
    - **rejectReviewQueueRow happy path** — same shape as accept, no `agentTemplateRef`.
    - **requestReview happy path** — fetch stub returns 200 with `{ taskRef, requested: true, requestedAt: '...' }`.

    Implementation requirements:

    types.ts:
    - Append `export type { ReviewQueueRow, ReviewReason, ArtifactRefSummary } from '@kagent/dto/review-queue';` immediately after the existing `DispositionOverlayRow` re-export (lines 29-33).

    api.ts:
    - Add the 4 fetch/POST helpers (`fetchReviewQueue`, `acceptReviewQueueRow`, `rejectReviewQueueRow`, `requestReview`) and `ReviewActionApiError` class per PATTERNS.md W3.1 verbatim.
    - Helpers use `encodeURIComponent` on `namespace` + `name` (URL-injection defense; mirror createTask pattern).
    - The POST helpers send `Content-Type: application/json` and JSON-stringify the body.
    - On non-200 response: try to parse the body as JSON for the `error` field; throw `ReviewActionApiError` with `status` and `message`.
    - Add `useReviewQueue` hook per PATTERNS.md W3.1 lines 1212-1249:
      - 5s polling interval (default per CONTEXT.md D-01-A "Claude's Discretion").
      - AbortController per refresh; ref-tracked for cancel.
      - State: `rows`, `loading`, `error`.
      - `refresh()` callable for manual refetch.
      - useEffect cleans up interval + AbortController on unmount.

  </behavior>

  <action>
    1. Modify `packages/workbench-ui/src/types.ts`: append the re-export line for the W0 DTOs immediately after the existing `DispositionOverlayRow` re-export.

    2. Append to `packages/workbench-ui/src/api.ts`:
       - SPDX header verified (file already has one).
       - Import `assertIsReviewQueueRow` from `@kagent/dto/review-queue` (or via `@kagent/dto` barrel).
       - Add `fetchReviewQueue` per PATTERNS.md W3.1.
       - Add `ReviewActionApiError` class.
       - Add `acceptReviewQueueRow`, `rejectReviewQueueRow`, `requestReview` POST helpers per PATTERNS.md W3.1.
       - Add `useReviewQueue` hook per PATTERNS.md W3.1 (5s polling default).
       - Add response-shape types (e.g., `AcceptReviewResponse`, `RejectReviewResponse`, `RequestReviewResponse`, `RequestReviewBody`, `AcceptReviewBody`) for caller-side type safety. The shapes mirror what Plan 03's POST handlers return.

    3. Extend `packages/workbench-ui/src/api.test.ts` with the ≥6 new tests listed in the behavior block. Use `globalThis.fetch` stubbing per Phase 2 gotchas (`globalThis.fetch = vi.fn().mockResolvedValueOnce(...)`).

    4. Run `pnpm -C packages/workbench-ui test --run api.test`. All tests pass.

    5. Run `pnpm -C packages/workbench-ui typecheck`.

    6. Atomic commit: `feat(phase-04-w3): add review-queue API helpers + useReviewQueue hook + DTO re-exports (REV-01)`. Co-author.

  </action>

  <verify>
    <automated>pnpm -C packages/workbench-ui test --run api --reporter=verbose && pnpm -C packages/workbench-ui typecheck && grep -q "fetchReviewQueue" packages/workbench-ui/src/api.ts && grep -q "acceptReviewQueueRow" packages/workbench-ui/src/api.ts && grep -q "useReviewQueue" packages/workbench-ui/src/api.ts && grep -q "ReviewActionApiError" packages/workbench-ui/src/api.ts && grep -q "ReviewQueueRow" packages/workbench-ui/src/types.ts</automated>
  </verify>

  <done>
    - `types.ts` re-exports `ReviewQueueRow`, `ReviewReason`, `ArtifactRefSummary`.
    - `api.ts` exports all 4 POST helpers + `useReviewQueue` hook + `ReviewActionApiError`.
    - `api.test.ts` has ≥6 new passing tests.
    - `pnpm -C packages/workbench-ui typecheck` passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (W3-B): Create ReviewPage.tsx + ReviewPage.module.css + ReviewPage.test.tsx; wire #/review hash route in App.tsx</name>

<read_first> - packages/workbench-ui/src/TaskList.tsx (lines 1-end — THE table-layout template; copy hash-link encoding, header layout, error/loading states, AbortController lifecycle) - packages/workbench-ui/src/TaskList.module.css (full file — CSS module convention; reusable class names if applicable) - packages/workbench-ui/src/NewTaskModal.tsx (lines 1-end — confirm-dialog modal pattern; copy backdrop click, Escape-key, role/aria attrs, focus management) - packages/workbench-ui/src/NewTaskModal.module.css (full file — modal CSS reference) - packages/workbench-ui/src/App.tsx (lines 49-77 parseHash + lines 95-132 mount blocks — the route registration site) - packages/workbench-ui/src/api.ts (Task 1 of this plan — `useReviewQueue` + `acceptReviewQueueRow` + `rejectReviewQueueRow`) - packages/workbench-ui/src/types.ts (Task 1 of this plan — `ReviewQueueRow` re-export) - packages/workbench-ui/src/command/source-binding.ts (lines 173-205 — `assertSourceField`, `useSourceField`, `useSourceFields` helpers; Task 4 of this plan adds `ReviewQueueFieldName`) - .planning/phases/04-review-queue-projection-promotion-path/04-CONTEXT.md (D-03-A reviewer write surface (UI) section verbatim) - .planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md (W3.3, W3.4 — verbatim code excerpts) - .planning/phases/04-review-queue-projection-promotion-path/04-RESEARCH.md (Q5 — hash-route + page-mount pattern; modal pattern)
</read_first>

  <files>
    packages/workbench-ui/src/ReviewPage.tsx (NEW)
    packages/workbench-ui/src/ReviewPage.module.css (NEW)
    packages/workbench-ui/src/ReviewPage.test.tsx (NEW)
    packages/workbench-ui/src/App.tsx (MODIFY — add #/review route)
  </files>

  <behavior>
    Tests written FIRST (RED). ReviewPage.test.tsx test cases (≥6):

    - **Test 1 — renders rows from useReviewQueue mock** — `vi.mock('./api.js', () => ({ useReviewQueue: () => ({ rows: [validRow1, validRow2], loading: false, error: null, refresh: vi.fn() }), acceptReviewQueueRow: vi.fn(), rejectReviewQueueRow: vi.fn() }))`. Render `<ReviewPage onBack={vi.fn()} />`. Asserts: 2 `<tr>` elements rendered (one per row); each cell has `data-source-field` attribute; the "Open Detail" link href encodes namespace+name.
    - **Test 2 — empty state** — useReviewQueue returns `{ rows: [], loading: false, error: null }`. Page renders an empty-state message ("No items pending review" or similar — planner picks).
    - **Test 3 — loading state** — useReviewQueue returns `{ rows: [], loading: true, error: null }`. Page renders a loading indicator.
    - **Test 4 — error state** — useReviewQueue returns `{ rows: [], loading: false, error: 'fetch failed' }`. Page renders the error message.
    - **Test 5 — Accept click -> confirm dialog -> POST -> refresh** — mock `acceptReviewQueueRow` to resolve with `{ decision: 'accepted', auditedAt: '...' }`; render with 1 row; click `[data-testid="accept-row-0"]` (or button by label); the confirm dialog opens (`screen.getByRole('dialog')`); click the Confirm button; assert `acceptReviewQueueRow` was called with the correct namespace+name; assert `refresh` was called once.
    - **Test 6 — Reject click -> confirm dialog -> POST -> refresh** — same pattern as Test 5 with reject.
    - **Test 7 — Confirm dialog Escape closes** — click Accept; press Escape on `document`; dialog is removed; no POST called.
    - **Test 8 — source-field attribute matches `ReviewQueueFieldName`** — render with 1 row; query for `td[data-source-field="reason"]` and assert it exists.

    Implementation requirements:

    ReviewPage.tsx:
    - SPDX MIT header.
    - Module-level JSDoc citing REV-02 + D-03-A.
    - Imports: `React`, `useEffect/useRef/useState`, `useReviewQueue`, `acceptReviewQueueRow`, `rejectReviewQueueRow`, `ReviewActionApiError` from `./api.js`; `ReviewQueueRow` from `./types.js`; `assertSourceField`, `useSourceField` from `./command/source-binding.js`; CSS module.
    - Component prop: `{ onBack: () => void }`.
    - Use `useReviewQueue()` hook for data.
    - Confirm-dialog state: `confirmRow: { row: ReviewQueueRow; action: 'accept' | 'reject' } | null`. When non-null, render the modal.
    - On Confirm: call `acceptReviewQueueRow(...)` or `rejectReviewQueueRow(...)`; on success call `refresh()` and close dialog; on `ReviewActionApiError` show inline error in the dialog.
    - Table rendering: `<thead>` with columns `Reason | Task | Agent | Reason Detail | Staleness | Actions`. Per row, `data-source-field={useSourceField('<key>')}` on each cell. `assertSourceField(row, '<key>')` on row-level fields per Phase 1/2 pattern. Open Detail link via `#/tasks/<encoded-ns>/<encoded-name>`.
    - The reason cell renders a colored "pill" via a `reasonClass(reason)` helper that maps each ReviewReason value to a CSS class (per-reason color coding in module.css).
    - `formatStaleness(seconds)` — small helper that prints "5h 23m" / "1d 2h" / "12s" format depending on magnitude.
    - **All user-supplied text rendered via JSX text nodes only** (`<td>{row.reasonDetail}</td>`) so React's automatic HTML-entity escaping handles XSS defense; do NOT use any innerHTML-bypassing API anywhere in the component.

    ReviewPage.module.css:
    - `.table` — table layout (mirror TaskList.module.css).
    - `.reasonPill` + per-reason color classes (`.reasonVerifier`, `.reasonSuspicious`, `.reasonHumanReq`, `.reasonCandidate`, `.reasonReplay`, `.reasonEval`).
    - `.actions` — per-row buttons row.
    - `.backdrop`, `.dialog`, `.dialogTitle`, `.dialogActions` — confirm modal.
    - `.empty`, `.loading`, `.error` — state-display blocks.

    App.tsx:
    - Add `interface ReviewRoute { readonly kind: 'review'; }` to the Route union (PATTERNS.md W3.3).
    - In `parseHash`, add `if (clean === 'review') return { kind: 'review' };`.
    - In the `App` component, add the mount block: `if (route.kind === 'review') { return <ReviewPage onBack={() => { window.location.hash = '#/'; }} />; }`.
    - Import `ReviewPage` from `./ReviewPage.js`.

  </behavior>

  <action>
    1. Write `packages/workbench-ui/src/ReviewPage.test.tsx` per the test cases above. Use `@testing-library/react` (already a workspace dep per Phase 2). RED phase — tests fail because `./ReviewPage.js` doesn't exist yet.

    2. Write `packages/workbench-ui/src/ReviewPage.tsx` per the behavior block.

    3. Write `packages/workbench-ui/src/ReviewPage.module.css` per the behavior block.

    4. Modify `packages/workbench-ui/src/App.tsx` per the behavior block.

    5. Run `pnpm -C packages/workbench-ui test --run ReviewPage` — all tests pass.

    6. Run `pnpm -C packages/workbench-ui typecheck`.

    7. Atomic commit: `feat(phase-04-w3): add ReviewPage + #/review hash route (REV-02)`. Co-author.

  </action>

  <verify>
    <automated>pnpm -C packages/workbench-ui test --run ReviewPage --reporter=verbose && pnpm -C packages/workbench-ui typecheck && test -f packages/workbench-ui/src/ReviewPage.tsx && test -f packages/workbench-ui/src/ReviewPage.module.css && test -f packages/workbench-ui/src/ReviewPage.test.tsx && grep -q "kind: 'review'" packages/workbench-ui/src/App.tsx && grep -q "ReviewPage" packages/workbench-ui/src/App.tsx && grep -q "useReviewQueue" packages/workbench-ui/src/ReviewPage.tsx && grep -q "data-source-field" packages/workbench-ui/src/ReviewPage.tsx</automated>
  </verify>

  <done>
    - `ReviewPage.tsx`, `ReviewPage.module.css`, `ReviewPage.test.tsx` all exist and compile.
    - All ≥6 ReviewPage tests pass.
    - `App.tsx` registers `#/review` -> `<ReviewPage />`.
    - Every cell rendering a `ReviewQueueRow` field carries `data-source-field`.
    - Confirm dialog mirrors NewTaskModal patterns (Esc, backdrop click).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (W3-C): Create ReviewActions.tsx + ReviewActions.module.css + ReviewActions.test.tsx; mount in TaskDetail.tsx</name>

<read_first> - packages/workbench-ui/src/TaskDetail.tsx (lines 100-102 — `<DetailBody>` mount site; full file for context on detail-page layout) - packages/workbench-ui/src/NewTaskModal.tsx (modal pattern — share with ReviewPage's confirm dialog OR duplicate per ReviewActions for self-containment; planner picks) - packages/workbench-ui/src/command/DispositionOverlay.tsx (full file — mirror file-shape for `command/ReviewActions.tsx`; shares directory) - packages/workbench-ui/src/command/DispositionOverlay.module.css (full file — CSS naming convention reference) - packages/workbench-ui/src/command/DispositionOverlay.test.tsx (full file — analog for component tests) - packages/workbench-ui/src/api.ts (Task 1 — `acceptReviewQueueRow`, `rejectReviewQueueRow`, `requestReview`) - packages/workbench-ui/src/types.ts (TaskDetail type — confirm field path for `task.phase`, `task.suspicious`, `task.pilotEvidence?.audit?.annotations` or wherever annotations live in the UI-side TaskDetail shape) - .planning/phases/04-review-queue-projection-promotion-path/04-CONTEXT.md (D-03 inline mount conditions verbatim) - .planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md (W3.5, W3.6 — verbatim code excerpts) - .planning/phases/04-review-queue-projection-promotion-path/04-RESEARCH.md (Q5 — modal pattern)
</read_first>

  <files>
    packages/workbench-ui/src/command/ReviewActions.tsx (NEW)
    packages/workbench-ui/src/command/ReviewActions.module.css (NEW)
    packages/workbench-ui/src/command/ReviewActions.test.tsx (NEW)
    packages/workbench-ui/src/TaskDetail.tsx (MODIFY — mount ReviewActions above DetailBody)
  </files>

  <behavior>
    Tests written FIRST (RED). ReviewActions.test.tsx test cases (≥4):

    - **Test 1 — renders for `phase === 'Failed'`** — given `task = { ..., phase: 'Failed', suspicious: [], pilotEvidence: undefined }`, render `<ReviewActions task={task} onDecision={vi.fn()} />`. The component is non-null; the Accept/Reject buttons exist.
    - **Test 2 — renders for `suspicious.length > 0`** — task has `suspicious: ['outlier']`, otherwise clean. Component renders.
    - **Test 3 — renders for `review-requested === 'true'` annotation** — task has `pilotEvidence?.audit?.annotations['kagent.knuteson.io/review-requested'] === 'true'` (planner verifies the access path; if UI's TaskDetail type doesn't surface annotations, plumb them via Plan 03-supplied detail GET enrichment OR via a separate prop). Component renders.
    - **Test 4 — renders for `template-candidate === 'true'` annotation** — same as Test 3 with the template-candidate key.
    - **Test 5 — does NOT render for clean task** — task has phase='Completed', suspicious=[], no relevant annotations. Component returns null.
    - **Test 6 — Accept click -> confirm dialog -> POST -> onDecision called** — mock `acceptReviewQueueRow` to resolve; render with eligible task; click Accept; click Confirm in dialog; `acceptReviewQueueRow` called with task's namespace+name; `onDecision` called once.
    - **Test 7 — Reject click -> confirm dialog -> POST -> onDecision called** — same with reject.

    Implementation requirements:

    ReviewActions.tsx:
    - SPDX MIT header.
    - JSDoc citing REV-02 + D-03-A inline-entry-point.
    - Component: `export function ReviewActions({ task, onDecision }: { task: TaskDetail; onDecision: () => void }): React.JSX.Element | null`.
    - **Eligibility check** (CONTEXT.md D-03 4-trigger conditions):
      ```typescript
      const annotations = task.pilotEvidence?.audit?.annotations ?? {};
      const eligible =
        task.phase === 'Failed' ||
        (task.suspicious?.length ?? 0) > 0 ||
        annotations['kagent.knuteson.io/review-requested'] === 'true' ||
        annotations['kagent.knuteson.io/template-candidate'] === 'true';
      if (!eligible) return null;
      ```
      The `task.pilotEvidence?.audit?.annotations` access path is NOT guaranteed in the existing UI TaskDetail shape. If absent, planner picks one of: (a) plumb annotations through a new optional field on the TaskDetail UI type (additive), or (b) accept that the inline component triggers ONLY on `phase === 'Failed' || suspicious.length > 0` for v0.2 and document the gap. Default per RESEARCH.md / PATTERNS.md: plumb annotations through.
    - Confirm-dialog state: `confirmAction: 'accept' | 'reject' | null`. Modal rendering identical pattern to ReviewPage.tsx (or extract a shared `<ConfirmDialog>` component — planner picks; default: duplicate the small modal for self-containment).
    - On Confirm: call `acceptReviewQueueRow(task.namespace, task.name, body)` or `rejectReviewQueueRow(...)`; on success call `onDecision()`; on `ReviewActionApiError` show inline error.
    - Render: header text "Review actions", Accept button, Reject button, optional "Request review" button (planner picks; default: only Accept/Reject inline, since `request` is the dedicated `#/review` page's purpose).
    - **All user-supplied text rendered via JSX text nodes only** so React's automatic HTML-entity escaping handles XSS defense; do NOT use any innerHTML-bypassing API anywhere in the component.

    ReviewActions.module.css:
    - `.container` — top-of-detail panel styling.
    - `.actions` — row of buttons.
    - `.backdrop`, `.dialog` — modal (or import shared from ReviewPage.module.css if the modal is extracted).

    TaskDetail.tsx:
    - Import `ReviewActions` from `./command/ReviewActions.js`.
    - Inside the rendering block where `<DetailBody detail={detail} />` is mounted (lines ~100-102 per PATTERNS.md W3.6), wrap in fragment with `<ReviewActions task={detail} onDecision={refetch} />` IMMEDIATELY ABOVE `<DetailBody>`. The `refetch` prop name may differ in TaskDetail (planner verifies the actual handler name; default: pass whatever the existing detail-refresh function is).

  </behavior>

  <action>
    1. Write `packages/workbench-ui/src/command/ReviewActions.test.tsx` per the test cases above (RED).

    2. Write `packages/workbench-ui/src/command/ReviewActions.tsx`. Decision-and-comment for the annotation-access path:
       - Verify whether `TaskDetail` UI type carries annotations. If yes, use them directly.
       - If no, add the annotations field as additive optional, propagate through Plan 03's detail GET handler if necessary. Document choice in JSDoc.

    3. Write `packages/workbench-ui/src/command/ReviewActions.module.css`.

    4. Modify `packages/workbench-ui/src/TaskDetail.tsx`: import + mount `<ReviewActions />` immediately above `<DetailBody>`.

    5. Run `pnpm -C packages/workbench-ui test --run ReviewActions` — all tests pass.

    6. Run `pnpm -C packages/workbench-ui typecheck`.

    7. Atomic commit: `feat(phase-04-w3): add inline ReviewActions component in TaskDetail (REV-02)`. Co-author.

  </action>

  <verify>
    <automated>pnpm -C packages/workbench-ui test --run ReviewActions --reporter=verbose && pnpm -C packages/workbench-ui typecheck && test -f packages/workbench-ui/src/command/ReviewActions.tsx && test -f packages/workbench-ui/src/command/ReviewActions.module.css && test -f packages/workbench-ui/src/command/ReviewActions.test.tsx && grep -q "ReviewActions" packages/workbench-ui/src/TaskDetail.tsx && grep -q "review-requested" packages/workbench-ui/src/command/ReviewActions.tsx && grep -q "template-candidate" packages/workbench-ui/src/command/ReviewActions.tsx</automated>
  </verify>

  <done>
    - `ReviewActions.tsx`, `ReviewActions.module.css`, `ReviewActions.test.tsx` exist and compile.
    - All ≥4 trigger-condition tests pass.
    - `TaskDetail.tsx` mounts `<ReviewActions />` above `<DetailBody>`.
    - The 4 trigger conditions (`phase==='Failed'`, `suspicious.length>0`, `review-requested='true'`, `template-candidate='true'`) all gate the component visibility.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (W3-D): Extend source-binding.ts with ReviewQueueFieldName closed enum + add narrowing tests</name>

<read_first> - packages/workbench-ui/src/command/source-binding.ts (lines 50-62 DispositionFieldName; lines 106-112 PressureFieldName re-export; lines 173-205 generic helpers; full file for context) - packages/workbench-ui/src/command/source-binding.test.ts (full file — confirm test patterns for narrowing assertions) - packages/dto/src/review-queue.ts (W0 — `ReviewQueueRow` interface fields) - .planning/phases/04-review-queue-projection-promotion-path/04-PATTERNS.md (W4.1 — verbatim ReviewQueueFieldName 14-member union) - .planning/phases/03-resource-flow-overlays/03-01-PLAN.md (Phase 3's FlowFieldName addition pattern)
</read_first>

  <files>
    packages/workbench-ui/src/command/source-binding.ts (MODIFY — add ReviewQueueFieldName)
    packages/workbench-ui/src/command/source-binding.test.ts (MODIFY — add narrowing tests)
  </files>

  <behavior>
    Implementation requirements:

    source-binding.ts:
    - Add the `ReviewQueueFieldName` closed-enum union per PATTERNS.md W4.1 verbatim:
      ```typescript
      /** Closed enumeration of ReviewQueueRow top-level field names.
       *  Mirror of DispositionFieldName / PressureFieldName / FlowFieldName.
       *  Phase 4 / REV-01 / D7. */
      export type ReviewQueueFieldName =
        | 'taskRef'
        | 'reason'
        | 'reasonDetail'
        | 'enqueuedAt'
        | 'stalenessSeconds'
        | 'phase'
        | 'targetAgent'
        | 'model'
        | 'suspicious'
        | 'verifierError'
        | 'traceLink'
        | 'artifactCount'
        | 'candidateTemplate'
        | 'replayDivergence';
      ```
    - Place the new type immediately after the existing closed-enum types (alphabetical or grouped per module convention).

    source-binding.test.ts test cases (≥1-2 added):

    - **Test 1 — `assertSourceField<ReviewQueueFieldName>(row, 'reason')` returns `'reason'`** — tests the runtime helper accepts the new closed-enum type. Mirrors existing PressureFieldName narrowing test.
    - **Test 2 — `useSourceFields<ReviewQueueFieldName>(['reason', 'reasonDetail'])` returns `'reason,reasonDetail'`** — tests the multi-field helper accepts the closed-enum.
    - **Test 3 (optional) — TS strict-compile rejects an invalid field name** — `// @ts-expect-error` test asserting `useSourceField<ReviewQueueFieldName>('not-a-field')` is a compile error.

  </behavior>

  <action>
    1. Modify `packages/workbench-ui/src/command/source-binding.ts`: add the `ReviewQueueFieldName` closed-enum after the existing closed-enum block.

    2. Modify `packages/workbench-ui/src/command/source-binding.test.ts`: add ≥2 narrowing tests for the new type.

    3. Run `pnpm -C packages/workbench-ui test --run source-binding`.

    4. Run `pnpm -C packages/workbench-ui typecheck`.

    5. Atomic commit: `feat(phase-04-w3): extend source-binding.ts with ReviewQueueFieldName (D7 / CC-01)`. Co-author.

  </action>

  <verify>
    <automated>pnpm -C packages/workbench-ui test --run source-binding --reporter=verbose && pnpm -C packages/workbench-ui typecheck && grep -q "export type ReviewQueueFieldName" packages/workbench-ui/src/command/source-binding.ts && grep -cE "'taskRef'|'reason'|'reasonDetail'|'enqueuedAt'|'stalenessSeconds'|'phase'|'targetAgent'|'model'|'suspicious'|'verifierError'|'traceLink'|'artifactCount'|'candidateTemplate'|'replayDivergence'" packages/workbench-ui/src/command/source-binding.ts | awk '$1 >= 14 { exit 0 } { exit 1 }'</automated>
  </verify>

  <done>
    - `source-binding.ts` exports `ReviewQueueFieldName` (14 members).
    - `source-binding.test.ts` has new narrowing tests passing.
    - `pnpm -C packages/workbench-ui typecheck` passes.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                                                                   |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Browser -> workbench-api             | UI POSTs reach the W2 backend via `fetch` with `application/json`; same-origin request; auth via session cookie or `X-Forwarded-User` from the reverse proxy. |
| `fetch` response -> component state  | The `assertIsReviewQueueRow` runtime guard (W0) is the drift defense for UI-side trust in the API response shape.                                             |
| URL parameters -> POST endpoint path | UI uses `encodeURIComponent` on namespace + name to defend against path-injection.                                                                            |

## STRIDE Threat Register

| Threat ID  | Category                             | Component                       | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Mitigation Plan                                                                                                                                                                             |
| ---------- | ------------------------------------ | ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-W3-01 | Spoofing                             | UI-side reviewer-id             | accept                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | UI does NOT send `reviewerId` in body by default (the W2 handler reads X-Forwarded-User from the proxy header). If the UI later wants to allow a "review as" pseudonym, that's future work. |
| T-04-W3-02 | Tampering                            | API response -> component state | mitigate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `assertIsReviewQueueRow` runs on every item per `fetchReviewQueue`. Drift / API-tampering / proxy-tampering is caught at fetch-site.                                                        |
| T-04-W3-03 | Tampering                            | URL path                        | mitigate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `encodeURIComponent(namespace) + encodeURIComponent(name)` on every API path construction. Mirror createTask pattern.                                                                       |
| T-04-W3-04 | Information Disclosure               | Confirm-dialog text             | accept                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | The dialog prompt text is hardcoded ("Are you sure you want to accept this task?"). No PII / secrets surface.                                                                               |
| T-04-W3-05 | EoP — Confirm dialog focus-trap      | accept                          | NewTaskModal precedent: NO formal focus-trap; Esc-to-close + backdrop-click + initial input focus only. The risk is a screen-reader user tabs out of the dialog into the page chrome — accepted per the existing UX precedent.                                                                                                                                                                                                                                                                                                                            |
| T-04-W3-06 | EoP — XSS via reasonDetail rendering | mitigate                        | All user-supplied text — including `reasonDetail`, `verifierError`, `suspicious[]` items, and `reasonText` echoed in the confirm dialog — is rendered via React JSX text nodes only (e.g., `<td>{row.reasonDetail}</td>`). React auto-escapes HTML entities at the text-node boundary. The plan explicitly forbids the use of any innerHTML-bypassing API anywhere in the component (no innerHTML setter, no equivalent React prop). The W2 classifier copies verifier output verbatim — but React escaping at the render boundary is the single defense. |
| T-04-W3-07 | DoS — polling burst on tab focus     | accept                          | The `useReviewQueue` hook polls at 5s intervals regardless of tab visibility. Future work: pause polling when `document.hidden` (deferred per CONTEXT.md).                                                                                                                                                                                                                                                                                                                                                                                                |

</threat_model>

<verification>
- `ReviewPage` renders rows; Accept/Reject buttons trigger confirm dialog; confirm calls W2 POST handlers
- Inline `ReviewActions` mounts ONLY for the 4 trigger conditions
- `data-source-field` DOM attributes present on every cell rendering a `ReviewQueueRow` field
- `useReviewQueue` hook polls every 5s with AbortController cleanup
- All ≥14 new vitest tests across `api.test`, `ReviewPage.test`, `ReviewActions.test`, `source-binding.test` pass
- `pnpm -C packages/workbench-ui typecheck` passes
- Phase 1+ vitest gotchas honored (globalThis.fetch, selective fake timers)
- All new files carry SPDX MIT header
</verification>

<success_criteria>

- The reviewer can navigate to `#/review`, see queue rows, click Accept/Reject, the W2 POST endpoints execute, the queue refreshes.
- The reviewer can also act inline from `TaskDetail` for tasks matching the 4 trigger conditions.
- Every cell carries `data-source-field` per D7/CC-01 contract.
- No phase-3 `attention` flow change ships in this plan (deferred to Plan 05).
- All UI tests pass; coverage ≥75% on glue code.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-review-queue-projection-promotion-path/04-04-SUMMARY.md` with:
- File list (NEW + MODIFIED).
- Test counts per test file (api, ReviewPage, ReviewActions, source-binding).
- Notes on any annotation-access plumbing decisions in TaskDetail (whether annotations were already present on the UI-side TaskDetail type or whether plumbing was needed).
- Notes on any modal-pattern duplication vs extraction decision (shared `<ConfirmDialog>` vs duplicated).
- Hand-off note for Plan 05 (W4): "All UI surfaces are live and source-bound. Plan 05 flips the Phase 3 `attention` flow gauge to read from the new review-queue projection; regenerates `cc-reload.test.tsx.snap` in a single dedicated commit; lands the W4 docs additions (AGENT-TEMPLATES.md, REPLAY-EVALS.md, SUBSTRATE-V1.md). The `useReviewQueue` hook is available for any Plan 05 integration that needs the row count."
</output>
