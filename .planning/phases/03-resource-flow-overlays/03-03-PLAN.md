---
phase: 03-resource-flow-overlays
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - docs/FLOW-LEGEND.md
  - docs/COMMAND-CENTER-CONTRACT.md
autonomous: true
requirements: [FLOW-02]
notes: |
  Wave 3 — developer-facing legend doc + optional contract footer link.
  No code changes; pure documentation. Per CONTEXT.md D-03-A locked
  decision: docs/FLOW-LEGEND.md is the developer-facing source of truth
  for the 8 flows; on-canvas legend is FORBIDDEN per Slice E acceptance
  (`docs/COMMAND-CENTER-CONTRACT.md:255` — "legend in developer docs,
  NOT in main UI chrome").

  Two atomic commits:
    Task 1: docs/FLOW-LEGEND.md — REQUIRED. Cites intel/constraints.md
    §C-flow-economy + COMMAND-CENTER-CONTRACT.md §7 Slice E + CONTEXT.md
    D-01..D-05. Contains the at-a-glance 8-row table + per-flow detail
    section (8 sections — one `## ` heading per flow).

    Task 2: docs/COMMAND-CENTER-CONTRACT.md — OPTIONAL footer link
    addition. Per CONTEXT.md D-03-A: "added to docs/COMMAND-CENTER-
    CONTRACT.md references section (§9 or footer) so future contributors
    land on it from the binding contract. The contract itself is NOT
    modified to add the 8 flows inline." Per RESEARCH.md Open Question 3
    recommendation: include as the LAST commit in Phase 3.

  After Plan 03 completes, Phase 3 is DONE — both FLOW-01 (covered by
  Plan 01 + Plan 02) and FLOW-02 (covered by Plan 03) are satisfied.

must_haves:
  truths:
    - 'The §11 bounds-test (Phase 3 — verbatim from CONTEXT.md `<specifics>` item 4): Declared capability: Command Center renders all 8 `C-flow-economy` flows as continuous gauges with proven backing source fields, reload-stable, base-building-only mode supported via the existing global flag. Bounded resource drain: dev-only assertions stay no-op in prod; flow `compute()` is O(snapshot size) per render and runs only on snapshot change; no new persistence; no new API surface. Observable state transition: orphan assertion catches any rendered gauge that lacks a backing source field; the cc-reload snapshot diff is reviewable; `data-source-field` DOM attributes are readable from devtools. Auditable output: vitest CI run is the auditable surface — `flows.test.ts`, `FlowOverlay.test.tsx`, extended `cc-reload.test.tsx` snapshot, `source-binding.test.ts` updates all fail loud on regression. Revocation path: `NODE_ENV=production` disables orphan assertions; `VITE_PRESSURE_DRAMATIZATION=false` subdues all overlays (now including flows); ALL Phase 3 changes are pure UI-package code + a developer doc so a single revert removes the entire phase.'
    - "The §15 one-sentence test (Phase 3 — verbatim from CONTEXT.md `<specifics>` item 5): Rendering the eight `C-flow-economy` flows as continuous source-bound gauges in Command Center, alongside Phase 2's pressure markers, gives operators legible resource economy state without expanding substrate primitives — strengthening observability of supply-vs-demand pressure in v0.2 and unlocking the promotion-to-real-source path documented in flows.ts comments for future phases."
    - '`docs/FLOW-LEGEND.md` exists at the repo root under `docs/`. EXACTLY 8 `## ` headings (one per flow: modelPower, tokenFlow, buildPower, podCapacity, artifactBandwidth, authority, trust, attention) PLUS the at-a-glance table heading (which uses `## At-a-glance` so the heading count is total 9 — but the per-flow section count is exactly 8). FLOW-02 acceptance: `[ "$(grep -c ''^## '' docs/FLOW-LEGEND.md)" -ge 8 ]` per VALIDATION.md task 03-XX-10.'
    - "`docs/FLOW-LEGEND.md` references `intel/constraints.md §C-flow-economy` AT LEAST ONCE per VALIDATION.md task 03-XX-09: `grep -q 'C-flow-economy' docs/FLOW-LEGEND.md`."
    - "Per CONTEXT.md D-03-A: each per-flow section includes (a) 2–3 sentence description, (b) the exact field path(s) (copy from `flows.ts` source bindings), (c) the fallback derivation expression where applicable (copy verbatim from `flows.ts` `compute()` body), (d) the Phase 4+ promotion plan (per RESEARCH.md Promotion Paths section), (e) the pressure trigger cross-reference to `pressure.ts` line numbers (or 'no direct trigger today' for tokenFlow), (f) the operator action (1–2 sentences)."
    - "Per CONTEXT.md D-03-A: docs/FLOW-LEGEND.md includes a 'Living doc' footer note: 'Living doc — update when `packages/workbench-ui/src/command/flows.ts` adds, removes, or promotes a flow.'"
    - "Per CONTEXT.md D-04-A discoverability: docs/FLOW-LEGEND.md notes the env var at the top: 'Visual treatment of all overlays (disposition, pressure, flows) is controlled by `VITE_PRESSURE_DRAMATIZATION`. Set `false` for base-building-only mode.'"
    - "Per Slice E acceptance criterion (`docs/COMMAND-CENTER-CONTRACT.md:255`) — NO on-canvas legend tooltip, NO sidebar key, NO '?' button on FlowOverlay. The legend lives ONLY in `docs/FLOW-LEGEND.md`. Plan 02's FlowOverlay.tsx already honors this (no help icon, no tooltip)."
    - 'Per CONTEXT.md D-03-A + RESEARCH.md Open Question 3 (Recommendation = include): the OPTIONAL Task 2 adds ONE footer/references line to `docs/COMMAND-CENTER-CONTRACT.md` linking to FLOW-LEGEND.md. The contract is NOT modified to enumerate the 8 flows inline (that would be a contract revision and is OUT OF SCOPE per CONTEXT.md OOS bullet).'
    - 'Phase 3 §11 bounds-test answer is fully satisfied after Plan 03 commits: declared capability proven (Plans 01+02 ship the data + UI; Plan 03 ships the legend); bounded resource drain proven (no new env vars; doc is markdown only); observable state transition proven (vitest CI + snapshot diff + grep on FLOW-LEGEND.md); auditable output proven (full vitest suite green + grep checks on FLOW-LEGEND.md); revocation path proven (revert all Phase 3 commits removes flows.ts + FlowOverlay.tsx + FlowOverlay.module.css + FlowOverlay.test.tsx + flows.test.ts + the source-binding.ts/test.ts deltas + the cc-snapshot.json delta + the cc-reload snapshot delta + docs/FLOW-LEGEND.md + the optional COMMAND-CENTER-CONTRACT.md footer link).'
  artifacts:
    - path: 'docs/FLOW-LEGEND.md'
      provides: 'Developer-facing legend mapping each of the 8 `C-flow-economy` flows to: substrate source field(s), pressure trigger cross-reference, operator action, v0.2 fallback (where applicable), and promotion path. Cites `intel/constraints.md §C-flow-economy`, `COMMAND-CENTER-CONTRACT.md §7 Slice E`, `CONTEXT.md D-01..D-05`. Includes at-a-glance 8-row table + 8 per-flow sections + living-doc footer.'
      contains: 'C-flow-economy'
      min_lines: 100
    - path: 'docs/COMMAND-CENTER-CONTRACT.md'
      provides: 'Existing Phase 1+2 binding contract + ONE new footer/references line linking to `docs/FLOW-LEGEND.md`. NOT a contract revision — the contract is not modified to enumerate flows inline. Single discoverability link only.'
      contains: 'FLOW-LEGEND.md'
  key_links:
    - from: 'docs/FLOW-LEGEND.md'
      to: '.planning/intel/constraints.md (§C-flow-economy)'
      via: 'markdown citation: "Per `intel/constraints.md §C-flow-economy` …"'
      pattern: 'C-flow-economy'
    - from: 'docs/FLOW-LEGEND.md'
      to: 'docs/COMMAND-CENTER-CONTRACT.md (§7 Slice E)'
      via: 'markdown citation: "Per `COMMAND-CENTER-CONTRACT.md §7 Slice E` …"'
      pattern: 'Slice E'
    - from: 'docs/FLOW-LEGEND.md'
      to: 'packages/workbench-ui/src/command/flows.ts (FLOW_TYPES + per-entry comments)'
      via: 'each per-flow section copies the source-field bindings from flows.ts'
      pattern: 'flows\\.ts'
    - from: 'docs/FLOW-LEGEND.md'
      to: 'packages/workbench-ui/src/command/pressure.ts (pressure-trigger cross-references)'
      via: 'each per-flow section cross-links to the pressure marker that complements the flow (e.g., modelPower ↔ gateway pressure)'
      pattern: 'pressure\\.ts'
    - from: 'docs/COMMAND-CENTER-CONTRACT.md'
      to: 'docs/FLOW-LEGEND.md'
      via: 'optional footer link — single line: "See also: `docs/FLOW-LEGEND.md` for the eight C-flow-economy flow definitions surfaced via Command Center FlowOverlay (Phase 3 / FLOW-01)."'
      pattern: 'FLOW-LEGEND\\.md'
---

<objective>
Wave 3 — developer-facing legend doc + optional contract footer link. Closes FLOW-02 ("A 'flow legend' exists in developer docs (NOT in main UI chrome per `COMMAND-CENTER-CONTRACT.md` Slice E acceptance) mapping each flow to its substrate source, pressure trigger, and operator action. Living doc updated as flows evolve.").

This plan ships:

1. `docs/FLOW-LEGEND.md` — REQUIRED. Developer-facing legend with: title + framing paragraph + at-a-glance 8-row table + 8 per-flow `## ` sections + living-doc footer. Cites `intel/constraints.md §C-flow-economy`, `COMMAND-CENTER-CONTRACT.md §7 Slice E`, CONTEXT.md D-01..D-05. Per-flow sections cite `flows.ts` source bindings (Plan 01) + `pressure.ts` cross-reference for the complementary pressure marker.
2. `docs/COMMAND-CENTER-CONTRACT.md` — OPTIONAL. Single new footer/references line linking to FLOW-LEGEND.md. NOT a contract revision — the contract is not modified to enumerate the 8 flows inline. Per CONTEXT.md D-03-A + RESEARCH.md Open Question 3 (Recommendation: include).

Purpose: close FLOW-02. Plans 01+02 shipped the data + UI; Plan 03 ships the legend that operators read alongside the live overlay. Phase 3 is complete after Plan 03.

Output: 1 new markdown file (`docs/FLOW-LEGEND.md`) + 1 modified markdown file (`docs/COMMAND-CENTER-CONTRACT.md` with a single new line). Both committed atomically.

§11 bounds-test answer (this plan's slice — completes phase-level answer):

- Declared capability: developer-facing legend exists with 8 per-flow sections + at-a-glance table + cross-references to `flows.ts` source bindings + cross-references to `pressure.ts` pressure markers + promotion paths + operator actions; the contract gets a discoverability link to the legend in a separate doc commit.
- Bounded resource drain: markdown only; no code change; no env var; no API surface; no test infrastructure addition.
- Observable state transition: `grep -c '^## ' docs/FLOW-LEGEND.md` returns ≥8 (8 per-flow sections present per VALIDATION.md task 03-XX-10); `grep -q 'C-flow-economy' docs/FLOW-LEGEND.md` returns 0 (citation present per task 03-XX-09).
- Auditable output: file-existence + grep checks (per VALIDATION.md). Reviewer reads each section per VALIDATION.md "Manual-Only Verifications" #1 — prose quality is judgment.
- Revocation path: deleting `docs/FLOW-LEGEND.md` removes the legend; reverting the optional Task 2 commit removes the contract footer link.

§15 one-sentence-test answer (this plan's slice — completes phase-level answer): Closing the FLOW-02 docs requirement with a developer-facing legend that lives in `docs/FLOW-LEGEND.md` (NOT in UI chrome) gives operators a discoverable source-of-truth for what each flow IS, where its data comes FROM, and how it WILL evolve — turning the 8 source-bound gauges from Plan 02 into legible substrate-derived resource economy state with provenance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-resource-flow-overlays/03-CONTEXT.md
@.planning/phases/03-resource-flow-overlays/03-RESEARCH.md
@.planning/phases/03-resource-flow-overlays/03-PATTERNS.md
@.planning/phases/03-resource-flow-overlays/03-VALIDATION.md
@.planning/phases/03-resource-flow-overlays/03-01-PLAN.md
@.planning/phases/03-resource-flow-overlays/03-02-PLAN.md
@.planning/intel/constraints.md
@docs/COMMAND-CENTER-CONTRACT.md
@packages/workbench-ui/src/command/flows.ts
@packages/workbench-ui/src/command/pressure.ts
@packages/workbench-ui/src/command/FlowOverlay.tsx

<interfaces>
<!-- Citations the executor needs verbatim. Extracted from existing files. -->

From .planning/intel/constraints.md §C-flow-economy (the canonical 8-flow source — read this section before drafting FLOW-LEGEND.md):

- Defines: model power, token flow, build power, pod capacity, artifact bandwidth, authority, trust, attention.
- Provides the upstream "why" for each flow's existence (Wave 3 doc paraphrases or quotes selectively).

From docs/COMMAND-CENTER-CONTRACT.md §7 Slice E (line ~255 — the binding constraint):

- "legend in developer docs, NOT in main UI chrome" — the FLOW-LEGEND.md location is mandated by this line.

From packages/workbench-ui/src/command/flows.ts (Plan 01 output — the per-flow source-of-truth):

- FLOW_TYPES contains 8 entries; each has a leading comment naming source field(s) + (for v0.2 fallbacks) ideal source + promotion phase.
- The FLOW-LEGEND.md per-flow sections COPY the comment text from flows.ts.

From packages/workbench-ui/src/command/pressure.ts (Phase 2 output — the pressure-trigger cross-reference target):

- 9 PRESSURE_TYPES entries — the FLOW-LEGEND.md per-flow sections cite the COMPLEMENTARY pressure marker for each flow (e.g., modelPower ↔ `gateway` pressure marker at `pressure.ts:80`).

Per-flow cross-reference map (planner uses this to fill in pressure-trigger column of the at-a-glance table + per-flow section):

- modelPower ↔ `gateway` pressure marker (pressure.ts:~80) — both read GatewayCapacityRow.inFlight + currentCap; pressure marker fires at threshold ≥0.8; flow gauge always shows ratio.
- tokenFlow ↔ no direct pressure trigger today (would map to a future per-model-class saturation marker).
- buildPower ↔ `context` pressure marker (pressure.ts:~205) — both read TaskSummary phase=Dispatched; pressure fires on childCount; flow gauge shows agent's in-flight count.
- podCapacity ↔ `pod` pressure marker (pressure.ts:~125) — both read TaskSummary.podName; pressure fires on Failed pod tasks; flow gauge shows substrate-wide active-pod count.
- artifactBandwidth ↔ `artifact` pressure marker (pressure.ts:~105) — both read TaskSummary.artifactCount; pressure fires when Completed task has 0 artifacts (artifact debt); flow gauge sums artifactCount.
- authority ↔ `policy` pressure marker (pressure.ts:~285) — both read TaskSummary.error containing 'policy'; pressure marks each Failed task; flow gauge sums substrate-wide policy denials.
- trust ↔ `verifier` pressure marker (pressure.ts:~228) — both read TaskSummary.error containing 'verifier' + suspicious; pressure marks each Failed task; flow gauge sums substrate-wide trust events.
- attention ↔ no direct pressure trigger today (Phase 4 owns the real review-queue projection; v0.2 stub uses union of phase=Failed + suspicious).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/FLOW-LEGEND.md (REQUIRED — closes FLOW-02)</name>

<read_first> - .planning/intel/constraints.md (read the §C-flow-economy section in full — this is the canonical upstream definition of the 8 flows; the doc paraphrases or quotes selectively) - docs/COMMAND-CENTER-CONTRACT.md (read §7 Slice E around line 255 — the binding constraint "legend in developer docs, NOT in main UI chrome"; also skim §6 Pressure Systems for the 9 pressure types being cross-linked) - packages/workbench-ui/src/command/flows.ts (Plan 01 output — read all 8 entries' leading comments; the per-flow sections of FLOW-LEGEND.md COPY this content) - packages/workbench-ui/src/command/pressure.ts (Phase 2 output — locate the 9 pressure entries; cross-link by line number per the `<interfaces>` mapping above) - .planning/phases/03-resource-flow-overlays/03-CONTEXT.md (D-03-A — locked decision on doc location + content; D-04-A — env-var note at top of doc; D-02-\* per-flow comment text supplied verbatim) - .planning/phases/03-resource-flow-overlays/03-RESEARCH.md (Acceptance Criteria Translation table for FLOW-02 acceptance — section count, citation, table-row count; Promotion Paths section for the per-flow promotion plans) - .planning/phases/03-resource-flow-overlays/03-PATTERNS.md (docs/FLOW-LEGEND.md section starting at line ~784 — full doc skeleton + at-a-glance table example + per-flow section example) - .planning/phases/03-resource-flow-overlays/03-VALIDATION.md (Manual-Only Verifications #1 — what the reviewer will check for prose quality)
</read_first>

  <files>
    docs/FLOW-LEGEND.md (NEW)
  </files>

  <action>
    Create `docs/FLOW-LEGEND.md`. Use the following structure (per CONTEXT.md D-03-A + PATTERNS.md "docs/FLOW-LEGEND.md" section + RESEARCH.md FLOW-02 Acceptance Criteria):

    ```markdown
    # Flow Legend (developer docs — NOT in main UI chrome)

    > Eight `C-flow-economy` resource flows rendered as continuous gauges in
    > Command Center's `<FlowOverlay />`. This is the developer-facing source
    > of truth for what each flow IS, where its data comes FROM, and how it
    > WILL evolve. Per `docs/COMMAND-CENTER-CONTRACT.md` §7 Slice E, this
    > legend is NOT replicated as on-canvas chrome (no tooltip, no "?"
    > button, no sidebar key). Operators read code + docs.
    >
    > Visual treatment of all overlays (disposition, pressure, flows) is
    > controlled by `VITE_PRESSURE_DRAMATIZATION`. Set `false` for
    > base-building-only mode (subdued styling, same data).
    >
    > Living doc — update when `packages/workbench-ui/src/command/flows.ts`
    > adds, removes, or promotes a flow.

    ## Sources

    - `.planning/intel/constraints.md` §C-flow-economy — canonical 8-flow definition
    - `docs/COMMAND-CENTER-CONTRACT.md` §7 Slice E — overlay binding contract
    - `.planning/phases/03-resource-flow-overlays/03-CONTEXT.md` D-01..D-05 — locked decisions
    - `packages/workbench-ui/src/command/flows.ts` — `FLOW_TYPES` source-of-truth (each entry's leading comment names source + promotion path)
    - `packages/workbench-ui/src/command/pressure.ts` — companion pressure markers (Phase 2)

    ## At-a-glance

    | Flow | Granularity | v0.2 source | Ideal source | Pressure trigger | Operator action | Promotion path |
    | ---- | ----------- | ----------- | ------------ | ---------------- | --------------- | -------------- |
    | modelPower | perEndpoint | `GatewayCapacityRow.inFlight + currentCap` | (clean — same) | `gateway` (`pressure.ts:~80`) | scale gateway / rebalance per-endpoint pool | n/a |
    | tokenFlow | perModelClass | `count(TaskSummary by model)` in phase=Dispatched (`unit='tasks'`) | `GatewayUsageRow.inputTokens + outputTokens` rolling 1m per model | (no direct trigger today) | watch model-class hot-spots; investigate when one model dominates dispatched tasks | promote when rolling-window aggregation reaches `useCommandSnapshot()` (snapshot already exposes `gatewayUsage` per state.ts:88) |
    | buildPower | perAgent | `count(TaskSummary where targetAgent=agent && phase=Dispatched)` (open-ended; no capacity bar) | `pilotEvidence.policy.maxConcurrentChildren` on TaskDetail | `context` (`pressure.ts:~205`) | rebalance work / spawn additional agents when one saturates | promote when `pilotEvidence` subset reaches `TaskSummary` |
    | podCapacity | substrateWide (v0.2 fallback) | `count(TaskSummary where podName !== undefined && phase ∈ {Dispatched,Pending})` (open-ended) | `ClusterNodeRow.managedPodCount / capacity['pods']` per node | `pod` (`pressure.ts:~125`) | scale cluster / cordon nodes / migrate pods | promote when cluster snapshot joins `useCommandSnapshot()` |
    | artifactBandwidth | substrateWide | `sum(TaskSummary.artifactCount where phase=Completed)` | (clean — same) | `artifact` (`pressure.ts:~105`) | drain artifact debt / triage long-tail completions | n/a |
    | authority | substrateWide | `count(TaskSummary where phase=Failed && error contains 'policy')` | structured `policy_denied` audit event | `policy` (`pressure.ts:~285`) | review denials / adjust capability-JWT scope | promote when audit-event kinds reach the SSE stream |
    | trust | substrateWide | `count(TaskSummary where suspicious.length>0 \|\| (phase=Failed && error contains 'verifier'))` | `pilotEvidence.verification.passed === false` on TaskDetail | `verifier` (`pressure.ts:~228`) | investigate verifier-failed + suspicious tasks; reviewer queue | promote when `pilotEvidence` subset reaches `TaskSummary` |
    | attention | substrateWide (Phase 3 stub) | `count(TaskSummary where phase=Failed \|\| suspicious.length>0)` (label='awaiting review queue projection — Phase 4') | real review-queue projection | (no direct trigger today) | jump to review queue and triage oldest items | **Phase 4** owns this — `compute()` body swap with no `FlowGauge` shape change |

    ## Per-flow detail

    [8 sections — one `## <flow>` heading per flow. For each, write 4–8 sentences covering: what the flow represents (paraphrase from intel/constraints.md §C-flow-economy); the exact source field path in flows.ts (verbatim); the v0.2 fallback derivation expression where applicable (copy from flows.ts compute() body); the cross-link to the companion pressure marker in pressure.ts (line number); the operator action (1–2 sentences); the promotion plan (where applicable, naming the future phase that promotes the flow).]

    ### modelPower
    [4–8 sentences per the schema above]

    ### tokenFlow
    [4–8 sentences. Include the planner-flagged note: `gatewayUsage` IS already on `useCommandSnapshot()` (state.ts:88) so promotion to real per-request token counts is a single-PR future change. v0.2 ships task-count proxy with `unit='tasks'` for honesty about scope.]

    ### buildPower
    [4–8 sentences]

    ### podCapacity
    [4–8 sentences]

    ### artifactBandwidth
    [4–8 sentences]

    ### authority
    [4–8 sentences]

    ### trust
    [4–8 sentences]

    ### attention
    [4–8 sentences. Emphasize: this is the v0.2 stub; Phase 4 owns the real review-queue projection (REV-01); the FlowGauge shape stays unchanged when Phase 4 swaps the compute() body.]

    ---

    _Living doc — update when `packages/workbench-ui/src/command/flows.ts` adds, removes, or promotes a flow._
    ```

    Implementation steps:
    1. Open `docs/COMMAND-CENTER-CONTRACT.md` and locate §7 Slice E (around line 255). Confirm the "legend in developer docs, NOT in main UI chrome" line is present — this is the binding constraint to cite.
    2. Open `.planning/intel/constraints.md` and read §C-flow-economy (around lines 40–58 per CONTEXT.md canonical_refs). Use this as the source for each flow's "what it IS" description (2–3 sentences per per-flow section).
    3. Open `packages/workbench-ui/src/command/flows.ts` (Plan 01 output) and read the 8 entries' leading comments. The per-flow section "v0.2 fallback derivation" content COPIES the relevant excerpt from these comments (or from CONTEXT.md D-02-* which contains the verbatim text).
    4. Open `packages/workbench-ui/src/command/pressure.ts` and confirm the line numbers for the companion pressure markers per the `<interfaces>` mapping above (e.g., `gateway` marker is around `pressure.ts:80`; `pod` marker around `pressure.ts:125`; `artifact` marker around `pressure.ts:105`; `context` marker around `pressure.ts:205`; `verifier` marker around `pressure.ts:228`; `policy` marker around `pressure.ts:285`). Adjust the line numbers in the at-a-glance table if they differ from the `<interfaces>` mapping (the mapping uses approximate `~NN` line numbers; planner reads pressure.ts to confirm exact lines).
    5. Create `docs/FLOW-LEGEND.md` with the structure above. Fill in the 8 per-flow sections per their schema. Use plain markdown (no HTML); follow the existing repo doc style (compare to `docs/COMMAND-CENTER-CONTRACT.md` for tone — concise, declarative, code-block-rich where exact paths matter).
    6. Verify the file:
       - `test -f docs/FLOW-LEGEND.md` exits 0.
       - `grep -q 'C-flow-economy' docs/FLOW-LEGEND.md` exits 0 (citation present per VALIDATION.md task 03-XX-09).
       - `[ "$(grep -c '^## ' docs/FLOW-LEGEND.md)" -ge 8 ]` evaluates true (≥8 `## ` headings — at minimum the 8 per-flow sections; the doc may also include `## Sources` and `## At-a-glance` and `## Per-flow detail` so the actual count is likely 11). The validation gate per VALIDATION.md task 03-XX-10 is `≥8`.
       - `grep -q 'VITE_PRESSURE_DRAMATIZATION' docs/FLOW-LEGEND.md` exits 0 (env-var discoverability per CONTEXT.md D-04-A).
       - `grep -q 'Slice E' docs/FLOW-LEGEND.md` exits 0 (cites the binding constraint).
       - `grep -q 'Living doc' docs/FLOW-LEGEND.md` exits 0 (footer note per CONTEXT.md D-03-A).
       - `grep -E '^### (modelPower|tokenFlow|buildPower|podCapacity|artifactBandwidth|authority|trust|attention)$' docs/FLOW-LEGEND.md | wc -l` returns 8 (exactly 8 per-flow `### ` subsections under `## Per-flow detail`).
    7. Commit: `source ~/.nvm/nvm.sh && nvm use 22 && git add docs/FLOW-LEGEND.md && git commit -m "docs(phase-03-03): add FLOW-LEGEND.md (FLOW-02 — 8-flow developer-facing legend)"`

  </action>

  <verify>
    <automated>cd /Users/chrisknuteson/Projects/ctkadvisors/active/kagent && test -f docs/FLOW-LEGEND.md && grep -q 'C-flow-economy' docs/FLOW-LEGEND.md && [ "$(grep -c '^## ' docs/FLOW-LEGEND.md)" -ge 8 ] && grep -q 'VITE_PRESSURE_DRAMATIZATION' docs/FLOW-LEGEND.md && grep -q 'Slice E' docs/FLOW-LEGEND.md && grep -q 'Living doc' docs/FLOW-LEGEND.md && [ "$(grep -cE '^### (modelPower|tokenFlow|buildPower|podCapacity|artifactBandwidth|authority|trust|attention)$' docs/FLOW-LEGEND.md)" -eq 8 ]</automated>
  </verify>

<acceptance_criteria> - `test -f docs/FLOW-LEGEND.md` exits 0 (file exists at the expected path per CONTEXT.md D-03-A) - `grep -q 'C-flow-economy' docs/FLOW-LEGEND.md` exits 0 (citation present per VALIDATION.md task 03-XX-09) - `[ "$(grep -c '^## ' docs/FLOW-LEGEND.md)" -ge 8 ]` evaluates true (≥8 `## ` headings present per VALIDATION.md task 03-XX-10 — accommodates the 8 per-flow sections plus structural sections like `## Sources`, `## At-a-glance`, `## Per-flow detail`) - `[ "$(grep -cE '^### (modelPower|tokenFlow|buildPower|podCapacity|artifactBandwidth|authority|trust|attention)$' docs/FLOW-LEGEND.md)" -eq 8 ]` evaluates true (EXACTLY 8 per-flow `### ` subsections, one per flow kind, in the canonical order) - `grep -q 'VITE_PRESSURE_DRAMATIZATION' docs/FLOW-LEGEND.md` exits 0 (env-var note present per CONTEXT.md D-04-A) - `grep -q 'Slice E' docs/FLOW-LEGEND.md` exits 0 (cites the binding constraint at COMMAND-CENTER-CONTRACT.md §7) - `grep -q 'Living doc' docs/FLOW-LEGEND.md` exits 0 (footer note per CONTEXT.md D-03-A) - `grep -q 'COMMAND-CENTER-CONTRACT.md' docs/FLOW-LEGEND.md` exits 0 (binding contract referenced at the top of the doc) - `grep -q 'flows.ts' docs/FLOW-LEGEND.md` exits 0 (cross-link to the source-of-truth code) - `grep -q 'pressure.ts' docs/FLOW-LEGEND.md` exits 0 (cross-link to companion pressure markers) - `grep -q 'Phase 4' docs/FLOW-LEGEND.md` exits 0 (the attention flow's promotion plan names Phase 4 per CONTEXT.md D-02-attention) - `wc -l docs/FLOW-LEGEND.md` returns ≥100 (substantive content; not a stub) - One atomic commit lands on the branch with `docs(phase-03-03):` scope per Conventional Commits + CLAUDE.md
</acceptance_criteria>

  <done>
    docs/FLOW-LEGEND.md exists with the at-a-glance table + 8 per-flow sections + framing paragraph + sources + footer; all grep gates pass; the doc cross-links to flows.ts (Plan 01) + pressure.ts (Phase 2) + intel/constraints.md §C-flow-economy + COMMAND-CENTER-CONTRACT.md §7 Slice E; one atomic commit landed. FLOW-02 acceptance is satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add discoverability footer link to docs/COMMAND-CENTER-CONTRACT.md (OPTIONAL but RECOMMENDED — single line)</name>

<read_first> - docs/COMMAND-CENTER-CONTRACT.md (read the entire file — confirm there's a §10 "Guidance for the next implementation agent" or similar near the end of the file; the link goes after that section in the "references" / "footer" area per CONTEXT.md D-03-A) - docs/FLOW-LEGEND.md (Task 1 output — confirm the file exists at the cited path) - .planning/phases/03-resource-flow-overlays/03-CONTEXT.md (D-03-A — discoverability bullet: "added to `docs/COMMAND-CENTER-CONTRACT.md` references section (§9 or footer) so future contributors land on it from the binding contract. The contract itself is NOT modified to add the 8 flows inline — that would be a contract revision and is out of Phase 3 scope.") - .planning/phases/03-resource-flow-overlays/03-RESEARCH.md (Open Question 3 — Recommendation: include as the LAST commit in Phase 3) - .planning/phases/03-resource-flow-overlays/03-PATTERNS.md (`docs/COMMAND-CENTER-CONTRACT.md` (OPTIONAL) section starting around line 854 — Discipline rules: LAST commit, NOT a contract revision)
</read_first>

  <files>
    docs/COMMAND-CENTER-CONTRACT.md (MODIFY — single new line in references/footer)
  </files>

  <action>
    1. Open `docs/COMMAND-CENTER-CONTRACT.md`. Identify the appropriate footer location:
       - Preferred: a "References" or "See also" section near the end of the file (planner picks based on actual file structure).
       - Fallback: append after the last existing section (e.g., §10 "Guidance for the next implementation agent") with a new `## See also` heading if no references section exists.
    2. Insert ONE new line. The line text MUST be EXACTLY:
       ```markdown
       See also: `docs/FLOW-LEGEND.md` for the eight `C-flow-economy` flow definitions surfaced via Command Center's `<FlowOverlay />` (Phase 3 / FLOW-01).
       ```
       If the section is `## See also`, place this as the first/only bullet. If appending to an existing references area, place it under whichever subsection already references other supporting docs.
    3. DO NOT enumerate the 8 flows inline in the contract. DO NOT modify §6 Pressure Systems. DO NOT modify §7 Slice A/B/C/D/E. DO NOT modify §2 Prime Directive. DO NOT modify §3 Source-of-Truth Map. The change is ONE LINE — discoverability only, NOT a contract revision per CONTEXT.md OOS bullet.
    4. Verify the file:
       - `git diff --stat docs/COMMAND-CENTER-CONTRACT.md` shows exactly 1 file changed; `+` lines should be 1 or 2 (one for the link itself, optionally one for a new heading or blank line).
       - `grep -q 'FLOW-LEGEND.md' docs/COMMAND-CENTER-CONTRACT.md` exits 0 (link present).
       - `grep -q 'docs/FLOW-LEGEND.md' docs/COMMAND-CENTER-CONTRACT.md` exits 0 (full path present in the link text).
    5. Commit: `source ~/.nvm/nvm.sh && nvm use 22 && git add docs/COMMAND-CENTER-CONTRACT.md && git commit -m "docs(phase-03-03): link FLOW-LEGEND.md from COMMAND-CENTER-CONTRACT.md references" -m "Per CONTEXT.md D-03-A discoverability bullet + RESEARCH.md Open Question 3 (Recommendation: include). Single-line addition; NOT a contract revision (the contract is not modified to enumerate the 8 flows inline — that would inflate Phase 3 scope and conflate living vs stable doc concerns)."`
    6. Verify the commit contains exactly one file: `git show --stat HEAD` should list ONLY `docs/COMMAND-CENTER-CONTRACT.md`. If any other file was accidentally included, run `git reset HEAD~1 --soft`, restage only the contract file, and re-commit.
  </action>

  <verify>
    <automated>cd /Users/chrisknuteson/Projects/ctkadvisors/active/kagent && grep -q 'FLOW-LEGEND.md' docs/COMMAND-CENTER-CONTRACT.md && grep -q 'docs/FLOW-LEGEND.md' docs/COMMAND-CENTER-CONTRACT.md && [ "$(git show --stat HEAD --pretty=format: | grep -E '^ docs/' | wc -l)" -eq 1 ]</automated>
  </verify>

<acceptance_criteria> - `grep -q 'FLOW-LEGEND.md' docs/COMMAND-CENTER-CONTRACT.md` exits 0 (link to FLOW-LEGEND.md present per CONTEXT.md D-03-A discoverability) - `grep -q 'docs/FLOW-LEGEND.md' docs/COMMAND-CENTER-CONTRACT.md` exits 0 (full doc path present in the link) - `grep -q 'FLOW-01' docs/COMMAND-CENTER-CONTRACT.md` returns NEW count > pre-Wave-3 count (the new line cites Phase 3 / FLOW-01) - The commit at HEAD contains EXACTLY ONE changed file: `docs/COMMAND-CENTER-CONTRACT.md` — verified by `git show --stat HEAD` listing only this file. The contract revision is intentionally minimal per CONTEXT.md OOS bullet ("Modifying `docs/COMMAND-CENTER-CONTRACT.md` §6 to enumerate the 8 flows inline is OFF the table — single discoverability link only"). - The diff to `docs/COMMAND-CENTER-CONTRACT.md` is small: `git diff HEAD~1 HEAD -- docs/COMMAND-CENTER-CONTRACT.md | grep -c '^+' | awk '{ if ($1 <= 5) exit 0; else exit 1 }'` (planner verifies the addition is ≤5 lines including any blank-line padding; signals the change is a discoverability link, not a contract revision) - One atomic commit lands on the branch with `docs(phase-03-03):` scope and a body explaining the discoverability rationale + the explicit non-revision note
</acceptance_criteria>

  <done>
    docs/COMMAND-CENTER-CONTRACT.md has a single new footer/references line linking to docs/FLOW-LEGEND.md; the contract is not otherwise modified (no inline flow enumeration; no Prime Directive change; no §6/§7 modification); commit is isolated to one file. Phase 3 is now fully complete — both FLOW-01 (Plans 01+02) and FLOW-02 (Plan 03) are satisfied; the phase delivers data + UI + reload-stability proof + developer doc + discoverability link.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                                                                           |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Phase 3 Plan 03 ships pure markdown documentation. No new trust boundaries — markdown is rendered by GitHub / VSCode / etc.; no code paths touched; no API surface; no env vars; no fixtures; no test infrastructure. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category               | Component                                     | Disposition                | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                     |
| --------- | ---------------------- | --------------------------------------------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-09   | Information disclosure | docs/FLOW-LEGEND.md leaks internal details    | accept                     | Doc is committed to a public-by-default repo (kagent is OSS / MIT). Contents reference only file paths + field names + operator-facing descriptions of public concepts (`C-flow-economy` is in the design north stars; `flows.ts` is shipped public code). No secrets, no PII, no internal-only architecture beyond what's already in `docs/COMMAND-CENTER-CONTRACT.md` and `intel/constraints.md`. |
| T-03-10   | Tampering              | Doc drift between flows.ts and FLOW-LEGEND.md | accept (manual discipline) | The "Living doc" footer + per-flow `### <kind>` heading naming convention makes drift visible in code review. The optional CI lint that grep-asserts every `FLOW_TYPES` entry has a corresponding `### ` section in FLOW-LEGEND.md is deferred per CONTEXT.md "Claude's Discretion" + RESEARCH.md Common Pitfalls — add when a real-world drift slips through review.                               |

**No HIGH-severity threats in Phase 3 Plan 03.** Pure markdown documentation; ASVS V2/V3/V4/V5/V6/V7/V8/V9 all N/A.
</threat_model>

<verification>
1. After Task 1: `test -f docs/FLOW-LEGEND.md && grep -q 'C-flow-economy' docs/FLOW-LEGEND.md && [ "$(grep -c '^## ' docs/FLOW-LEGEND.md)" -ge 8 ]` exits 0 (file exists; citation present; ≥8 `## ` headings present per VALIDATION.md task 03-XX-09 + 03-XX-10).
2. After Task 1: `[ "$(grep -cE '^### (modelPower|tokenFlow|buildPower|podCapacity|artifactBandwidth|authority|trust|attention)$' docs/FLOW-LEGEND.md)" -eq 8 ]` exits 0 (exactly 8 per-flow `### ` subsections in canonical order).
3. After Task 2: `grep -q 'FLOW-LEGEND.md' docs/COMMAND-CENTER-CONTRACT.md` exits 0 (discoverability link present); `git show --stat HEAD` lists ONLY docs/COMMAND-CENTER-CONTRACT.md as the changed file.
4. Phase-level: `pnpm -C packages/workbench-ui test -- --run` exits 0 (Phase 3 doesn't touch tests in Wave 3 but the suite from Wave 2 should still be green).
5. Phase-level: `cd packages/workbench-ui && pnpm exec tsc --noEmit` exits 0.
6. Manual reviewer check (per VALIDATION.md "Manual-Only Verifications" #1): reviewer reads each of 8 sections in FLOW-LEGEND.md; checks for substrate source field cited, pressure trigger cited, operator action stated, v0.2 fallback noted (where applicable), promotion path stated. Approval signal goes in the SUMMARY.md.
7. `git log --oneline -n 9` shows the full Phase 3 commit history: 4 commits from Plan 01 (Wave 1) + 4 commits from Plan 02 (Wave 2) + 2 commits from Plan 03 (Wave 3) = 10 atomic commits with `phase-03-{01|02|03}` scope per CLAUDE.md Conventional Commits.
</verification>

<success_criteria>
**Plan 03 success = Wave 3 doc commit landed AND optional contract footer link landed AND Phase 3 fully complete.**

Concretely:

- ✅ `docs/FLOW-LEGEND.md` exists at the expected path with at-a-glance table + 8 per-flow `### ` subsections + framing + sources + footer.
- ✅ All grep gates from VALIDATION.md task 03-XX-09 + 03-XX-10 pass: `grep -q 'C-flow-economy'`, `grep -c '^## '` ≥8, the per-flow `### ` subsection count is exactly 8.
- ✅ `docs/COMMAND-CENTER-CONTRACT.md` has ONE new footer/references line linking to `docs/FLOW-LEGEND.md`. Contract is NOT otherwise modified (no inline flow enumeration; no Prime Directive change).
- ✅ `git show --stat HEAD` for Task 2's commit lists ONLY `docs/COMMAND-CENTER-CONTRACT.md` (one-file isolation per CONTEXT.md OOS discipline).
- ✅ Two atomic commits with `docs(phase-03-03):` scope.
- ✅ FLOW-02 acceptance criterion fully satisfied: legend exists; lives in developer docs (NOT in main UI chrome); maps each flow to substrate source + pressure trigger + operator action; living-doc note present.
- ✅ Phase 3 §11 bounds-test answer is FULLY satisfied across Plans 01+02+03.
- ✅ Phase 3 §15 one-sentence-test answer is FULLY satisfied across Plans 01+02+03.

**Phase 3 is COMPLETE after Plan 03.** Both FLOW-01 (Plans 01+02) and FLOW-02 (Plan 03) requirements are satisfied. The substrate gains 8 always-visible source-bound resource-flow gauges in Command Center + a developer-facing legend doc + a discoverability link from the binding contract — without expanding substrate primitives (no new CRDs, no new endpoints, no new env vars, no new persistence).
</success_criteria>

<output>
After completion, create `.planning/phases/03-resource-flow-overlays/03-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` template. Include:
- Wave-3 execution timing
- The final line count of `docs/FLOW-LEGEND.md` and a brief note on which sections were most informative
- Whether Task 2 (the optional contract footer link) was included (default: yes per RESEARCH.md Open Question 3 recommendation)
- A phase-completion summary: total commits across Plans 01+02+03 (expected: ~10), total new files (expected: 5 new files — flows.ts, flows.test.ts, FlowOverlay.tsx, FlowOverlay.module.css, FlowOverlay.test.tsx, docs/FLOW-LEGEND.md = 6 new files), total modified files (expected: 4 — source-binding.ts, source-binding.test.ts, cc-snapshot.json, CommandView.tsx + 1 modified contract = 5), total snapshot files regenerated (expected: 2 — cc-reload.test.tsx.snap + FlowOverlay.test.tsx.snap)
- A note on whether the phase passes the §11 bounds test + §15 one-sentence test (planner answers: YES if the verification gates above all green; otherwise flag specific gaps)
- A pointer to `/gsd-verify-work` as the next slash command for the operator
- A reminder that Phase 4 (REV-* requirements + the real review-queue projection that promotes the `attention` flow stub) is the next active phase per ROADMAP.md
</output>
