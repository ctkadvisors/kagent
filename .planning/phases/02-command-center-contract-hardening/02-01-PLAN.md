---
phase: 02-command-center-contract-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/workbench-ui/src/command/source-binding.ts
  - packages/workbench-ui/src/command/source-binding.test.ts
  - packages/workbench-ui/src/command/pressure.ts
  - packages/workbench-ui/src/command/pressure.test.ts
  - packages/workbench-ui/src/command/PressureOverlay.tsx
  - packages/workbench-ui/src/command/PressureOverlay.test.tsx
  - packages/workbench-ui/src/command/PressureOverlay.module.css
  - packages/workbench-ui/src/command/cc-orphan.test.ts
  - packages/workbench-ui/src/command/cc-reload.test.tsx
  - packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json
autonomous: true
requirements: [CC-01, CC-02, CC-03, CC-04]
notes: |
  Wave 0 scaffolding for Phase 2. Creates every NEW file as a strict-typed
  skeleton (it.todo placeholders for tests; null-render component; empty
  PRESSURE_TYPES array) so subsequent waves drop implementation into stable
  filenames. Extends source-binding.ts with the four new closed-enum
  field-name types AND widens the runtime helpers to be generic over DTO
  type (per RESEARCH.md Finding 14, Option A — closed enum still enforced
  at every call site by TypeScript). Captures the cc-snapshot.json fixture
  by hand from types.ts shapes (homelab is GitOps-only; no live workbench-api
  to curl per RESEARCH.md Finding 8).

must_haves:
  truths:
    - 'packages/workbench-ui/src/command/source-binding.ts exports four new closed-enum types (AgentSummaryFieldName, TaskSummaryFieldName, GatewayCapacityFieldName, PressureFieldName) alongside the existing DispositionFieldName, and the runtime helpers (assertSourceField, assertSourceFields, useSourceField, useSourceFields) accept any of the five DTO types via TypeScript generics — narrowing is enforced at the call site by the closed-enum K.'
    - 'Existing Phase 1 source-binding.test.ts tests (Tests 1-10) still pass after the generics widening — no behavioral regression in the disposition slice.'
    - 'A skeleton pressure.ts module exists exporting PressureMarker interface, PressureType interface, an empty PRESSURE_TYPES readonly array, and a PressureFieldName type derived from PRESSURE_TYPES[number]["kind"]. MIT license header present. Strict-typed.'
    - 'A skeleton pressure.test.ts file exists with vi.stubEnv beforeEach/afterEach, a makeSnapshot factory matching CommandSnapshot, and 18 it.todo placeholders (one fires + one absent test per pressure kind across all 9 kinds). The file passes vitest as a placeholder.'
    - 'A skeleton PressureOverlay.tsx component exists matching DispositionOverlay.tsx structure (snapshot prop + pressureDramatization?: boolean prop; renders null when markers.length === 0). MIT license header present.'
    - 'A skeleton PressureOverlay.test.tsx file exists with helper makeSnapshot factory and it.todo placeholders for the snapshot/source-binding/dramatization tests. The file passes vitest as a placeholder.'
    - 'PressureOverlay.module.css exists as an empty placeholder so the import in PressureOverlay.tsx resolves without error.'
    - 'A skeleton cc-orphan.test.ts file exists with three it.todo placeholders covering the canvas-side orphan throw, the AgentPanel field-orphan throw, and NODE_ENV=production no-op. The file passes vitest.'
    - 'A skeleton cc-reload.test.tsx file exists with one it.todo placeholder for the mount/unmount/remount DOM + scene-graph deep-equal assertion. The file passes vitest.'
    - 'The fixture file packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json exists with valid JSON conforming to the four DTO shapes (AgentSummaryRow[], TaskSummary[], GatewayCapacityRow[], DispositionOverlayRow[]) and contains at least one triggering scenario for every one of the 9 pressure types per CC-04 fixture coverage requirements.'
    - "The §11 bounds test (Phase 2): declared capability = Command Center renders Agent / Task / Gateway / disposition / pressure objects with proven backing source fields, reload-stable, and operator can answer 'what object is this?' at every selection; bounded resource drain = dev-only assertions are no-op in prod (zero runtime cost), pressure classification is O(snapshot size) per render and runs only on snapshot change, no new persistence, no new API surface, snapshot fixture is committed once and re-used; observable state transition = orphan assertion throws in dev with a referenceable error pointing at COMMAND-CENTER-CONTRACT.md §2, scene-graph snapshot diff is inspectable, data-source-field DOM attributes are readable from devtools / scrapeable by future CI lint; auditable output = vitest CI run is the auditable surface; revocation path = NODE_ENV=production disables the orphan assertions, VITE_PRESSURE_DRAMATIZATION=false disables visual treatment for all 9 pressure types, ALL Phase 2 changes are pure UI-package code so a single revert removes the entire phase."
    - 'The §15 one-sentence test (Phase 2): Generalizing source-binding from the disposition slice to the whole Command Center, plus reload-stable rendering and operational read depth on selection panels, makes the existing Command Center provably faithful to substrate state and gives operators legible failure pressure — strengthening observability and review without expanding substrate primitives.'
  artifacts:
    - path: 'packages/workbench-ui/src/command/source-binding.ts'
      provides: 'Extended with AgentSummaryFieldName, TaskSummaryFieldName, GatewayCapacityFieldName, PressureFieldName closed-enum types; generic runtime helpers'
      exports:
        [
          'AgentSummaryFieldName',
          'TaskSummaryFieldName',
          'GatewayCapacityFieldName',
          'PressureFieldName',
          'assertSourceField',
          'assertSourceFields',
          'useSourceField',
          'useSourceFields',
        ]
    - path: 'packages/workbench-ui/src/command/pressure.ts'
      provides: 'Skeleton: PressureMarker interface, PressureType interface, empty PRESSURE_TYPES readonly array, PressureFieldName derived type'
      exports: ['PressureMarker', 'PressureType', 'PRESSURE_TYPES', 'PressureFieldName']
    - path: 'packages/workbench-ui/src/command/pressure.test.ts'
      provides: 'Skeleton: 18 it.todo placeholders — one present + one absent per pressure kind (9 kinds × 2 = 18)'
      contains: 'it.todo'
    - path: 'packages/workbench-ui/src/command/PressureOverlay.tsx'
      provides: 'Skeleton component: returns null until classify implemented'
      exports: ['PressureOverlay', 'PressureOverlayProps']
    - path: 'packages/workbench-ui/src/command/PressureOverlay.test.tsx'
      provides: 'Skeleton: it.todo placeholders for snapshot/source-binding/dramatization tests'
      contains: 'it.todo'
    - path: 'packages/workbench-ui/src/command/PressureOverlay.module.css'
      provides: 'Empty placeholder so import resolves'
    - path: 'packages/workbench-ui/src/command/cc-orphan.test.ts'
      provides: 'Skeleton: it.todo for canvas-orphan throw, AgentPanel field orphan, prod no-op'
      contains: 'it.todo'
    - path: 'packages/workbench-ui/src/command/cc-reload.test.tsx'
      provides: 'Skeleton: it.todo for DOM + scene-graph reload-stability deep-equal'
      contains: 'it.todo'
    - path: 'packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json'
      provides: 'Hand-crafted CommandSnapshot fixture covering all 9 pressure-trigger scenarios + reload-stability test data'
      contains: 'agents'
  key_links:
    - from: 'packages/workbench-ui/src/command/pressure.ts'
      to: 'packages/workbench-ui/src/command/state.ts'
      via: "imports CommandSnapshot type-only via './state.js'"
      pattern: 'import type \{ CommandSnapshot \}'
    - from: 'packages/workbench-ui/src/command/source-binding.ts'
      to: 'packages/workbench-ui/src/command/pressure.ts'
      via: "re-exports PressureFieldName via 'export type \\{ PressureFieldName \\} from ./pressure.js'"
      pattern: 'export type .* PressureFieldName'
    - from: 'packages/workbench-ui/src/command/PressureOverlay.tsx'
      to: 'packages/workbench-ui/src/command/pressure.ts'
      via: "imports PRESSURE_TYPES, PressureMarker, PressureType from './pressure.js'"
      pattern: 'PRESSURE_TYPES'
---

<objective>
Wave 0 scaffolding for Phase 2 Command Center contract hardening. Creates the file-existence skeleton every later wave depends on (per 02-VALIDATION.md Wave 0 gates) AND extends `packages/workbench-ui/src/command/source-binding.ts` with the four new closed-enum field-name types per the locked decision D-CC-01-A.

Purpose: Without these files in place, Wave 1's CC-01 / CC-04 tests cannot run, Wave 2's CC-03 panel additions cannot import the new enum types, and Wave 3's CC-02 reload-stability test has no fixture to load. The widening of `source-binding.ts` runtime helpers to generics (per RESEARCH.md Finding 14, Option A) lets every later wave reuse Phase 1's verified `isDevBuild()` + assert pattern across all four DTO types without per-DTO helper duplication.

Output: 8 NEW files + 2 MODIFIED files. All scaffolds compile under TypeScript strict mode and pass `pnpm -C packages/workbench-ui test -- --run`. No implementation logic in this plan — Wave 1+ fills in the bodies. The `cc-snapshot.json` fixture is the only file with real data; everything else is structural.

§11 bounds-test answer (Phase 2 — verbatim from CONTEXT.md `<specifics>` item 4): declared capability = Command Center can render Agent / Task / Gateway / disposition / pressure objects with proven backing source fields, reload-stable, and operator can answer "what object is this?" at every selection; bounded resource drain = dev-only assertions are no-op in prod (zero runtime cost), pressure classification is O(snapshot size) per render and runs only on snapshot change, no new persistence, no new API surface, snapshot fixture is committed once and re-used across reload tests; observable state transition = orphan assertion throws in dev with a referenceable error pointing at COMMAND-CENTER-CONTRACT.md §2, scene-graph snapshot diff is inspectable, `data-source-field` DOM attributes are readable from devtools / scrapeable by future CI lint; auditable output = vitest CI run is the auditable surface — orphan-assertion test, reload-stability test, per-pressure-type test all fail loud on regression; revocation path = `NODE_ENV=production` disables the orphan assertions (tests stub this explicitly), `VITE_PRESSURE_DRAMATIZATION=false` disables visual treatment for all 9 pressure types, ALL Phase 2 changes are pure UI-package code (`packages/workbench-ui/`) so a single revert removes the entire phase.

§15 one-sentence-test answer (Phase 2 — verbatim from CONTEXT.md `<specifics>` item 5): "Generalizing source-binding from the disposition slice to the whole Command Center, plus reload-stable rendering and operational read depth on selection panels, makes the existing Command Center provably faithful to substrate state and gives operators legible failure pressure — strengthening observability and review without expanding substrate primitives."

Critical Phase 2 ground rules honored throughout:

- D7 / Prime Directive (COMMAND-CENTER-CONTRACT.md §2) — every newly rendered field maps back to a substrate source.
- No new CRD, no new workbench-api endpoint, no `/api/pressure` — pure UI work in `packages/workbench-ui/`.
- No new substrate write surface (Slice C / Slice D explicitly out per §7 ordering).
- Read-side ONLY; "read-depth precedes write-depth" is load-bearing.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-command-center-contract-hardening/02-CONTEXT.md
@.planning/phases/02-command-center-contract-hardening/02-RESEARCH.md
@.planning/phases/02-command-center-contract-hardening/02-PATTERNS.md
@.planning/phases/02-command-center-contract-hardening/02-VALIDATION.md
@docs/COMMAND-CENTER-CONTRACT.md
@packages/workbench-ui/src/command/source-binding.ts
@packages/workbench-ui/src/command/source-binding.test.ts
@packages/workbench-ui/src/command/DispositionOverlay.tsx
@packages/workbench-ui/src/command/DispositionOverlay.test.tsx
@packages/workbench-ui/src/command/DispositionOverlay.module.css
@packages/workbench-ui/src/command/state.ts
@packages/workbench-ui/src/types.ts
@packages/dto/src/types.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from source files in this session. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From `packages/workbench-ui/src/types.ts` (lines 48-68 — TaskSummary):

```typescript
export interface TaskSummary {
  readonly name: string;
  readonly namespace: string;
  readonly uid: string;
  readonly phase?: AgentTaskPhase; // 'Pending' | 'Dispatched' | 'Completed' | 'Failed'
  readonly targetAgent?: string;
  readonly targetCapability?: string;
  readonly model?: string;
  readonly createdAt?: string;
  readonly startedAt?: string;
  readonly completedAt?: string;
  readonly podName?: string;
  readonly error?: string;
  readonly suspicious?: readonly string[];
  readonly artifactCount?: number;
  readonly childCount?: number;
  readonly aggregatePhase?: AggregatePhase;
}
```

From `packages/workbench-ui/src/types.ts` (lines 191-204 — AgentSummaryRow):

```typescript
export interface AgentSummaryRow {
  readonly name: string;
  readonly namespace: string;
  readonly model?: string;
  readonly modelClass?: string;
  readonly tools?: readonly string[];
  readonly capabilities?: readonly string[];
}
```

From `packages/workbench-ui/src/types.ts` (lines 248-265 — GatewayCapacityRow):

```typescript
export interface GatewayCapacityRow {
  readonly model: string;
  readonly endpoint: string;
  readonly backendKind: string;
  readonly inFlight: number;
  readonly currentCap: number;
  readonly seed: number;
  readonly max: number;
  readonly minSafe: number;
  readonly recentP50Ms: number | null; // null, NOT undefined — guard with != null
  readonly crName?: string;
  readonly crNamespace?: string;
}
```

From `packages/workbench-ui/src/command/state.ts` (lines 48-71 — CommandSnapshot):

```typescript
export interface CommandSnapshot {
  readonly agents: ReadonlyMap<string, AgentSummaryRow>;
  readonly tasks: ReadonlyMap<string, TaskSummary>;
  readonly gatewayCapacity: readonly GatewayCapacityRow[];
  readonly gatewayUsage: readonly GatewayUsageRow[];
  readonly dispositions: ReadonlyMap<string, DispositionOverlayRow>;
  readonly events: readonly ActivityEvent[];
  readonly lastEventAt: number;
  readonly error: string | null;
}
```

From `packages/workbench-ui/src/command/source-binding.ts` (lines 44-56 — existing DispositionFieldName, DO NOT remove):

```typescript
type DispositionFieldName =
  | 'agentRef'
  | 'namespace'
  | 'agentName'
  | 'configMapName'
  | 'idleBehavior'
  | 'spentTokensToday'
  | 'postsToday'
  | 'proposalsToday'
  | 'overBudget'
  | 'overBudgetReason'
  | 'overBudgetEventCountToday'
  | 'dailyBoundaryUtc';
```

The four new Phase 2 closed-enum types to ADD (verbatim from CONTEXT.md D-CC-01-A and RESEARCH.md Findings 10/11/12):

```typescript
export type AgentSummaryFieldName =
  | 'name'
  | 'namespace'
  | 'model'
  | 'modelClass'
  | 'tools'
  | 'capabilities';

export type TaskSummaryFieldName =
  | 'name'
  | 'namespace'
  | 'uid'
  | 'phase'
  | 'targetAgent'
  | 'targetCapability'
  | 'model'
  | 'createdAt'
  | 'startedAt'
  | 'completedAt'
  | 'podName'
  | 'error'
  | 'suspicious'
  | 'artifactCount'
  | 'childCount'
  | 'aggregatePhase';

export type GatewayCapacityFieldName =
  | 'model'
  | 'endpoint'
  | 'backendKind'
  | 'inFlight'
  | 'currentCap'
  | 'seed'
  | 'max'
  | 'minSafe'
  | 'recentP50Ms'
  | 'crName'
  | 'crNamespace';

// Re-exported from pressure.ts to keep PRESSURE_TYPES the single source of truth:
export type { PressureFieldName } from './pressure.js';
```

The 9 pressure kinds (CONTEXT.md D-CC-04-A) — used in the `it.todo` test names:
`'context' | 'gateway' | 'policy' | 'verifier' | 'artifact' | 'trace' | 'pod' | 'quota' | 'telemetry'`

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend source-binding.ts with four new closed-enum types and widen runtime helpers to generics</name>
  <files>
    packages/workbench-ui/src/command/source-binding.ts,
    packages/workbench-ui/src/command/source-binding.test.ts
  </files>
  <read_first>
    packages/workbench-ui/src/command/source-binding.ts (current implementation — read all 178 lines so the existing DispositionFieldName + helpers are preserved verbatim and only widened),
    packages/workbench-ui/src/command/source-binding.test.ts (Tests 1-10 must continue passing after generics widening),
    .planning/phases/02-command-center-contract-hardening/02-PATTERNS.md (`### src/command/source-binding.ts (MODIFY — extend with 4 new enum types)` section — verbatim shape to mirror),
    .planning/phases/02-command-center-contract-hardening/02-RESEARCH.md (Findings 10-13 — exact closed-enum members for each new type, isDevBuild() body),
    packages/workbench-ui/src/types.ts (AgentSummaryRow lines 191-204, TaskSummary lines 48-68, GatewayCapacityRow lines 248-265 — closed-enum sources of truth)
  </read_first>
  <behavior>
    - Test A: assertSourceField passes silently when field is present on AgentSummaryRow (e.g., `assertSourceField(agentRow, 'capabilities')` does not throw).
    - Test B: assertSourceField THROWS in dev for a synthesized AgentSummaryRow missing `capabilities` (cast via `as unknown as AgentSummaryRow`); error message matches `/source-binding violation: rendered field 'capabilities' has no backing source/`.
    - Test C: assertSourceField is a no-op in production (`vi.stubEnv('NODE_ENV', 'production')`) for the same orphan AgentSummaryRow.
    - Test D: useSourceField('capabilities' as AgentSummaryFieldName) returns the literal string `'capabilities'`.
    - Test E: assertSourceFields THROWS in dev for a synthesized TaskSummary missing `phase`; error message matches `/sourceFields=phase,targetAgent/` AND mentions `computed value`.
    - Test F: useSourceFields(['phase', 'targetAgent'] as TaskSummaryFieldName[]) returns `'phase,targetAgent'`.
    - Test G: assertSourceField passes for every member of the AgentSummaryFieldName closed enum (`name`, `namespace`, `model`, `modelClass`, `tools`, `capabilities`) on a fully-populated AgentSummaryRow.
    - Test H: assertSourceField passes for every member of the GatewayCapacityFieldName closed enum (`model`, `endpoint`, `backendKind`, `inFlight`, `currentCap`, `seed`, `max`, `minSafe`, `recentP50Ms`, `crName`, `crNamespace`) on a fully-populated GatewayCapacityRow.
    - Test I: existing Tests 1-10 in source-binding.test.ts (DispositionOverlayRow scope) all still pass.
  </behavior>
  <action>
    1. **Modify `packages/workbench-ui/src/command/source-binding.ts`:**
       - Keep lines 1-99 verbatim (MIT header, top jsdoc, existing DispositionFieldName, isDevBuild() function body — DO NOT touch the precedence order tested by source-binding.test.ts Tests 3, 5, 9).
       - **Convert `DispositionFieldName` from `type` to `export type`** (line 44 — add `export ` keyword) so cc-orphan.test.ts can import it. The existing `assertSourceField(row: DispositionOverlayRow, field: DispositionFieldName)` signature still works because DispositionOverlayRow already has all those keys.
       - **Add the three NEW closed-enum exports immediately after the existing DispositionFieldName** (around line 57):
         ```typescript
         /** Closed enumeration of AgentSummaryRow top-level field names. */
         export type AgentSummaryFieldName =
           | 'name' | 'namespace' | 'model' | 'modelClass' | 'tools' | 'capabilities';

         /** Closed enumeration of TaskSummary top-level field names. */
         export type TaskSummaryFieldName =
           | 'name' | 'namespace' | 'uid' | 'phase' | 'targetAgent' | 'targetCapability'
           | 'model' | 'createdAt' | 'startedAt' | 'completedAt' | 'podName' | 'error'
           | 'suspicious' | 'artifactCount' | 'childCount' | 'aggregatePhase';

         /** Closed enumeration of GatewayCapacityRow top-level field names. */
         export type GatewayCapacityFieldName =
           | 'model' | 'endpoint' | 'backendKind' | 'inFlight' | 'currentCap'
           | 'seed' | 'max' | 'minSafe' | 'recentP50Ms' | 'crName' | 'crNamespace';

         /**
          * Re-exported from pressure.ts so PRESSURE_TYPES stays the single
          * source of truth for the pressure kind union (per CONTEXT.md D-CC-04-A).
          */
         export type { PressureFieldName } from './pressure.js';
         ```
       - **Widen the four runtime helpers to generics** per RESEARCH.md Finding 14 Option A. Replace the existing function signatures (lines 112, 132, 153, 175) with these generic versions; KEEP the function bodies (the assertion logic and error messages) IDENTICAL so existing tests still pass:
         ```typescript
         export function assertSourceField<T extends object, K extends string>(
           row: T,
           field: K,
         ): void {
           if (!isDevBuild()) return;
           if (!(field in row)) {
             const ref = (row as { agentRef?: string }).agentRef;
             throw new Error(
               `source-binding violation: rendered field '${String(field)}' has no backing source` +
                 (ref !== undefined ? ` (agentRef=${ref})` : '') +
                 `. See COMMAND-CENTER-CONTRACT.md §2 Prime Directive.`,
             );
           }
         }

         export function useSourceField<K extends string>(field: K): K {
           return field;
         }

         export function assertSourceFields<T extends object, K extends string>(
           row: T,
           fields: readonly K[],
         ): void {
           if (!isDevBuild()) return;
           for (const field of fields) {
             if (!(field in row)) {
               const ref = (row as { agentRef?: string }).agentRef;
               throw new Error(
                 `source-binding violation: rendered field '${String(field)}' has no backing source` +
                   (ref !== undefined ? ` (agentRef=${ref})` : '') +
                   ` [computed value listed sourceFields=${fields.join(',')}]. ` +
                   `See COMMAND-CENTER-CONTRACT.md §2 Prime Directive.`,
               );
             }
           }
         }

         export function useSourceFields<K extends string>(fields: readonly K[]): string {
           return fields.join(',');
         }
         ```
       - **Critical: preserve the EXACT error-message substrings** that source-binding.test.ts Tests 2 and 8 match against:
         - `/source-binding violation: rendered field '\w+' has no backing source/` (Test 2)
         - `/sourceFields=spentTokensToday,idleBehavior/` (Test 8)
         - `/computed value/` (Test 8)
         The wording above (`source-binding violation:` not `disposition source-binding violation:`) is a deliberate widening — Test 2 currently matches the regex `/source-binding violation: rendered field 'spentTokensToday' has no backing source/` which the new wording still satisfies. The previous prefix `disposition ` was specific to the old DispositionOverlayRow-only signature; the generic helper now serves four DTO types.

    2. **Modify `packages/workbench-ui/src/command/source-binding.test.ts`:**
       - Keep all existing Tests 1-10 verbatim. They must continue passing after the generics widening.
       - **Append a new `describe('source-binding (CC-01 generalization to AgentSummaryRow / TaskSummary / GatewayCapacityRow)', ...)` block** at the end of the file (after line 190), using the existing `vi.stubEnv` beforeEach/afterEach pattern. The block contains 9 `it(...)` tests (Tests A-I from `<behavior>` above). Mirror the test-naming style of source-binding.test.ts (`'Test A — ...'`, `'Test B — ...'`, etc.). For each test:
         - Build a synthetic DTO using `as unknown as <DTO type>` casting where needed (matches Test 2/8 pattern at lines 67-80, 147-160).
         - Use the `assertSourceField` / `useSourceField` / `assertSourceFields` / `useSourceFields` helpers exactly as Phase 1 does.
         - For Test G/H, fully populate the AgentSummaryRow / GatewayCapacityRow (all six / all eleven fields including `recentP50Ms: 42` for Gateway).

    3. **Run** `pnpm -C packages/workbench-ui test -- --run source-binding` and confirm zero failures.

  </action>
  <verify>
    <automated>pnpm -C packages/workbench-ui test -- --run source-binding</automated>
  </verify>
  <done>
    - `pnpm -C packages/workbench-ui test -- --run source-binding` exits 0.
    - `grep -E "^export type (AgentSummaryFieldName|TaskSummaryFieldName|GatewayCapacityFieldName)" packages/workbench-ui/src/command/source-binding.ts` finds 3 lines.
    - `grep -E "export type \\{ PressureFieldName \\} from './pressure\\.js'" packages/workbench-ui/src/command/source-binding.ts` finds 1 line.
    - `grep -E "^export type DispositionFieldName" packages/workbench-ui/src/command/source-binding.ts` finds 1 line (the `export ` was added; previously it was a private type).
    - `grep -E "function assertSourceField<T extends object, K extends string>" packages/workbench-ui/src/command/source-binding.ts` finds 1 line.
    - All 10 existing tests + 9 new tests in source-binding.test.ts pass (count via vitest output: ≥19 tests in the file, all green).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create pressure.ts skeleton, pressure.test.ts skeleton, PressureOverlay.tsx skeleton, PressureOverlay.test.tsx skeleton, PressureOverlay.module.css placeholder</name>
  <files>
    packages/workbench-ui/src/command/pressure.ts,
    packages/workbench-ui/src/command/pressure.test.ts,
    packages/workbench-ui/src/command/PressureOverlay.tsx,
    packages/workbench-ui/src/command/PressureOverlay.test.tsx,
    packages/workbench-ui/src/command/PressureOverlay.module.css
  </files>
  <read_first>
    packages/workbench-ui/src/command/DispositionOverlay.tsx (lines 1-191 — exact template to mirror for PressureOverlay.tsx structure including MIT header, props interface shape, FC component shape, useSourceFields usage, styles import),
    packages/workbench-ui/src/command/DispositionOverlay.test.tsx (lines 1-339 — exact template to mirror for PressureOverlay.test.tsx structure including makeSnapshot helper, vi.stubEnv beforeEach/afterEach, snapshotShape function for reload-stability),
    packages/workbench-ui/src/command/DispositionOverlay.module.css (1-143 — class-name set: .card, .header, .list, .row, .pressureMarker, .pressureMarkerSubdued — to mirror in PressureOverlay.module.css),
    packages/workbench-ui/src/command/state.ts (CommandSnapshot interface lines 48-71 — pressure.ts imports CommandSnapshot type-only),
    .planning/phases/02-command-center-contract-hardening/02-PATTERNS.md (the four sections: `### src/command/pressure.ts (CREATE — new module)`, `### src/command/pressure.test.ts (CREATE — new test)`, `### src/command/PressureOverlay.tsx (CREATE — new component)`, `### src/command/PressureOverlay.test.tsx (CREATE — new test)`, `### src/command/PressureOverlay.module.css (CREATE — new style)` — every structural choice is documented verbatim)
  </read_first>
  <acceptance_criteria>
    - File `packages/workbench-ui/src/command/pressure.ts` exists, contains `SPDX-License-Identifier: MIT`, exports `PressureMarker` (interface), `PressureType` (interface), `PRESSURE_TYPES` (readonly array, EMPTY in this scaffold), and `PressureFieldName` type.
    - `grep -E "export interface PressureMarker" packages/workbench-ui/src/command/pressure.ts` finds 1 line.
    - `grep -E "export interface PressureType" packages/workbench-ui/src/command/pressure.ts` finds 1 line.
    - `grep -E "export const PRESSURE_TYPES: readonly PressureType\\[\\] = \\[\\];" packages/workbench-ui/src/command/pressure.ts` finds 1 line (empty array).
    - `grep -E "export type PressureFieldName" packages/workbench-ui/src/command/pressure.ts` finds 1 line.
    - File `packages/workbench-ui/src/command/pressure.test.ts` exists, contains exactly 18 `it.todo` calls (one per `<test>` description below).
    - `grep -c "it.todo" packages/workbench-ui/src/command/pressure.test.ts` outputs 18.
    - File `packages/workbench-ui/src/command/PressureOverlay.tsx` exists, exports `PressureOverlay` (FC) and `PressureOverlayProps` interface; the component currently returns `null` unconditionally (`return null;`).
    - `grep -E "export interface PressureOverlayProps" packages/workbench-ui/src/command/PressureOverlay.tsx` finds 1 line.
    - `grep -E "export const PressureOverlay" packages/workbench-ui/src/command/PressureOverlay.tsx` finds 1 line.
    - File `packages/workbench-ui/src/command/PressureOverlay.test.tsx` exists, contains at least 4 `it.todo` calls (one each for: snapshot reload-stability, dramatic class present when pressureDramatization=true, dramatic class absent when pressureDramatization=false, source-field assertion fires for orphan).
    - `grep -c "it.todo" packages/workbench-ui/src/command/PressureOverlay.test.tsx` outputs ≥4.
    - File `packages/workbench-ui/src/command/PressureOverlay.module.css` exists (may be empty or contain placeholder comment; presence is what matters).
    - `pnpm -C packages/workbench-ui test -- --run pressure` exits 0 (the it.todo placeholders register as skipped, not as failures).
    - `pnpm -C packages/workbench-ui test -- --run PressureOverlay` exits 0.
    - The whole package compiles: `pnpm -C packages/workbench-ui exec tsc --noEmit -p tsconfig.build.json` exits 0 (NO TypeScript errors introduced).
  </acceptance_criteria>
  <action>
    1. **Create `packages/workbench-ui/src/command/pressure.ts`:**
       ```typescript
       /**
        * SPDX-License-Identifier: MIT
        * Copyright (c) 2026 Chris Knuteson
        */

       /**
        * pressure — Phase 2 / CC-04. UI-side classification of nine
        * pressure types from existing CommandSnapshot fields. No new
        * workbench-api endpoint, no new substrate state. Each entry in
        * PRESSURE_TYPES declares its source field(s), classify function,
        * and detail-link computation. Wave 1 fills in the per-type
        * implementations; this Wave-0 scaffold only fixes the shape.
        *
        * See COMMAND-CENTER-CONTRACT.md §6 Pressure Systems for the nine
        * canonical pressure types, and CONTEXT.md D-CC-04-A for the
        * decision to derive all 9 UI-side from snapshot fields.
        */

       import type { CommandSnapshot } from './state.js';

       export interface PressureMarker {
         readonly kind: PressureType['kind'];
         /** Single source field; mutually exclusive with sourceFields. */
         readonly sourceField?: string;
         /** Multiple source fields when the marker derives from a computed value. */
         readonly sourceFields?: readonly string[];
         /** ns/name of the impacted Agent or Task — used for stable React keys + detail links. */
         readonly affectedKey?: string;
         /** Hash-route deep link (#/tasks/<ns>/<name>, #/gateway, #/cluster). */
         readonly detailLink: string;
         /** Operator-facing label (rendered as anchor textContent). */
         readonly label: string;
       }

       export interface PressureType {
         readonly kind:
           | 'context' | 'gateway' | 'policy' | 'verifier'
           | 'artifact' | 'trace' | 'pod' | 'quota' | 'telemetry';
         readonly sourceField?: string;
         readonly sourceFields?: readonly string[];
         readonly classify: (snapshot: CommandSnapshot) => PressureMarker[];
         readonly detailLink: (marker: PressureMarker) => string;
       }

       /**
        * Wave 0 scaffold — empty array. Wave 1 (02-02-PLAN.md) populates
        * with all nine entries (gateway, artifact, pod, quota, telemetry,
        * context, verifier, trace, policy) per CONTEXT.md D-CC-04-A.
        */
       export const PRESSURE_TYPES: readonly PressureType[] = [];

       /**
        * Derived from PRESSURE_TYPES['kind'] so the closed-enum stays in
        * one place. After Wave 1 populates PRESSURE_TYPES, this becomes
        * the union of all nine kind literals automatically.
        */
       export type PressureFieldName = PressureType['kind'];
       ```

    2. **Create `packages/workbench-ui/src/command/pressure.test.ts`:**
       ```typescript
       /**
        * SPDX-License-Identifier: MIT
        * Copyright (c) 2026 Chris Knuteson
        */

       /**
        * Phase 2 / CC-04 — pressure classification tests.
        *
        * Wave 0 scaffold: 18 it.todo placeholders (9 kinds × 2 — fires
        * when source data is present; does NOT fire when source data is
        * absent). Wave 1 fills in the bodies after PRESSURE_TYPES is
        * populated.
        */

       import { afterEach, beforeEach, describe, it, vi } from 'vitest';

       describe('pressure (CC-04 — classification)', () => {
         beforeEach(() => { vi.stubEnv('NODE_ENV', 'development'); });
         afterEach(() => { vi.unstubAllEnvs(); });

         it.todo('gateway saturation — fires when inFlight/currentCap >= 0.8');
         it.todo('gateway saturation — does NOT fire when inFlight/currentCap < 0.8');
         it.todo('artifact debt — fires when phase=Completed and artifactCount=0');
         it.todo('artifact debt — does NOT fire when phase=Completed and artifactCount>0');
         it.todo('pod failure — fires when phase=Failed and podName is defined');
         it.todo('pod failure — does NOT fire when phase=Failed and podName is undefined');
         it.todo('quota wall — fires when a disposition row has overBudget=true');
         it.todo('quota wall — does NOT fire when no disposition row has overBudget=true');
         it.todo('stale telemetry — fires when now − lastEventAt > 30s');
         it.todo('stale telemetry — does NOT fire when now − lastEventAt <= 30s');
         it.todo('context pressure — fires (TaskSummary heuristic) when childCount>=2 and phase=Dispatched');
         it.todo('context pressure — does NOT fire when childCount<2 or phase!=Dispatched');
         it.todo('verifier failure — fires when phase=Failed and error contains "verifier"');
         it.todo('verifier failure — does NOT fire when error does not contain "verifier"');
         it.todo('trace gap — fires when phase is terminal (Completed or Failed)');
         it.todo('trace gap — does NOT fire when phase is non-terminal');
         it.todo('policy denial — fires when phase=Failed and error contains "policy"');
         it.todo('policy denial — does NOT fire when error does not contain "policy"');
       });
       ```

    3. **Create `packages/workbench-ui/src/command/PressureOverlay.module.css`** — initially a single placeholder comment so the import resolves; Wave 2 fills in the real styles. Note CSS does NOT need the MIT SPDX header per CLAUDE.md, but include a brief comment so future readers know why it's empty:
       ```css
       /* Phase 2 / CC-04 — PressureOverlay styles. Wave 0 scaffold; styles
          land in 02-03-PLAN.md when the component renders real markers. */
       ```

    4. **Create `packages/workbench-ui/src/command/PressureOverlay.tsx`:**
       ```typescript
       /**
        * SPDX-License-Identifier: MIT
        * Copyright (c) 2026 Chris Knuteson
        */

       /**
        * PressureOverlay — Phase 2 / CC-04. Sibling overlay rendering the
        * nine pressure types from CommandSnapshot fields. Mirrors
        * DispositionOverlay's HTML-over-canvas pattern: a top-level
        * <aside> with absolutely-positioned anchors per marker, each
        * marker carrying data-source-field(s) attributes per
        * COMMAND-CENTER-CONTRACT.md §2 Prime Directive (D7).
        *
        * Wave 0 scaffold: returns null until PRESSURE_TYPES is populated
        * in Wave 1 (02-02-PLAN.md) and full JSX lands in Wave 2
        * (02-03-PLAN.md).
        */

       import { type FC, useMemo } from 'react';

       import { PRESSURE_TYPES } from './pressure.js';
       import type { PressureMarker } from './pressure.js';
       import type { CommandSnapshot } from './state.js';
       // Module CSS imported so Wave 2 can drop in styles without an additional file change.
       // eslint-disable-next-line @typescript-eslint/no-unused-vars
       import styles from './PressureOverlay.module.css';

       export interface PressureOverlayProps {
         readonly snapshot: CommandSnapshot;
         /**
          * Slice E base-building-only fallback. Default true. When false
          * the same data still renders but the dramatic CSS class is
          * replaced with a subdued one.
          */
         readonly pressureDramatization?: boolean;
       }

       export const PressureOverlay: FC<PressureOverlayProps> = ({
         snapshot,
         pressureDramatization: _pressureDramatization = true,
       }) => {
         // Wave 1 (02-02-PLAN.md) populates PRESSURE_TYPES with 9 entries;
         // until then this returns [] and the component renders null.
         const markers = useMemo<readonly PressureMarker[]>(
           () => PRESSURE_TYPES.flatMap((pt) => pt.classify(snapshot)),
           [snapshot],
         );
         if (markers.length === 0) return null;

         // Wave 2 (02-03-PLAN.md) replaces this with the full <aside>+<ul>
         // JSX (mirrors DispositionOverlay.tsx lines 71-189). Until then
         // the component cannot reach this branch (PRESSURE_TYPES is empty).
         return null;
       };
       ```
       Notes:
       - `_pressureDramatization` and the `styles` import are flagged unused for now via the underscore prefix and the eslint disable; Wave 2 removes both annotations. If the project's ESLint config is strict on unused-vars without underscore exception, swap to `void _pressureDramatization;` or move the destructuring into Wave 2's task. Confirm by running tsc/eslint after the file lands.
       - The `useMemo` is placed now (not deferred) so the same Wave-0 file shape ships forward — Wave 2 only adds JSX, no signature change.

    5. **Create `packages/workbench-ui/src/command/PressureOverlay.test.tsx`:**
       ```tsx
       /**
        * SPDX-License-Identifier: MIT
        * Copyright (c) 2026 Chris Knuteson
        */

       /**
        * Phase 2 / CC-04 — PressureOverlay tests.
        *
        * Wave 0 scaffold: it.todo placeholders for the four real test
        * cases. Wave 2 (02-03-PLAN.md) replaces them with actual render
        * tests once PRESSURE_TYPES is populated and the JSX lands.
        */

       import { afterEach, beforeEach, describe, it, vi } from 'vitest';

       describe('PressureOverlay (CC-04)', () => {
         beforeEach(() => { vi.stubEnv('NODE_ENV', 'development'); });
         afterEach(() => { vi.unstubAllEnvs(); });

         it.todo('renders each pressure marker with data-source-field or data-source-fields');
         it.todo('reload stability — re-render with same snapshot produces equal selector tree');
         it.todo('pressureDramatization=true applies dramatic class on markers');
         it.todo('pressureDramatization=false keeps same data but does NOT apply dramatic class');
       });
       ```

    6. **Run** `pnpm -C packages/workbench-ui test -- --run pressure PressureOverlay` and `pnpm -C packages/workbench-ui exec tsc --noEmit -p tsconfig.build.json`. Both must exit 0.

  </action>
  <verify>
    <automated>pnpm -C packages/workbench-ui test -- --run pressure PressureOverlay && pnpm -C packages/workbench-ui exec tsc --noEmit -p tsconfig.build.json</automated>
  </verify>
  <done>
    - All five files exist at the documented paths.
    - vitest exits 0 with no failures (the it.todo placeholders register as todo, not failures).
    - tsc --noEmit passes with no errors introduced by the new files.
    - The MIT header is on every `.ts`/`.tsx` file (verifiable with `grep -L "SPDX-License-Identifier: MIT"` returning empty for the four .ts/.tsx files in this task).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create cc-orphan.test.ts skeleton, cc-reload.test.tsx skeleton, and __fixtures__/cc-snapshot.json with all 9 pressure-trigger scenarios</name>
  <files>
    packages/workbench-ui/src/command/cc-orphan.test.ts,
    packages/workbench-ui/src/command/cc-reload.test.tsx,
    packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json
  </files>
  <read_first>
    packages/workbench-ui/src/command/source-binding.test.ts (Tests 2, 3, 8, 9 — orphan throw + prod no-op pattern; cc-orphan.test.ts mirrors this exactly),
    packages/workbench-ui/src/command/DispositionOverlay.test.tsx (lines 193-228 — Test 7 mount/rerender/snapshotShape pattern; cc-reload.test.tsx mirrors but uses unmount/remount instead of rerender),
    packages/workbench-ui/src/types.ts (DTO shapes for the fixture: AgentSummaryRow, TaskSummary, GatewayCapacityRow),
    packages/dto/src/types.ts (DispositionOverlayRow shape — `idleBehavior.attentionBudget.tokensPerDay`, `idleBehavior.proposalScope.maxProposalsPerDay`, etc.),
    .planning/phases/02-command-center-contract-hardening/02-PATTERNS.md (sections `### src/command/cc-orphan.test.ts (CREATE)`, `### src/command/cc-reload.test.tsx (CREATE)`, `### src/command/__fixtures__/cc-snapshot.json (CREATE — fixture)`),
    .planning/phases/02-command-center-contract-hardening/02-RESEARCH.md (Findings 8 + 14 — fixture coverage requirements per pressure type)
  </read_first>
  <acceptance_criteria>
    - File `packages/workbench-ui/src/command/cc-orphan.test.ts` exists with MIT header and contains exactly 3 `it.todo` calls naming: canvas-side orphan throw, AgentPanel field-orphan throw, NODE_ENV=production no-op.
    - `grep -c "it.todo" packages/workbench-ui/src/command/cc-orphan.test.ts` outputs 3.
    - File `packages/workbench-ui/src/command/cc-reload.test.tsx` exists with MIT header and contains exactly 1 `it.todo` call naming the mount/unmount/remount DOM + scene-graph deep-equal assertion.
    - `grep -c "it.todo" packages/workbench-ui/src/command/cc-reload.test.tsx` outputs 1.
    - File `packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json` exists and is valid JSON parsable by `node -e "JSON.parse(require('fs').readFileSync(process.argv[1],'utf8'))" packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json`.
    - The fixture contains exactly four top-level keys: `agents`, `tasks`, `gatewayCapacity`, `dispositions` (verifiable via `node -e "console.log(Object.keys(JSON.parse(require('fs').readFileSync('packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json','utf8'))).sort().join(','))"` outputs `agents,dispositions,gatewayCapacity,tasks`).
    - The fixture's `agents` array contains ≥3 AgentSummaryRow shapes (each with at minimum `name`, `namespace` populated).
    - The fixture's `tasks` array contains ≥6 TaskSummary entries with at least one `phase: 'Failed'`, one `phase: 'Completed'` with `artifactCount: 0` (artifact-debt trigger), one `phase: 'Failed'` with `podName` set (pod-failure trigger), one `phase: 'Failed'` with `error` containing `"verifier"` (verifier-failure trigger), one `phase: 'Failed'` with `error` containing `"policy"` (policy-denial trigger), one `phase: 'Dispatched'` with `childCount >= 2` (context-pressure heuristic trigger), and at least one terminal task (trace-gap trigger).
    - The fixture's `gatewayCapacity` array contains ≥2 rows, including at least one with `inFlight / currentCap >= 0.8` (gateway-saturation trigger; e.g., `{"inFlight": 8, "currentCap": 10}`).
    - The fixture's `dispositions` array contains ≥1 DispositionOverlayRow with `overBudget: true` (quota-wall trigger).
    - Every task `targetAgent` value (when present) corresponds to an existing entry in `agents` whose `namespace`+`name` match — i.e., the fixture has NO orphan task references (so the CC-01 canvas-side orphan assertion does not spuriously fire when cc-reload.test.tsx loads it in Wave 3).
    - `pnpm -C packages/workbench-ui test -- --run cc-orphan cc-reload` exits 0.
  </acceptance_criteria>
  <action>
    1. **Create `packages/workbench-ui/src/command/cc-orphan.test.ts`:**
       ```typescript
       /**
        * SPDX-License-Identifier: MIT
        * Copyright (c) 2026 Chris Knuteson
        */

       /**
        * Phase 2 / CC-01 — canvas-side orphan assertion test.
        *
        * Wave 0 scaffold: 3 it.todo placeholders. Wave 1 (02-02-PLAN.md)
        * fills in the bodies once the CC-01 assertion lands in
        * CommandView.tsx's agentNodes useMemo (per CONTEXT.md D-CC-01-A).
        *
        * Mirrors the orphan-throw pattern from source-binding.test.ts
        * Tests 2, 3, 8, 9 (vi.stubEnv 'NODE_ENV' beforeEach/afterEach;
        * `as unknown as <DTO>` casting for synthesized orphans).
        */

       import { afterEach, beforeEach, describe, it, vi } from 'vitest';

       describe('cc-orphan (CC-01 — canvas-side orphan assertion)', () => {
         beforeEach(() => { vi.stubEnv('NODE_ENV', 'development'); });
         afterEach(() => { vi.unstubAllEnvs(); });

         it.todo('canvas-side orphan assertion throws in dev when a task references an agent key not in snapshot.agents');
         it.todo('AgentPanel field-orphan: assertSourceField throws when capabilities is missing on a synthesized AgentSummaryRow');
         it.todo("NODE_ENV='production' makes both assertions no-ops (vi.stubEnv)");
       });
       ```

    2. **Create `packages/workbench-ui/src/command/cc-reload.test.tsx`:**
       ```tsx
       /**
        * SPDX-License-Identifier: MIT
        * Copyright (c) 2026 Chris Knuteson
        */

       /**
        * Phase 2 / CC-02 — reload-stability test.
        *
        * Wave 0 scaffold: 1 it.todo placeholder. Wave 3 (02-04-PLAN.md)
        * implements the full mount → unmount → remount → deep-equal
        * assertion against the captured cc-snapshot.json fixture, capturing
        * BOTH a DOM snapshot (panels + overlays) AND a scene-graph JSON
        * snapshot (computeLayout output, serialized via
        * Object.fromEntries(layout.agents)) per CONTEXT.md D-CC-02-A.
        */

       import { afterEach, beforeEach, describe, it, vi } from 'vitest';

       describe('cc-reload (CC-02 — reload-stable rendering)', () => {
         beforeEach(() => { vi.stubEnv('NODE_ENV', 'development'); });
         afterEach(() => { vi.unstubAllEnvs(); });

         it.todo('mount → unmount → remount with same fixture: DOM snapshot AND scene-graph snapshot are deep-equal');
       });
       ```

    3. **Create `packages/workbench-ui/src/command/__fixtures__/` directory** if it does not exist (`mkdir -p` is fine; the `__snapshots__/` sibling already exists).

    4. **Create `packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json`** — hand-crafted fixture per RESEARCH.md Finding 8 (no live workbench-api on homelab; CLAUDE.md GitOps-only). Use the exact DTO shapes from `packages/workbench-ui/src/types.ts`. The fixture MUST cover the 9 pressure-type triggers AND avoid orphan task→agent references. Recommended structure (the executor may adjust IDs/names but MUST keep the trigger conditions intact):

       ```json
       {
         "agents": [
           {"name": "researcher-01", "namespace": "kagent-system", "model": "workers-ai/@cf/meta/llama-4-scout-17b-16e-instruct", "modelClass": "tool-caller-default", "tools": ["http", "mcp"], "capabilities": ["research", "summarize"]},
           {"name": "curator-02", "namespace": "kagent-system", "model": "workers-ai/@cf/meta/llama-4-scout-17b-16e-instruct", "tools": ["http"], "capabilities": ["curate"]},
           {"name": "executor-03", "namespace": "kagent-system", "modelClass": "tool-caller-fast", "capabilities": ["execute"]}
         ],
         "tasks": [
           {"name": "research-001", "namespace": "kagent-system", "uid": "u-001", "phase": "Completed", "targetAgent": "researcher-01", "model": "workers-ai/@cf/meta/llama-4-scout-17b-16e-instruct", "createdAt": "2026-05-10T10:00:00Z", "startedAt": "2026-05-10T10:00:30Z", "completedAt": "2026-05-10T10:01:30Z", "podName": "research-001-pod", "artifactCount": 2, "childCount": 0},
           {"name": "research-002", "namespace": "kagent-system", "uid": "u-002", "phase": "Completed", "targetAgent": "curator-02", "createdAt": "2026-05-10T10:02:00Z", "completedAt": "2026-05-10T10:02:45Z", "artifactCount": 0, "childCount": 0},
           {"name": "research-003", "namespace": "kagent-system", "uid": "u-003", "phase": "Failed", "targetAgent": "researcher-01", "podName": "research-003-pod", "error": "verifier check failed: structural mismatch in artifact 0", "createdAt": "2026-05-10T10:03:00Z", "startedAt": "2026-05-10T10:03:15Z", "completedAt": "2026-05-10T10:03:50Z", "artifactCount": 1, "childCount": 0},
           {"name": "research-004", "namespace": "kagent-system", "uid": "u-004", "phase": "Failed", "targetAgent": "curator-02", "podName": "research-004-pod", "error": "policy denial: capability 'execute' not in scope for templates", "createdAt": "2026-05-10T10:04:00Z", "startedAt": "2026-05-10T10:04:05Z", "completedAt": "2026-05-10T10:04:10Z", "artifactCount": 0, "childCount": 0},
           {"name": "fanout-005", "namespace": "kagent-system", "uid": "u-005", "phase": "Dispatched", "targetAgent": "executor-03", "createdAt": "2026-05-10T10:05:00Z", "startedAt": "2026-05-10T10:05:02Z", "childCount": 3, "artifactCount": 0, "suspicious": ["high-fanout"]},
           {"name": "research-006", "namespace": "kagent-system", "uid": "u-006", "phase": "Pending", "targetAgent": "researcher-01", "createdAt": "2026-05-10T10:06:00Z", "artifactCount": 0, "childCount": 0}
         ],
         "gatewayCapacity": [
           {"model": "workers-ai/@cf/meta/llama-4-scout-17b-16e-instruct", "endpoint": "https://gateway.ai.cloudflare.com/v1/.../workers-ai", "backendKind": "cloudflare-workers-ai", "inFlight": 8, "currentCap": 10, "seed": 8, "max": 16, "minSafe": 4, "recentP50Ms": 420, "crName": "cf-llama-4-scout", "crNamespace": "kagent-system"},
           {"model": "workers-ai/@cf/meta/llama-3.3-70b-instruct-fp8-fast", "endpoint": "https://gateway.ai.cloudflare.com/v1/.../workers-ai-fp8", "backendKind": "cloudflare-workers-ai", "inFlight": 1, "currentCap": 8, "seed": 4, "max": 12, "minSafe": 2, "recentP50Ms": null, "crName": "cf-llama-3-3-70b", "crNamespace": "kagent-system"}
         ],
         "dispositions": [
           {
             "agentRef": "kagent-system/researcher-01",
             "namespace": "kagent-system",
             "agentName": "researcher-01",
             "configMapName": "researcher-01-disposition",
             "idleBehavior": {
               "readChannels": ["kagent-system/research-channel"],
               "attentionBudget": {"tokensPerDay": 50000, "pollIntervalSeconds": 300},
               "proposalScope": {"mayProposeAgainst": ["templates"], "maxProposalsPerDay": 3}
             },
             "spentTokensToday": 55000,
             "postsToday": 0,
             "proposalsToday": 1,
             "overBudget": true,
             "overBudgetReason": "tokens_exceeded",
             "overBudgetEventCountToday": 2,
             "dailyBoundaryUtc": "2026-05-10T00:00:00.000Z"
           }
         ]
       }
       ```

       Trigger coverage of the 9 pressure types (executor verifies after writing):
       - **gateway** — gateway row 1: `inFlight=8 / currentCap=10 = 0.8 ≥ 0.8` ✓
       - **artifact** — task `research-002`: `phase=Completed && artifactCount=0` ✓
       - **pod** — task `research-003`: `phase=Failed && podName='research-003-pod'` ✓
       - **quota** — disposition row 1: `overBudget=true` ✓
       - **telemetry** — fires UI-side from `lastEventAt` staleness (NOT in fixture; the test stubs `Date.now()` to simulate stale) — no fixture data needed
       - **context** — task `fanout-005`: `phase=Dispatched && childCount=3 ≥ 2` ✓
       - **verifier** — task `research-003`: `phase=Failed && error.includes('verifier')` ✓
       - **trace** — terminal tasks `research-001`/`research-002`/`research-003`/`research-004` are `Completed`/`Failed` (terminal phase) ✓
       - **policy** — task `research-004`: `phase=Failed && error.includes('policy')` ✓

       **Orphan-free verification:** every task with `targetAgent !== undefined` references one of `researcher-01`, `curator-02`, or `executor-03`, and every one of those agents exists in the `agents` array under namespace `kagent-system`. Confirmed by inspection of the fixture above.

    5. **Run** `pnpm -C packages/workbench-ui test -- --run cc-orphan cc-reload` and confirm 0 failures (it.todo placeholders count as todos, not failures).

    6. **Validate JSON:** `node -e "JSON.parse(require('fs').readFileSync('packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json','utf8'))"` exits 0.

  </action>
  <verify>
    <automated>pnpm -C packages/workbench-ui test -- --run cc-orphan cc-reload && node -e "const f=JSON.parse(require('fs').readFileSync('packages/workbench-ui/src/command/__fixtures__/cc-snapshot.json','utf8')); if(!(f.agents.length>=3 && f.tasks.length>=6 && f.gatewayCapacity.length>=2 && f.dispositions.length>=1)) {console.error('coverage fail');process.exit(1)}"</automated>
  </verify>
  <done>
    - The three new files exist at the documented paths.
    - Fixture is parseable JSON with the four required top-level keys.
    - Fixture has ≥3 agents, ≥6 tasks, ≥2 gateway rows, ≥1 over-budget disposition.
    - All 9 pressure triggers have at least one source row in the fixture (or in the case of telemetry, are documented as test-stubbed).
    - vitest exits 0; the it.todo placeholders register as todo, not failures.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                                                                                                 |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Workbench-api → Workbench-ui (read) | UI consumes `/api/agents`, `/api/tasks`, `/api/gateway/capacity`, `/api/dispositions` JSON; SSE `/api/stream` for cache invalidation. Phase 2 adds NO new boundary (no new endpoint, no new write surface). |
| Build-time env → runtime config     | `NODE_ENV` and `VITE_PRESSURE_DRAMATIZATION` are build-time inputs that gate dev assertions and visual treatment.                                                                                           |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                          |
| --------- | ---------------------- | ------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering              | `cc-snapshot.json` fixture            | accept      | Committed to git as a test fixture; only consumed by vitest in jsdom. Not loaded at runtime in production. No XSS/parsing exposure beyond what jsdom + JSON.parse already do.                                                                            |
| T-02-02   | Information Disclosure | `data-source-field` DOM attributes    | accept      | The attribute values are DTO field NAMES (not values). Field names are not secrets — they appear in the OpenAPI/types.ts files committed to the public repo. The Phase 1 disposition slice already exposes them. No new disclosure.                      |
| T-02-03   | Denial of Service      | `pressure.classify()` per-render cost | mitigate    | Wave 1 wraps `classify()` calls in `useMemo([snapshot])` so they run only when snapshot Maps change, not per-frame. Wave 0 scaffold uses an empty PRESSURE_TYPES array (zero work). RESEARCH.md Pitfall 2 documents this.                                |
| T-02-04   | Elevation of Privilege | dev-only assertion `throw`            | accept      | The orphan assertion only THROWS in dev (`isDevBuild()===true`); in prod it's a no-op. NODE_ENV=production override is tested. Tests stub via `vi.stubEnv('NODE_ENV', 'production')`. The throw is a contract-violation signal, not a security boundary. |

Phase 2 introduces NO new authentication, authorization, or input-handling code. The four threats above cover the only surfaces this Wave-0 plan touches; all are either accepted as low-risk (T-01, T-02, T-04) or mitigated by the documented `useMemo` wrapping (T-03 — Wave 1 follow-through, scaffold is zero-cost).
</threat_model>

<verification>
- `pnpm -C packages/workbench-ui test -- --run` exits 0 across the whole package.
- `pnpm -C packages/workbench-ui exec tsc --noEmit -p tsconfig.build.json` exits 0.
- All 8 new files exist at the documented paths under `packages/workbench-ui/src/command/`.
- `source-binding.ts` exports the four new closed-enum types AND keeps the existing DispositionFieldName + helpers (existing Tests 1-10 still green).
- `__fixtures__/cc-snapshot.json` is valid JSON with the four required top-level keys and the per-pressure-type trigger rows documented in Task 3.
- Existing Phase 1 tests (DispositionOverlay.test.tsx 12 tests, source-binding.test.ts 10 tests, state.test.ts) all still pass — no behavioral regression introduced by the generics widening.
</verification>

<success_criteria>

- All three tasks pass `pnpm -C packages/workbench-ui test -- --run` AND `pnpm -C packages/workbench-ui exec tsc --noEmit`.
- The Wave 0 gate flips from `❌ W0` to `✅ W0` for every row in 02-VALIDATION.md's per-task verification map.
- Wave 1 (02-02-PLAN.md) can begin without any further scaffolding work — every file it needs to import from already exists with the right signature.
- No existing test in `packages/workbench-ui/` regresses.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-command-center-contract-hardening/02-01-SUMMARY.md` per the GSD summary template.
</output>
