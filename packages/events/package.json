{
  "name": "@kagent/events",
  "version": "0.0.0",
  "private": true,
  "description": "Wave 3 Events sub-team — typed CloudEvents-shaped pub/sub on NATS JetStream + cap-gated topic ACLs + event-trigger AgentTask minting (per docs/SUBSTRATE-V1.md §3.7 + WAVES.md §5.1).",
  "license": "MIT",
  "author": "Chris Knuteson <chris@ctkadvisors.net>",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "build": "tsc -p .",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@kagent/capability-types": "workspace:*",
    "nats": "2.29.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
