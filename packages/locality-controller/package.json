{
  "name": "@kagent/locality-controller",
  "version": "0.0.0",
  "private": true,
  "description": "Wave 3 / Locality sub-team — Workspace-derived NodeAffinity, speculative execution, and pod-pressure circuit breaker. Pure helpers consumed by the operator reconciler + admission gate.",
  "license": "MIT",
  "author": "Chris Knuteson <chris@ctkadvisors.net>",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "build": "tsc -p .",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@kubernetes/client-node": "1.4.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
