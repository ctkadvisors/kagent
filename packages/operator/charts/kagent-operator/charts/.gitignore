# Helm dependency-build packages the sub-charts into .tgz files +
# generates a Chart.lock. The unpacked sub-chart directory IS source
# (we ship it from this repo); the .tgz + Chart.lock are build outputs
# regenerated by `helm dependency build`. Don't track them.
*.tgz
