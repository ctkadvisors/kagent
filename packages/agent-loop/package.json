{
  "name": "@kagent/agent-loop",
  "version": "0.0.0",
  "private": true,
  "description": "In-pod agent execution loop — forked AgentExecutor + RunBudget + run-end detector middleware.",
  "license": "MIT",
  "author": "Chris Knuteson <chris@ctkadvisors.net>",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
