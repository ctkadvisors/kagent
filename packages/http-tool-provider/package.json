{
  "name": "@kagent/http-tool-provider",
  "version": "0.0.0",
  "private": true,
  "description": "HTTP REST ToolProvider — calls arbitrary endpoints with configurable auth + path templating; surfaces non-2xx as ToolResult{isError:true} not raw throws.",
  "license": "MIT",
  "author": "Chris Knuteson <chris@ctkadvisors.net>",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests"
  },
  "dependencies": {
    "@kagent/agent-loop": "workspace:*"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
