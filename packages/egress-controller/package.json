{
  "name": "@kagent/egress-controller",
  "version": "0.0.0",
  "private": true,
  "description": "Wave 4 / Egress sub-team — per-Agent NetworkPolicy / CiliumNetworkPolicy generation from Agent.spec.egress + Tenant.spec.defaultEgress fallback. Pure-functional policy builders + tenant-default resolver consumed by the operator reconciler.",
  "license": "MIT",
  "author": "Chris Knuteson <chris@ctkadvisors.net>",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "build": "tsc -p .",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@kubernetes/client-node": "1.4.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
