{
  "name": "@kagent/mcp-tool-provider",
  "version": "0.0.0",
  "private": true,
  "description": "MCP stdio ToolProvider — wraps @modelcontextprotocol/sdk to spawn MCP servers as subprocesses; tools/list discovery + tools/call invocation with structured error envelope + AbortSignal plumbing.",
  "license": "MIT",
  "author": "Chris Knuteson <cknuteson@gmail.com>",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests"
  },
  "dependencies": {
    "@kagent/agent-loop": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "tsx": "4.21.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
