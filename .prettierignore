dist/
coverage/
pnpm-lock.yaml
.tsbuildinfo
*.tsbuildinfo
node_modules/

# Subagent worktrees live here — they're git-worktree branches, not part
# of the main worktree's checked-in tree. Prettier walks the filesystem
# regardless and would otherwise lint stale or experimental copies.
.claude/

# Phase 0 docs are locked spec — exempt from prettier churn.
# Re-enable selectively if a doc is intentionally reopened.
docs/
README.md
CLAUDE.md
LICENSE

# Audit + RC evidence packs are locked artifacts — markdown reports
# generated during reviews and JSON/markdown captured by the workbench
# evidence collector. Prettier reformatting churns column-aligned tables
# and breaks reproducibility against captured snapshots. Same treatment
# as docs/.
evidence/

# Helm templates — Go templating syntax ({{- ... -}}) is not valid YAML
# and prettier's YAML parser fails on it. The chart README + values.yaml
# remain prettier-managed; only templates/ is exempt.
packages/operator/charts/*/templates/
packages/operator/charts/*/templates/**
# Wave 3 v0.4.3-identity — embedded SPIRE sub-chart templates also use
# Helm templating; same prettier-incompat as the parent's templates/.
packages/operator/charts/*/charts/*/templates/
packages/operator/charts/*/charts/*/templates/**
packages/llm-gateway/charts/*/templates/
packages/llm-gateway/charts/*/templates/**

# Vendored Helm sub-chart tarballs + Chart.lock — managed by `helm
# dependency update`, not by hand. Don't reformat the locked digests.
packages/llm-gateway/charts/*/charts/
packages/llm-gateway/charts/*/Chart.lock
packages/operator/charts/*/Chart.lock
