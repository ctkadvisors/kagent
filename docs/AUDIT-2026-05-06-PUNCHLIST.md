# Audit Punchlist — kagent (2026-05-06)

Companion to [`AUDIT-2026-05-06.md`](./AUDIT-2026-05-06.md). Each row maps to a specific claim in the project's docs and a concrete code or strategic gap. Sort: severity, then category.

**Schema:** severity / category / claim / finding / evidence / smallest fix.

---

## BLOCKER (do not ship to external pilots without these)

| # | severity | category | claim | finding | evidence | recommendation |
|---|---|---|---|---|---|---|
| B1 | BLOCKER | code | "JWT capability bundle, narrows on spawn — sealed at substrate level" (`SUBSTRATE-V1.md` §3.6) | `definePublishEvent` falls back to a SELF-MINTED capability bundle when no JWT is mounted, bypassing JWKS verification entirely for the publish path. Defense-in-depth false confidence. | `packages/agent-pod/src/main.ts:434-451` (synthetic bundle from `Agent.spec.capabilityClaims.publish`); `builtin-tools-publish.ts:138-148` (cap check trusts the bundle). | Delete the synthetic-bundle fallback. Refuse `publish_event` with `policy_denied:no_capability` when no JWT is mounted. |
| B2 | BLOCKER | code | "Capability mint per AgentTask is operator-side; verified at agent-pod boot" (`SUBSTRATE-V1.md` §3.6, `WAVES.md` §4.1) | Capability bundle is fail-OPEN by design — JWT mount absence (e.g., upgrade error, missing chart value, deleted Secret) silently disables every cap-gated guardrail with only a log line. | `packages/agent-pod/src/cap-consumer.ts:148-167` (`loadCapabilityOptional` returns `undefined` on ENOENT); `main.ts:117-125` (calls `loadCapabilityOptional`); downstream guards strip cap context silently. | Gate fail-open behind `agentPod.capability.allowMissing: false` (default); refuse to boot if `KAGENT_CAP_JWT_FILE` was stamped on env but file is missing. |
| B3 | BLOCKER | code | "Verifier `verify_completion` is a substrate-mediated post-completion quality gate" (`SUBSTRATE-V1.md` §3.2, `verifier.ts:967-973`) | Verifier Jobs share `managed-by=kagent-operator` + `kagent.knuteson.io/task=<parent>` labels with dispatch Jobs; `job-watch.ts` and `failure-detector.ts` never consult the `VERIFIER_JOB_LABEL` discriminator. Failed verifier Jobs (i.e., on `verdict:fail`) append `JobFailedAfterComplete` conditions to the parent AgentTask forever. | `packages/operator/src/verifier.ts:721-725,744-746,124` vs `packages/operator/src/job-watch.ts:34-35`. | In `job-watch.ts`, drop events whose `metadata.labels[VERIFIER_JOB_LABEL] === 'true'` BEFORE calling `parentTaskRef`. One-line guard. Add regression test. |
| B4 | BLOCKER | doc/code | "Cluster operators can swap a model class via one chart-value edit, with kubectl-friendly hot-reload — same pattern the audit-stream config uses" (`docs/MODEL-ROUTING.md §4`) | Model-class map is a one-shot env-JSON parse at boot from `KAGENT_AGENT_MODEL_CLASSES_JSON`. No ConfigMap watch, no SIGHUP, no chart-side `configmap-model-classes.yaml` template. Updates require operator pod restart. | `packages/operator/src/main.ts:194-223,507`; chart `templates/deployment.yaml:268-269`; absence of `configmap-model-classes.yaml`. | Either ship the ConfigMap-watch path (1 day) OR update `MODEL-ROUTING.md §4` to delete the hot-reload claim and document Helm rollout (5 minutes). |
| B5 | BLOCKER | code | "Workbench surfaces gateway capacity tuning safely" (`docs/WORKBENCH.md` §5; gateway routes documented in chart) | PATCH `/api/modelendpoints` admits `minSafe: 0`. Combined with AIMD halving on a single 5xx, `cap` collapses to 0 permanently — the model is 429'd until operator restart or hand-edit. Authenticated-user → cluster-wide LLM outage in one PATCH. | `packages/workbench-api/src/routes/gateway.ts:90` (`MIN_SAFE_MIN = 0`); `packages/llm-gateway/src/aimd.ts:139,157` (clamp at minSafe); `router.ts:129` (cap-check). | Raise `MIN_SAFE_MIN` to 1. |
| B6 | BLOCKER | auth | "fail-closed header-trust auth (`X-Forwarded-User`) per AGENT-SELF-SERVICE.md §3.5" | Default chart install ships `ingress.authMiddleware=''` AND `networkPolicy.ingressFrom=[]`; chart README acknowledges flannel CNI (K3s default) does NOT enforce NetworkPolicies. Any in-cluster pod can `curl -H 'X-Forwarded-User: anyone' workbench-api/api/tasks` and create AgentTasks. | `packages/workbench-api/src/auth.ts:75-106`; `packages/operator/charts/kagent-workbench/values.yaml:265,267`; `templates/networkpolicy.yaml:48-60`; `templates/ingress.yaml:35,66-98` (no auth chained on plain Ingress path). | Make chart `fail` Helm install if `ingress.authMiddleware` is empty AND `WORKBENCH_AUTH_REQUIRED != "false"` AND no `acknowledgeUnauthenticated: true` is set. |
| B7 | BLOCKER | secret | "v0.1.8-secret-hygiene — no plaintext API keys in spawned-Job env" (`docs/ROADMAP.md` Wave 0; `WAVES.md` §2.1) | Bundled-Postgres Secret writes the DSN with cleartext password under `stringData.dsn`, with `sslmode=disable`. Anyone with `get secret` RBAC in the release namespace reads the cleartext. Helm's `lookup` round-trip leaks via `helm get manifest`. | `packages/llm-gateway/charts/llm-gateway/templates/secret-bundled.yaml:33-43`. | Move to `sslmode=verify-full`; mount user/password separately via `secretKeyRef`; document Sealed-Secrets / external-secrets as the production path. |

---

## HIGH (fix before any external announcement)

| # | severity | category | claim | finding | evidence | recommendation |
|---|---|---|---|---|---|---|
| H1 | HIGH | strategic | "kagent is the only OSS K3s-native agent framework" (`README.md`, `docs/WHY.md` §1, `docs/DESIGN-V0.1.md` §1) | False as of 2026-05-06. `kubernetes-sigs/agent-sandbox` (2,057★, May 2026, Apache-2.0) ships per-agent isolation as a Kubernetes SIG project with explicit Kata/RuntimeClass support. Solo.io kagent.dev (2,689★, CNCF Sandbox) ships `Agent` + `SandboxAgent` CRDs that delegate to agent-sandbox. Argo Events on NATS + Red Hat Kagenti SPIFFE pattern compose 4-of-7 substrate primitives in OSS today. | https://github.com/kubernetes-sigs/agent-sandbox; https://kubernetes.io/blog/2026/03/20/running-agents-on-kubernetes-with-agent-sandbox/; https://github.com/kagent-dev/kagent/blob/main/go/api/v1alpha2/sandboxagent_types.go. | Narrow the claim to the capability-sealed-JWT-narrowing primitive (the one nobody else ships). Rewrite README first paragraph and `docs/WHY.md` §1 accordingly. Pre-public-release rename per `CLAUDE.md` "naming note" is now urgent, not eventually. |
| H2 | HIGH | strategic | "Composable — any framework runs in a pod" (`README.md`, `docs/PRIOR-ART.md`) | TRUE-WITH-CAVEATS. Vercel AI SDK and Strands TS drop in cleanly (~100 LOC adapter); Claude Agent SDK does NOT (Anthropic Commercial ToS + cannot route through LiteLLM + ships as a CLI subprocess). All third-party frameworks need per-framework `canUseTool` wrappers because no framework other than Claude Agent SDK has the hook. Trace span shape diverges across frameworks. | R3 audit report (in `AUDIT-2026-05-06.md`); `@kagent/agent-loop` ~3,000 LOC reproduces primitives Vercel AI SDK gives free. | (a) Document the friction explicitly in `docs/DROP-IN.md`. (b) Ship a `@kagent/agent-loop-vercel-ai` reference adapter as Phase-2 proof. (c) Demote `@kagent/agent-loop` to `@kagent/agent-loop-default`. |
| H3 | HIGH | strategic | "v0.1 ships when the comparison-rig in Phase 5 proves no-regression vs the prior `homelab-orchestrator` baseline" (`README.md`, `docs/WHY.md` §6, `docs/ROADMAP.md`) | The pre-committed falsifiable success criterion has not been measured. `docs/V0.1-COMPARISON.md` does not exist. `evidence/` has `rc1/` and a `demo-success.png`. Tags reach `v0.5.4-keyrotation`, but the "v0.1 ships when…" gate is unfulfilled. | `ls docs/` confirms absence; `ls evidence/` shows demo-success.png + rc1/ only; `git tag --sort=-creatordate` shows v0.5.x tags ahead of an unshipped v0.1. | Either run the comparison rig and publish numbers, OR retire the falsifiable-success framing in `WHY.md §6` and replace with the substrate-completeness milestone. |
| H4 | HIGH | strategic | "kagent's Event primitive is the loose-coordination wire" (`SUBSTRATE-V1.md` §3.7, `docs/PRIOR-ART.md`) | A2A protocol v1.2 is GA, governed by the Linux Foundation, in production at 150+ orgs (Vertex / AgentCore / Foundry); kagent's substrate docs do not mention A2A. kagent agents cannot trade tasks with agents on Vertex/Foundry/AgentCore without a custom adapter. | https://a2a-protocol.org/latest/specification/; absence of A2A references in `PRIOR-ART.md`, `SUBSTRATE-V1.md`. | Add A2A to `PRIOR-ART.md` §1 immediately. Plan a `v0.4.x-a2a` slot to make kagent's NATS Event subjects speak A2A on the wire (or expose an A2A bridge). |
| H5 | HIGH | code | "Test discipline: ≥85% coverage on the operator reconciler, ≥75% on glue code" (`CLAUDE.md`) | `vitest.config.ts:16-21` declares `coverage.thresholds = { lines: 0, functions: 0, branches: 0, statements: 0 }`. Promise unenforced. Real coverage is high in practice (17K LOC tests vs 24K LOC source) but a future regression lands silently. | `packages/operator/vitest.config.ts:16-21`; same pattern in `packages/agent-pod/vitest.config.ts`. | Set non-zero thresholds in CI (e.g., `lines: 80`, `branches: 70`) and tighten over time. |
| H6 | HIGH | test | "Operator reconciler is well-tested for race conditions" (implicit in test surface size) | Zero direct tests for `watch.ts` or `job-watch.ts`. The 6 informer error→restart sites all use `setTimeout(() => void informer.start(), 5000)` — the `void` discards rejection from `start()`. K3s control-plane restart >5s could leave the operator with a dead informer until pod restart. | `find packages/operator/src -name "watch.test.ts" \| -name "job-watch.test.ts"` returns nothing. Restart sites: `watch.ts:112-117`, `job-watch.ts:122-127,135-140`, `main.ts:771-776,781-786,792-797,2740-2745,3096-3101`. | Wrap restarts in `safeRestart(informer)` with backoff + cap; add `watch.test.ts` covering error→restart against a stub apiserver that fails the first retry. |
| H7 | HIGH | code | "Tools allowlist enforced at boot — unknown names fail FAST" (`docs/ROADMAP.md` §5 line 103) | The boot-time check only validates names IN `Agent.spec.tools`. The runner appends substrate tools (`spawn_child_task`, `publish_event`, blackboard, etc.) UNCONDITIONALLY when env-flags fire — the LLM can call tools not in `Agent.spec.tools` and the call dispatches successfully. | `packages/agent-pod/src/runner.ts:531-565` (resolveToolProviders); `packages/agent-loop/src/executor.ts:708` (global providerFor lookup). | In `runner.ts:resolveToolProviders`, after appending substrate providers, assert every exposed tool name is also in `config.agentSpec.tools` (or in a documented "implicit-when-X" set). |
| H8 | HIGH | code | "Status writeback is the canonical signal of task outcome" (`SUBSTRATE-V1.md` §3.2) | Status writer uses `merge-patch+json` with no `resourceVersion` precondition. Last-writer-wins races between agent-pod and operator job-watch can clobber `Completed` with `Failed`. | `packages/agent-pod/src/status.ts:30,131-141`. | Build the patch as JSON Patch with a `test` op asserting current `status.phase` is `Pending` or `Dispatched` (refuse to overwrite a terminal phase). |
| H9 | HIGH | code | "429-retry honors `Retry-After`; bounded; doesn't starve the loop" (`docs/RESILIENT-CONTRACTS.md` §2(b); commit `c74ab16`) | `Retry-After` is honored verbatim with no clamp. Gateway returning `Retry-After: 600` sleeps the loop 10 minutes; outer `runConfig.timeoutSeconds` AbortSignal fires mid-sleep but `chatWithRetry`'s `await this.sleep` is NOT abort-aware. Pod gets SIGKILLed by kubelet before any status patch. | `packages/agent-loop/src/executor.ts:296-302,386`; `packages/agent-pod/src/builtin-tools-wait.ts:216-241,311-315`. | Replace `await this.sleep(backoffMs)` with abort-aware sleep that races `setTimeout` against `llmCtx.abortSignal`'s `'abort'`; cap `Retry-After` at `Math.min(retryAfterSec * 1000, 30_000)`. |
| H10 | HIGH | code | "wait_for_child_task uses K8s API efficiently" (implicit in WS-L design, `AGENT-SELF-SERVICE.md` §5) | `getTaskByUid` does unbounded `listNamespacedCustomObject` with no fieldSelector then linear-scans for `metadata.uid`. Default polling = 300 LIST calls per parent waiter, each returning every AgentTask in the namespace. In a busy cluster, apiserver throttle + parent budget evaporation. | `packages/agent-pod/src/k8s-task-creator.ts:296-314`. | Use `listLiveChildren(parent)` first to narrow by `kagent.knuteson.io/parent-task-uid=<uid>` label, OR `getNamespacedCustomObject` with the child's *name* (which `spawn_child_task` returns). |
| H11 | HIGH | code | "JWKS endpoint is the trust root for capability JWT verification" (`SUBSTRATE-V1.md` §3.6) | JWKS fetch at boot has no retry / timeout / cache; default `defaultFetchJwks` is a single `fetch()`. Operator template-server flap during pod boot = pod CrashLoopBackoff with no `Failed` status patch. AgentTask sits in `Dispatched` forever. | `packages/agent-pod/src/cap-consumer.ts:121-134,196-210`. | Add 10s timeout + 3-attempt exponential retry around `defaultFetchJwks`; on terminal failure write a `Failed` status patch with `error: jwks_unreachable: <detail>` BEFORE exiting. |
| H12 | HIGH | code | (env-JSON migration to ConfigMap is shipped per `WAVES.md` §3.1 / `v0.2.0-typed-io`) | `parentDistillation` env-JSON path has no size validation. Single oversized field → `argument list too long` on Pod start → AgentTask gets generic `Failed` with no usable `error:` reason. | `packages/agent-pod/src/env.ts:324-332`; combined with env-JSON fallback at `:466-491`. | Refuse the parse with explicit size cap (`> 32_768` bytes); gate env-JSON fallback behind hard 256 KiB cap on combined `KAGENT_AGENT_SPEC + KAGENT_TASK_SPEC` payload at `job-spec.ts:666-671`. |
| H13 | HIGH | dos | "GATEWAY-CONTRACT.md §7 — gateway responds 429 with Retry-After on backpressure" | Gateway never propagates upstream backend `Retry-After` to the agent-pod. Real 429s from OpenAI/Anthropic/Cloudflare land as `kind: 'dispatch_error'` (HTTP 502 with no Retry-After), so agent-pod retries IMMEDIATELY and stampedes the upstream. | `packages/llm-gateway/src/server.ts:289` (Retry-After only on at_cap); providers throw plain Error; `router.ts:188-214` converts to dispatch_error. | Wrap provider errors in typed `BackendError { status, retryAfter }`; have `router.ts` propagate Retry-After through new `kind: 'backend_throttled'` discriminator. |
| H14 | HIGH | crypto | "Admin-token comparison uses constant-time `timingSafeEqual`" (admin-routes.ts comment) | Length-mismatch early-return BEFORE `timingSafeEqual` leaks `ADMIN_API_TOKEN` length. Combined with `randAlphaNum 32` chart recommendation, attacker binary-searches lengths. | `packages/llm-gateway/src/admin-routes.ts:38-40,43`. | Compare HMAC-SHA256 digests of both sides for constant-length. |
| H15 | HIGH | secret | "Gateway error handling does not echo upstream content into audit records" (implicit in PII story, GATEWAY-CONTRACT.md §8) | Provider error responses propagate upstream response bodies VERBATIM into `usage_records.error_message`, surfaced via `/admin/usage`. Misbehaving backend echoing prompt → PII exposure to anyone with admin token. | `packages/llm-gateway/src/providers/openai-compat-provider.ts:74-75`; `router.ts:189,204`. | Truncate to first 256 chars + regex-scrub `sk-[A-Za-z0-9_-]+` before throw + before recording. |
| H16 | HIGH | input | "POST /api/tasks validates input shape" (`AGENT-SELF-SERVICE.md` §3.3) | `payload` field is opaque `unknown` with no size cap or shape check. 100 AgentTasks × 1 MB `payload` = 100 MB written to etcd. | `packages/workbench-api/src/routes/validators.ts:197`; `tasks.ts:212`. | Add `MAX_PAYLOAD_BYTES = 65_536` check after `JSON.stringify(payload)` in `validators.ts`. |
| H17 | HIGH | rbac | "Workbench actions are scoped to the operator's release namespace" (implicit in WORKBENCH.md design) | Workbench actions ClusterRole grants cluster-scoped `agenttasks: [create]`. Combined with B6 spoof, any caller can create AgentTasks in ANY namespace by setting `req.namespace`. | `packages/operator/charts/kagent-workbench/templates/clusterrole-actions.yaml:33`; `workbench-api/src/routes/tasks.ts:180`. | Scope to `Role` in `.Release.Namespace`; reject requests in `tasks.ts:180` whose `namespace !== defaultNamespace` until per-user namespace ACL exists. |
| H18 | HIGH | auth | "API key rotation policy can revoke stale keys" (admin-routes.ts) | `last_used_at` TOUCH path documented but never invoked — every request bypasses last_used_at tracking. Audit posture broken. | `packages/llm-gateway/src/db/api-keys.ts:138-140`; `auth.ts:46-76` (no touchLastUsed call); `server.ts:229`. | Call `apiKeyRepo.touchLastUsed(auth.keyHash)` fire-and-forget after successful authenticate. |
| H19 | HIGH | auth | "Conformance harness verifies traceparent, X-Kagent-* headers, 429+Retry-After, key rotation, mTLS fallback" (`docs/GATEWAY-CONTRACT.md` §14) | `evaluateMtlsSvidFallback` is mocked-only — reads result from an INPUT struct, not a live probe. Harness verifies the CLI invoker's claims, not the gateway. | `packages/llm-gateway/src/conformance.ts:116-153`; `conformance-cli.ts:17-21`. | Add a real probe (open TLS connection with SPIRE workload-API socket as cert source); OR rename `evaluateMtlsSvidFallback` → `recordMtlsSvidExpectation` and flag result as `source: declared`. |
| H20 | HIGH | crypto | "Operator CA signs caps with ES256 or RS256" (`packages/operator/src/cap-ca.ts`, `WAVES.md` §4.1) | `detectAlgFromPem` distinguishes ES256 vs RS256 via length heuristic on base64 body — not via ASN.1 parse. A malformed PEM dropped into the mounted Secret (sloppy chart upgrade) could trigger alg-confusion at sign time. | `packages/operator/src/cap-ca.ts:349-357`. | Require explicit `KAGENT_CAP_SIGNING_ALG` env when `KAGENT_IDENTITY_ENABLED=true` AND a SPIRE-managed key file is present; fail closed when unset. |

---

## MEDIUM (fix as time permits / before scaling pilot)

| # | severity | category | claim | finding | evidence | recommendation |
|---|---|---|---|---|---|---|
| M1 | MEDIUM | doc/code | "AgentWorkflow + supervision route child failures via strategy" (`SUBSTRATE-V1.md` §4.2) | Supervision currently only records intent for `restart` actions; `Agent.spec.supervisionStrategy = 'one_for_one'` does almost nothing. Documented honestly in source comments but external consumers miss it. | `packages/operator/src/supervision-router.ts:34-44,520-525`. | Add explicit warning in `docs/WAVES.md §6.3` and Agent CRD JSDoc that `restart` actions only record intent; consumer is responsible for re-issuing. |
| M2 | MEDIUM | code | "Supervision routes are O(1) — informer-cache backed" (file comment) | `fetchParentTask` and `fetchTaskByUid` LIST every supervision event despite the comment saying "informer-cache caveat." `else if (deps.listChildrenForParent !== undefined)` branch does nothing. | `packages/operator/src/supervision-router.ts:412-441,545-575`. | Add `getTaskByUid?: (uid: string) => AgentTask \| undefined` to `SupervisionRouterDeps`; wire from main.ts (the same closure already feeds `reconcileParentFromChildEvent`). |
| M3 | MEDIUM | code | "Verifier dispatches scriptRef as one-shot Job with bounded budget" (`SUBSTRATE-V1.md §3.2`) | Poll loop has no exponential backoff — 500ms ticks for full timeout. Default 60s × 500ms = 130 reads against `batchApi.readNamespacedJob` per verifier run. | `packages/operator/src/verifier.ts:620-651`. | Switch to Job-watch-based completion signal (operator already has `jobPodInformer`), or add exponential backoff (500ms → 1s → 2s capped at 5s). |
| M4 | MEDIUM | code | "Idempotency dedupe is operator-local, 24h TTL" (typed-io / `WAVES.md §3.1`) | `IdempotencyCache` is process-local; operator restart loses the entire dedupe table. First reconcile after restart sees `'miss'` for every task. Real risk of double-dispatch on tasks that crashed mid-Pending. | `packages/operator/src/main.ts:1390`; `applyIdempotencyDedupe`. | Either document restart-loss explicitly, OR seed the cache from the AgentTask informer's first sync (walk Completed tasks, derive idempotency key, `recordOutputs`). |
| M5 | MEDIUM | code | (chart values are validated at install-time) | `parseModelClassesEnv` throws on malformed JSON → operator CrashLoop. Chart writes JSON via `toJson | quote` — Helm-side typo bricks operator with no apiserver-side validation. Argo reports "Healthy" until the next sync notices. | `packages/operator/src/main.ts:200-210,507`; chart `_helpers.tpl` lacks validation. | Replace `toJson` with `mustToJson` in chart so failure surfaces at `helm install` instead of pod-boot. |
| M6 | MEDIUM | code | "spawn_child_task is fail-closed at the agent-pod" (`AGENT-SELF-SERVICE.md` §4.4) | When ANY cap bundle is mounted but `cap.claims.spawn = ['*']`, all `Agent.spec.allowedChildAgents` checks are SKIPPED. A developer-set wide-open cap bypasses the GitOps allowlist. | `packages/agent-pod/src/builtin-tools-spawn.ts:283-294,262`. | Keep guardrails 2+3 as defense-in-depth even when `parentCap` is mounted. |
| M7 | MEDIUM | code | "mTLS / SPIFFE identity for agent-pod ↔ gateway" (`docs/ROADMAP.md` Wave 4 v0.4.3-identity) | mTLS probe is structurally optimistic — gateways that accept-and-ignore client certs are silently treated as mTLS-supporting. Audit + observability records claim per-task identity attestation that didn't actually happen. | `packages/agent-pod/src/svid-client.ts:249-298`. | Have the gateway emit `X-Kagent-Identity-Verified: spiffe://...` on success; until then, log probe at WARN with `mtlsSupported: true (UNVERIFIED)`. |
| M8 | MEDIUM | code | "write_artifact registers at boot when Helm is configured" (implicit) | `write_artifact` resolves env at handler-call time, not boot. Misconfig only surfaces on FIRST call (potentially many minutes into a run). | `packages/agent-pod/src/builtin-tools.ts:807-844,675`. | Lift `resolveWriterEnvOrDisabled` out of handler into `buildBuiltinToolRegistry`; refuse registration when env is missing. |
| M9 | MEDIUM | code | "CAS reads bounded by READ_ARTIFACT_MAX_BYTES" (8 MiB) | `PvcCasBackend.read` returns full `Buffer` from `readFileSync` — 500 MB blob loads into RSS even though the cap would refuse it on the way out. OOMKilled before cap fires. | `packages/agent-pod/src/cas-backend.ts:232-245`; `builtin-tools.ts:1169` (cap). | fstat before `readFileSync`; refuse with clear error if size exceeds `READ_ARTIFACT_MAX_BYTES`. |
| M10 | MEDIUM | code | "Job/Pod failure reflection protects against stuck tasks" (`docs/WORKBENCH.md` §3.6) | OOMKilled / NetworkPolicy egress denial / DNS failure → Pod fails before `writeStatus` runs. `runConfig.timeoutSeconds` is OPTIONAL everywhere — when unset, hung LiteLLM call has no timeout. | `packages/agent-pod/src/main.ts:111,565-568`; `runner.ts:265-269`; `OpenAICompatibleLLMClient.chat` with no per-request timeout. | Stamp default `runConfig.timeoutSeconds` (e.g., 30 min) at admission time when none provided; add SIGTERM grace window where `main.ts` flushes a best-effort `Failed` patch. |
| M11 | MEDIUM | code | (env-JSON path retired per WAVES.md §3.1) | Env-JSON fallback is still live and `useConfigMap !== false` can be silently flipped. Neither side has a deprecation timer or removal-tag. ROADMAP.md Phase 4.x bullet at line 91 still unchecked. | `packages/agent-pod/src/env.ts:466-491`; `packages/operator/src/job-spec.ts:605`; ROADMAP.md line 91. | Stamp `KAGENT_SPEC_SOURCE` annotation on Pod indicating which path operator chose; tag a removal release; check the bullet in ROADMAP. |
| M12 | MEDIUM | code | (env-only feature flags surface escalation paths) | `KAGENT_BLACKBOARD_FAIL_OPEN=true` flag silently grants `read: ['*'], write: ['*']` to ANY Agent. Cluster-wide, no per-Agent gate. | `packages/agent-pod/src/main.ts:389-393`. | Log WARN at boot when set; gate behind explicit `acknowledgeUnsafe: true` Helm value. |
| M13 | MEDIUM | code | "Verifier surfaces structured reason on failure" | `runLlmJudgeVerifier` doesn't retry transient 502/503 from Langfuse; non-`text` prompt type reasons lose context. | `packages/operator/src/verifier.ts`; `main.ts:3585-3593` (`buildLangfusePromptFetcherForOperator`). | Add 3-attempt exponential retry for 502/503; surface "wrong prompt type" as structured reason. |
| M14 | MEDIUM | code | "Cluster snapshot is performant" (`docs/WORKBENCH.md` §6) | `/api/cluster/snapshot` issues uncached cluster-wide `listNode()` on every call; no rate-limiting on any workbench-api endpoint. | `packages/workbench-api/src/routes/cluster.ts:309`. | Wrap `listNode` response in 5s-TTL cache; longer-term switch to node informer; add per-user rate-limiter. |
| M15 | MEDIUM | secret | "Gateway usage is safe to surface to workbench users" | `/api/gateway/usage` proxies gateway `error_message` rows verbatim — combined with H15, leaked upstream content surfaces to authenticated workbench users. | `packages/workbench-api/src/routes/gateway.ts:274-310`; `gateway-client.ts:146-159`. | Scrub `errorMessage` in the workbench-api projection (regex `sk-[A-Za-z0-9_-]+` → `sk-***`). |
| M16 | MEDIUM | dos | "Workbench SSE stream is the live update channel" (`docs/WORKBENCH.md` §5) | `/api/stream` (SSE) has no per-client connection cap or per-user rate limit. 10k connections × 100 cache events/sec = 1M callbacks/sec. | `packages/workbench-api/src/routes/stream.ts:45-82`; `cache.ts:185`. | Cap concurrent connections per user (e.g., 5) and total (e.g., 1000). |
| M17 | MEDIUM | auth | "WORKBENCH_AUTH_REQUIRED is fail-closed" (auth.ts:65) | `auth.ts:65` checks `raw !== 'false'`. Helm's `quote` pipeline yields `value: "false"` for boolean YAML overrides — auth silently disables when operator sets `api.authRequired: false` thinking they're being explicit. | `packages/workbench-api/src/auth.ts:65`; chart `values.yaml:73`; `deployment.yaml:120`. | Add `validateValues` template that fails if `.Values.api.authRequired != "true"` AND `acknowledgeUnauthenticated: true` is not set. |
| M18 | MEDIUM | crypto | "JTI is opaque per-task identifier" | `defaultJti` uses 4 bytes (~32 bits) of random suffixed to an 8-char prefix from task UID. If downstream consumer trusts JTI as unguessable (e.g., webhook secret), 32 bits is insufficient. | `packages/operator/src/cap-ca.ts:451-463`. | Use 16 bytes for JTI random suffix. |
| M19 | MEDIUM | code | "Admin endpoints handle malformed input cleanly" | `parseRevokeIdFromUrl` does NOT validate `id` is numeric — Postgres pg's parameterized query is safe from SQLi but pg throws parse error → generic 500 + Postgres error text exposure. | `packages/llm-gateway/src/admin-routes.ts:291-300`. | Validate `id` is numeric in `parseRevokeIdFromUrl`. |
| M20 | MEDIUM | code | "ModelEndpoint admission rejects collisions" (implicit) | Two `ModelEndpoint` CRs with same `(spec.model, spec.backendUrl)` survive admission → AIMD has duplicate entries while ModelIndex has one → workbench join silently drops the second CR with no warning. | `packages/llm-gateway/src/model-watch.ts:80-88` + `model-index.ts:36-38` + `packages/workbench-api/src/routes/gateway.ts:108-142`. | Add admission rule in `packages/operator/src/admission.ts` rejecting collision. |
| M21 | MEDIUM | code | "Operator informer errors are observable" | `informer.onError` swallowed with single `console.error`. No metric, no readiness probe flip, no audit event. Wedged informer is invisible until tasks visibly stop processing. | `packages/operator/src/main.ts:939-941`. | Emit `substrate.informer_error` audit event from `onError`; expose `/healthz` returning 500 if no informer event has fired in N minutes. |
| M22 | MEDIUM | code | "Verifier and supervision audit fields are stable" | `task.spec.targetAgent` referenced in audit hooks blow up to `undefined` when capability-targeted. | `packages/operator/src/verifier.ts:1017,1039,1063`; `supervision-router.ts:786,812`. | Thread resolved `agent.metadata.name` from `resolveTargetAgent` into the audit hook. |
| M23 | MEDIUM | code | (admin token surface narrow) | `gateway-client.ts:120` uses workbench's gateway-admin token for every gateway call. Memory-disclosure CVE in any node module leaks both contexts. | `packages/workbench-api/src/gateway-client.ts:120`; `main.ts:125`. | Issue a workbench-bound API key via `POST /admin/keys` with `modelAllowlist=[]` and use that for workbench reads. |

---

## LOW / NIT

| # | severity | category | claim | finding | evidence | recommendation |
|---|---|---|---|---|---|---|
| L1 | LOW | naming | "kagent.spawn_child_task" prefix in AGENT-SELF-SERVICE.md §11 (Q1) | Tools are bare-named (`spawn_child_task`); doc-stated prefix doesn't ship. MCP-server collision possible. | `packages/agent-pod/src/builtin-tools-spawn.ts:186`; `wait_for_*` similar. | Rename to `kagent_spawn_child_task` or document substrate-tool name reservation. |
| L2 | LOW | crypto | (default JWKS over HTTPS) | `defaultFetchJwks` does not pin URL to HTTPS; default is `http://kagent-template...:8081`. | `packages/agent-pod/src/cap-consumer.ts:196-210`. | Document in-cluster trust assumption explicitly OR add mTLS path. |
| L3 | LOW | input | "Artifact names are validated" | `validateArtifactName` accepts Unicode bidi-override (U+202E etc.) — deceptive trace UI display. | `packages/agent-pod/src/artifacts.ts:180-185`. | Blacklist U+202A-U+202E, U+2066-U+2069. |
| L4 | LOW | crypto | "MAX_ESCALATION_DEPTH is configurable" | Hardcoded 8 in supervision-router; not env-overridable. | `packages/operator/src/supervision-router.ts:282`. | Make env-overridable. |
| L5 | LOW | secret | "parseSecurityContextEnv fails closed on misconfig" | console.warn + fall back to undefined when env malformed. | `packages/operator/src/main.ts:552-570`. | For security-relevant config, fail-closed (CrashLoop). |
| L6 | LOW | code | "IdempotencyCache uses LRU eviction" | `seenCompleted.size > 10_000 → seenCompleted.clear()` is a memory bound, not LRU. | `packages/operator/src/main.ts:2501-2503`. | Switch to LRU or per-task TTL. |
| L7 | LOW | code | "ClusterRole is least-privilege" | `secrets: [get,create,patch,delete]` is cluster-wide even when operator watches a single namespace. | `packages/operator/charts/kagent-operator/templates/clusterrole.yaml:173-175`. | Split into Role for in-namespace + ClusterRole for cluster-wide, conditional on `Values.watchAllNamespaces`. |
| L8 | LOW | code | "createChildTask returns canonical UID" | `createChildTask` swallows missing `created.metadata.uid` and returns `uid: ''` — downstream waits on uid='' will timeout. | `packages/agent-pod/src/k8s-task-creator.ts:267-271`. | Assert `meta.uid !== undefined`; throw clear error. |
| L9 | LOW | code | "Conformance harness mirrors a real probe" | Mostly mocked-only; `evaluateMtlsSvidFallback` reads its result from input. (Same root as H19; LOW because the rename-to-`record*` solves it cosmetically.) | `packages/llm-gateway/src/conformance.ts:116-153`. | (See H19.) |
| L10 | LOW | code | (refusal detection covers known providers) | `detectRefusal` hard-codes English refusal phrases. Non-English models invisible. | `packages/agent-loop/src/detectors/refusal.ts:26-48`. | Add per-locale phrase set OR document English-only assumption. |
| L11 | LOW | dockerfile | (image is minimal) | `packages/agent-pod/Dockerfile:120` copies entire `/repo` (including all tests, dev fixtures) into runtime image. | Same. | Copy only `packages/*/dist/`, `packages/*/package.json`, prod-only `node_modules`. |
| L12 | LOW | code | (stub providers fail loudly) | `bedrock-provider.ts` stub THROWs on every call; misconfig at runtime, not admission. | `packages/llm-gateway/src/providers/bedrock-provider.ts:40,45`. | Admission-reject `ModelEndpoint.spec.backendKind: bedrock` until adapter implemented. |
| L13 | LOW | code | (workbench preview is bounded) | `cluster.ts:303 lastResultPreview` returns 200 chars of arbitrary task output to authenticated viewer. If results carry secrets (loose-prompted LLMs), info disclosure. | `packages/workbench-api/src/routes/cluster.ts:303`. | Either don't show preview, OR scrub via the same regex used elsewhere. |
| L14 | LOW | code | (executor latency is accurate) | `chatWithRetry`'s post-attempt `llmStart = Date.now()` rewrite at `executor.ts:540-551` produces misleading per-attempt latency. | `packages/agent-loop/src/executor.ts:540-551`. | Have `chatWithRetry` return success-attempt's actual start time. |
| L15 | LOW | code | (X-Kagent-* headers represent attribution) | `headers.ts:34-55 parseKagentHeaders` reads `X-Kagent-Task-UID` / `X-Kagent-Agent` from EVERY caller and writes to `usage_records.task_uid` / `agent_name`. Attribution is lieable. | `packages/llm-gateway/src/headers.ts:34-55`. | Document explicitly in GATEWAY-CONTRACT.md that these are advisory, not authoritative. |
| L16 | LOW | code | (verifier returns clean message on infra error) | `verifier.ts:801 guessExitCode` parses Job condition message string for "exit code N" — fragile cross-K8s-version. | Same. | Pin tests against target K8s versions. |
| L17 | LOW | code | "tasks.ts surfaces clean errors" | `tasks.ts:262` returns `K8s API call failed: ${err.message}` directly to caller — minor info disclosure. | `packages/workbench-api/src/routes/tasks.ts:262`. | Sanitize before return. |

---

## Strategic-only (not code)

| # | severity | category | claim | finding | evidence | recommendation |
|---|---|---|---|---|---|---|
| S1 | HIGH | strategic | "Naming note: kagent.dev (Solo.io) is K8s-ops-agents — different domain" (`README.md` §Naming note) | Solo.io kagent now has 2,689★, CNCF Sandbox status, an `Agent` CRD, AND a `SandboxAgent` CRD that integrates kubernetes-sigs/agent-sandbox. The framing "different domain" is no longer defensible; the substrate primitives directly overlap. | https://github.com/kagent-dev/kagent/blob/main/go/api/v1alpha2/sandboxagent_types.go. | Rename pre-public-release. Of the candidates in CLAUDE.md (`agentforge`, `kfarm`, `agentpod`, `podforge`), `agentpod` collides with the k8s-sigs Sandbox CR concept; `kfarm` and `podforge` are clean. |
| S2 | MEDIUM | strategic | "Kata Containers v0.2 — agentPod.runtimeClasses.strict='kata' once Kata is deployed" (`docs/DESIGN-V0.1.md` §4.4, `docs/PRIOR-ART.md` §3.4) | Operator-side wiring confirmed at `job-spec.ts:837-849`. Cluster-side install still deferred. **No published cold-start / concurrency benchmark exists for the runc OR Kata path.** | Same; `evidence/` lacks benchmarks. | Publish baseline benchmarks (cold-start latency, ~50/100/500 concurrent pods on K3s) before any production-scale framing. |
| S3 | MEDIUM | strategic | "v0.1 ships when the comparison rig proves no-regression" (`docs/WHY.md` §6) | Tags reach `v0.5.4-keyrotation` but the comparison rig has not run; `docs/V0.1-COMPARISON.md` does not exist. The pre-committed gate is unfulfilled. | `git tag --sort=-creatordate`; `ls docs/V0.1-COMPARISON.md` returns no file. | Either: (a) run rig, publish numbers, declare v0.1 shipped; (b) update WHY.md §6 to retire the falsifiable framing and replace with substrate-completeness. |
| S4 | LOW | strategic | "We don't reinvent — we use LiteLLM, Langfuse, Restate" (`docs/PRIOR-ART.md` §1) | The bundled `@kagent/llm-gateway` package (157 LOC main + 405 LOC server + ~10 providers) duplicates LiteLLM's job for the OSS distribution. README admits "OSS bundle is dev-convenience only" but the existence + complexity is at odds with the "use LiteLLM" framing. | `packages/llm-gateway/src/*`; `docs/SUBSTRATE-V1.md` §2 ("kagent ships an OSS bundled gateway for OSS / dev convenience"). | Document scope explicitly: when does a deployment use bundled vs. LiteLLM vs. enterprise? Avoid two-supported-paths drift. |
| S5 | NIT | strategic | (kagent-loop is the in-pod runtime) | `@kagent/agent-loop` ~3,000 LOC reproduces primitives Vercel AI SDK / Strands TS / Pydantic AI ship for free. The four narrow places it earns its keep (`detectors/`, `chatWithRetry`, `registry.ts`, `TraceSink`) could be ~600 LOC of middleware rather than a whole executor. | R3 audit. | Demote `@kagent/agent-loop` to `@kagent/agent-loop-default`; ship a `@kagent/agent-loop-vercel-ai` reference adapter; market kagent as substrate-with-default-runtime, not substrate-and-runtime. |

---

*End of punchlist. Cross-references: every row maps to a section in [`AUDIT-2026-05-06.md`](./AUDIT-2026-05-06.md).*
